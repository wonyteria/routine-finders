<div class="space-y-10">
  <div class="flex items-center justify-between">
    <h2 class="text-xl font-black text-slate-900">참여자 목록 (<%= participants.count %>명)</h2>
    <div class="flex items-center gap-2">
      <span class="w-3 h-3 bg-emerald-500 rounded-full animate-pulse"></span>
      <span class="text-xs font-bold text-slate-400">현재 <%= participants.count %>명이 함께하고 있습니다</span>
    </div>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    <% participants.each do |p| %>
      <div class="p-6 bg-white border border-slate-100 rounded-[32px] hover:border-indigo-100 transition-all group">
        <div class="flex items-center gap-4">
          <div class="relative shrink-0">
            <img src="<%= p.user.profile_image %>" class="w-14 h-14 rounded-2xl object-cover shadow-sm bg-slate-50" alt="">
            <% if p.completion_rate.to_i >= 90 %>
              <div class="absolute -top-1 -right-1 w-6 h-6 bg-amber-400 rounded-full border-2 border-white flex items-center justify-center text-[10px] shadow-sm">👑</div>
            <% end %>
          </div>
          <div class="flex-1 min-w-0">
            <p class="font-black text-slate-900 truncate"><%= p.user.nickname %></p>
            <div class="flex items-center gap-2 mt-1">
              <span class="text-[10px] font-black text-indigo-600 bg-indigo-50 px-2 py-0.5 rounded-md">Lv.<%= p.user.level %></span>
              <span class="text-[10px] text-slate-400 font-bold"><%= time_ago_in_words(p.joined_at) %>전 참여</span>
            </div>
          </div>
        </div>

        <div class="mt-6 space-y-3">
          <div class="flex items-center justify-between text-[11px] font-black uppercase tracking-widest">
            <span class="text-slate-400">Completion</span>
            <span class="text-indigo-600"><%= p.completion_rate.to_i %>%</span>
          </div>
          <div class="h-1.5 bg-slate-100 rounded-full overflow-hidden">
            <div class="h-full bg-indigo-500 transition-all duration-1000" style="width: <%= p.completion_rate.to_i %>%"></div>
          </div>
          <div class="flex items-center justify-between mt-2">
            <div class="flex items-center gap-1.5">
              <svg class="w-3.5 h-3.5 text-amber-500" fill="currentColor" viewBox="0 0 24 24"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <span class="text-xs font-bold text-slate-600"><%= p.current_streak %>일 연속</span>
            </div>
            <% if p.status == "failed" %>
              <span class="text-[10px] font-black text-rose-500 uppercase tracking-widest">Failed</span>
            <% end %>
          </div>
        </div>
      </div>
    <% end %>

    <% if participants.empty? %>
      <div class="col-span-full py-20 text-center space-y-4">
        <div class="w-20 h-20 bg-slate-100 rounded-[32px] flex items-center justify-center mx-auto text-slate-300">
          <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"/></svg>
        </div>
        <p class="text-slate-400 font-bold italic">아직 참여자가 없습니다.<br>이 챌린지의 첫 번째 주인공이 되어보세요!</p>
      </div>
    <% end %>
  </div>
</div>
