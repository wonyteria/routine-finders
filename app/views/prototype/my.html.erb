<div class="space-y-10 pt-2 pb-10">
  <!-- 1. Profile Summary -->
  <section class="text-center space-y-4">
    <div class="relative inline-block">
      <div class="w-24 h-24 rounded-[32px] border-4 border-indigo-500 overflow-hidden shadow-2xl relative z-10">
        <img src="<%= current_user&.profile_image %>" class="w-full h-full object-cover">
      </div>
      <div class="absolute -bottom-2 -right-2 bg-indigo-500 text-white w-8 h-8 rounded-full flex items-center justify-center text-sm font-black border-4 border-[#0C0B12] z-20">Lv.<%= current_user&.level || 1 %></div>
    </div>
    <div class="space-y-1.5 px-6">
      <h2 class="text-xl font-black text-white"><%= current_user&.nickname %></h2>
      <p class="text-[11px] font-bold text-slate-400 leading-relaxed"><%= current_user&.bio.presence || "나를 표현하는 한 줄을 입력해보세요" %></p>
      
      <% if current_user&.sns_links.present? && current_user.sns_links.values.any?(&:present?) %>
        <div class="flex justify-center gap-2 pt-1">
          <% if current_user.instagram.present? %>
            <%= link_to current_user.instagram.start_with?('http') ? current_user.instagram : "https://instagram.com/#{current_user.instagram.delete_prefix('@')}", target: "_blank", class: "w-7 h-7 rounded-lg bg-white/5 flex items-center justify-center text-slate-400 hover:text-white hover:bg-white/10 transition-all border border-white/5" do %>
              <svg class="w-3.5 h-3.5" fill="currentColor" viewBox="0 0 24 24"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/></svg>
            <% end %>
          <% end %>
          <% if current_user.threads.present? %>
            <%= link_to current_user.threads.start_with?('http') ? current_user.threads : "https://threads.net/@#{current_user.threads.delete_prefix('@')}", target: "_blank", class: "w-7 h-7 rounded-lg bg-white/5 flex items-center justify-center text-slate-400 hover:text-white hover:bg-white/10 transition-all border border-white/5" do %>
              <svg class="w-3.5 h-3.5" fill="currentColor" viewBox="0 0 24 24"><path d="M12.186 24h-.007c-3.581-.024-6.334-1.205-8.184-3.509C2.35 18.44 1.5 15.586 1.472 12.01v-.017c.03-3.579.879-6.43 2.525-8.482C5.845 1.205 8.6.024 12.18 0h.014c2.746.02 5.043.725 6.826 2.098 1.677 1.29 2.858 3.13 3.509 5.467l-2.04.569c-1.104-3.96-3.898-5.984-8.304-6.015-2.91.022-5.11.936-6.54 2.717C4.307 6.504 3.616 8.914 3.589 12c.027 3.086.718 5.496 2.057 7.164 1.43 1.783 3.631 2.698 6.54 2.717 2.623-.02 4.358-.631 5.8-2.045 1.647-1.613 1.618-3.593 1.09-4.798-.31-.71-.873-1.3-1.634-1.75-.192 1.352-.622 2.446-1.284 3.272-.886 1.102-2.14 1.704-3.73 1.79-1.202.065-2.361-.218-3.259-.801-1.063-.689-1.685-1.74-1.752-2.96-.065-1.17.408-2.253 1.332-3.05.857-.738 2.017-1.168 3.456-1.282.989-.078 1.998-.03 3.002.112v-.49c0-1.072-.249-1.896-.74-2.45-.51-.574-1.26-.867-2.232-.871h-.053c-.86.004-1.593.263-2.177.77a3.07 3.07 0 00-.942 1.323l-1.972-.532c.396-1.02 1.028-1.878 1.877-2.55 1.003-.793 2.227-1.196 3.64-1.196h.072c1.712.012 3.076.585 4.053 1.702.898 1.027 1.354 2.413 1.354 4.12v1.96c0 .104.002.21.005.318.012.432.025.876.025 1.322 0 .61-.027 1.178-.083 1.706a3.939 3.939 0 01-.234 1.004c.59.394 1.1.868 1.512 1.412.866 1.146 1.178 2.602.878 4.1-.401 2.004-1.61 3.666-3.404 4.678-1.446.816-3.21 1.229-5.25 1.229zm-.065-8.103c-.073 0-.145.002-.218.005-.94.05-1.666.326-2.158.82-.472.475-.69 1.073-.65 1.778.036.641.318 1.178.838 1.598.567.456 1.353.688 2.34.637 1.107-.06 1.965-.473 2.55-1.228.437-.564.728-1.305.866-2.205-.02-.01-.04-.018-.06-.027a7.966 7.966 0 00-3.508-.378z"/></svg>
            <% end %>
          <% end %>
          <% if current_user.blog.present? %>
            <%= link_to current_user.blog.start_with?('http') ? current_user.blog : "https://#{current_user.blog}", target: "_blank", class: "w-7 h-7 rounded-lg bg-white/5 flex items-center justify-center text-slate-400 hover:text-white hover:bg-white/10 transition-all border border-white/5" do %>
              <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10l4 4v10a2 2 0 01-2 2z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 8h4m-4 4h8"/></svg>
            <% end %>
          <% end %>
          <% if current_user.youtube.present? %>
            <%= link_to current_user.youtube.start_with?('http') ? current_user.youtube : "https://youtube.com/#{current_user.youtube.delete_prefix('@')}", target: "_blank", class: "w-7 h-7 rounded-lg bg-white/5 flex items-center justify-center text-slate-400 hover:text-white hover:bg-white/10 transition-all border border-white/5" do %>
              <svg class="w-3.5 h-3.5" fill="currentColor" viewBox="0 0 24 24"><path d="M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"/></svg>
            <% end %>
          <% end %>
          <% if current_user.twitter.present? %>
            <%= link_to current_user.twitter.start_with?('http') ? current_user.twitter : "https://x.com/#{current_user.twitter.delete_prefix('@')}", target: "_blank", class: "w-7 h-7 rounded-lg bg-white/5 flex items-center justify-center text-slate-400 hover:text-white hover:bg-white/10 transition-all border border-white/5" do %>
              <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 24 24"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
            <% end %>
          <% end %>
        </div>
      <% end %>
    </div>

    <!-- Quick Stats -->
    <div class="flex justify-center gap-8 pt-4">
      <div class="text-center">
        <p class="text-lg font-black text-white tabular-nums"><%= @total_activities %></p>
        <p class="text-[9px] font-bold text-slate-600 uppercase tracking-widest">활동 기록</p>
      </div>
      <div class="w-px h-8 bg-white/10"></div>
      <div class="text-center">
        <p class="text-lg font-black text-white tabular-nums"><%= @current_streak %>일</p>
        <p class="text-[9px] font-bold text-slate-600 uppercase tracking-widest">연속 성공</p>
      </div>
      <div class="w-px h-8 bg-white/10"></div>
      <div class="text-center">
        <p class="text-lg font-black text-white tabular-nums"><%= @current_month_points %><%= current_user&.is_rufa_club_member? ? 'P' : '' %></p>
        <p class="text-[9px] font-bold text-slate-600 uppercase tracking-widest"><%= current_user&.is_rufa_club_member? ? '월간 포인트' : '이번 달 점수' %></p>
      </div>
    </div>
  </section>

  <!-- 1.5. Pending Membership Notice -->
  <% if current_user&.routine_club_members&.where(payment_status: :pending)&.exists? %>
    <% pending_member = current_user.routine_club_members.where(payment_status: :pending).first %>
    <section class="px-2 animate-badge-pop">
      <div class="app-glass-card p-5 bg-amber-500/10 border-amber-500/30 flex items-center justify-between gap-4">
        <div class="flex items-center gap-4">
          <div class="w-12 h-12 bg-amber-500/20 rounded-2xl flex items-center justify-center text-2xl shadow-lg shadow-amber-500/10">⌛</div>
          <div>
            <div class="flex items-center gap-2">
              <h4 class="text-sm font-black text-amber-500">클럽 가입 확인 중</h4>
              <span class="px-1.5 py-0.5 bg-amber-500/20 rounded text-[8px] font-black text-amber-500">PENDING</span>
            </div>
            <p class="text-[10px] font-bold text-slate-400 mt-1 leading-tight">
              호스트가 입금을 확인하고 있습니다.<br/>
              승인이 완료되면 루파 전용 혜택을 이용할 수 있습니다.
            </p>
          </div>
        </div>
        <%= link_to guide_routine_clubs_path(source: 'prototype'), class: "shrink-0 w-8 h-8 rounded-full bg-white/5 flex items-center justify-center text-slate-400" do %>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 5l7 7-7 7"/></svg>
        <% end %>
      </div>
    </section>
  <% end %>

  <!-- 2. Goals Section (Weekly/Monthly/Yearly) -->
  <section class="space-y-4">
    <div class="flex justify-between items-center px-2">
      <h3 class="text-xs font-black text-slate-500 uppercase tracking-[0.2em]">나의 목표</h3>
      <button data-action="click->proto-ui#openModal" 
              data-modal-id="edit-goals" 
              onclick="document.getElementById('edit-goals').classList.remove('hidden'); document.body.classList.add('overflow-hidden')"
              class="text-[10px] font-bold text-indigo-400 hover:text-indigo-300 transition-colors cursor-pointer">수정</button>
    </div>

    <div class="grid grid-cols-1 gap-3">
      <% 
        week_of_month = ((Date.current.day - 1) / 7) + 1
        week_start_day = (week_of_month - 1) * 7 + 1
        current_week_start = Date.new(Date.current.year, Date.current.month, week_start_day)
        
        weekly_needs_update = current_user.weekly_goal_updated_at.nil? || current_user.weekly_goal_updated_at < current_week_start
        monthly_needs_update = current_user.monthly_goal_updated_at.nil? || current_user.monthly_goal_updated_at < Date.current.beginning_of_month
        yearly_needs_update = current_user.yearly_goal_updated_at.nil? || current_user.yearly_goal_updated_at < Date.current.beginning_of_year
      %>
      
      <!-- Weekly Goal -->
      <div class="app-glass-card p-5 <%= weekly_needs_update ? 'border-amber-500/30 bg-amber-500/5' : 'border-indigo-500/20 bg-indigo-500/5' %>">
        <div class="flex items-start justify-between mb-3">
          <div class="flex items-center gap-2">
            <span class="text-xl">🎯</span>
            <div>
              <p class="text-xs font-black text-white">주간 목표</p>
              <p class="text-[9px] font-bold text-slate-500"><%= Date.current.strftime('%Y년 %m월') %> <%= week_of_month %>주차</p>
            </div>
          </div>
          <% if weekly_needs_update %>
            <span class="px-2 py-1 bg-amber-500/20 text-amber-400 text-[8px] font-black rounded-full">갱신 필요</span>
          <% end %>
        </div>
        <% if current_user.weekly_goal.present? %>
          <p class="text-sm font-medium text-slate-300 leading-relaxed"><%= current_user.weekly_goal %></p>
        <% else %>
          <p class="text-sm font-medium text-slate-500 italic">이번 주 목표를 설정해보세요</p>
        <% end %>
      </div>

      <!-- Monthly Goal -->
      <div class="app-glass-card p-5 <%= monthly_needs_update ? 'border-amber-500/30 bg-amber-500/5' : 'bg-white/[0.02]' %>">
        <div class="flex items-start justify-between mb-3">
          <div class="flex items-center gap-2">
            <span class="text-xl">🌙</span>
            <div>
              <p class="text-xs font-black text-white">월간 목표</p>
              <p class="text-[9px] font-bold text-slate-500"><%= Date.current.strftime('%Y년 %m월') %></p>
            </div>
          </div>
          <% if monthly_needs_update %>
            <span class="px-2 py-1 bg-amber-500/20 text-amber-400 text-[8px] font-black rounded-full">갱신 필요</span>
          <% end %>
        </div>
        <% if current_user.monthly_goal.present? %>
          <p class="text-sm font-medium text-slate-300 leading-relaxed"><%= current_user.monthly_goal %></p>
        <% else %>
          <p class="text-sm font-medium text-slate-500 italic">이번 달 목표를 설정해보세요</p>
        <% end %>
      </div>

      <!-- Yearly Goal -->
      <div class="app-glass-card p-5 <%= yearly_needs_update ? 'border-amber-500/30 bg-amber-500/5' : 'bg-white/[0.02]' %>">
        <div class="flex items-start justify-between mb-3">
          <div class="flex items-center gap-2">
            <span class="text-xl">⭐</span>
            <div>
              <p class="text-xs font-black text-white">연간 목표</p>
              <p class="text-[9px] font-bold text-slate-500"><%= Date.current.year %>년</p>
            </div>
          </div>
          <% if yearly_needs_update %>
            <span class="px-2 py-1 bg-amber-500/20 text-amber-400 text-[8px] font-black rounded-full">갱신 필요</span>
          <% end %>
        </div>
        <% if current_user.yearly_goal.present? %>
          <p class="text-sm font-medium text-slate-300 leading-relaxed"><%= current_user.yearly_goal %></p>
        <% else %>
          <p class="text-sm font-medium text-slate-500 italic">올해 목표를 설정해보세요</p>
        <% end %>
      </div>
    </div>
  </section>

  <section class="space-y-4" data-controller="growth-filter">
    <div class="flex justify-between items-center px-2">
      <h3 class="text-xs font-black text-slate-500 uppercase tracking-[0.2em]">성장 분석</h3>
      <div class="flex bg-white/5 p-1 rounded-xl gap-1">
        <% ['주간', '월간', '연간'].each_with_index do |label, i| %>
          <button data-action="click->growth-filter#change" 
                  data-filter-id="<%= ['week', 'month', 'year'][i] %>"
                  onclick="const container = this.closest('section'); 
                           container.querySelectorAll('[data-filter-id]').forEach(b => b.classList.remove('bg-indigo-500', 'text-white'));
                           container.querySelectorAll('[data-filter-id]').forEach(b => b.classList.add('bg-transparent', 'text-slate-500'));
                           this.classList.replace('bg-transparent', 'bg-indigo-500');
                           this.classList.replace('text-slate-500', 'text-white');
                           
                           // Update Card Content
                           const periodText = container.querySelector('.growth-period-text');
                           const mainValue = container.querySelector('.growth-main-value');
                           const graphContainer = container.querySelector('.growth-graph-container');
                           
                            if('<%= ['week', 'month', 'year'][i] %>' === 'week') {
                              periodText.innerText = '이번 주 달성률';
                              mainValue.innerHTML = '<%= @weekly_completion %>% <% if @weekly_growth >= 0 %><span class=&quot;text-xs text-emerald-500 font-bold ml-1&quot;>↑ <%= @weekly_growth %>%</span><% else %><span class=&quot;text-xs text-red-500 font-bold ml-1&quot;>↓ <%= @weekly_growth.abs %>%</span><% end %>';
                              graphContainer.innerHTML = `<%= @weekly_data.zip(@weekly_labels).map.with_index { |(h, label), idx| "<div class='flex-1 h-full relative group/bar flex flex-col justify-end'><div class='rounded-t-lg transition-all duration-700 #{idx == @daily_index ? 'bg-indigo-500 shadow-[0_0_20px_rgba(124,77,255,0.4)]' : 'bg-white/10 group-hover/bar:bg-white/20'}' style='height: #{[h, 15].max}%'></div><div class='absolute -top-10 left-1/2 -translate-x-1/2 bg-slate-900 text-white text-[10px] font-black py-1.5 px-2.5 rounded-lg opacity-0 group-hover/bar:opacity-100 transition-all pointer-events-none z-30 shadow-xl border border-white/10 transform translate-y-2 group-hover/bar:translate-y-0 whitespace-nowrap'>#{label} • #{h}%</div></div>" }.join.html_safe %>`;
                            } else if ('<%= ['week', 'month', 'year'][i] %>' === 'month') {
                              periodText.innerText = '이번 달 달성률';
                              mainValue.innerHTML = '<%= @monthly_completion %>% <% if @monthly_growth >= 0 %><span class=&quot;text-xs text-emerald-500 font-bold ml-1&quot;>↑ <%= @monthly_growth %>%</span><% else %><span class=&quot;text-xs text-red-500 font-bold ml-1&quot;>↓ <%= @monthly_growth.abs %>%</span><% end %>';
                              graphContainer.innerHTML = `<%= @monthly_data.zip(@monthly_labels).map.with_index { |(h, label), idx| "<div class='flex-1 h-full relative group/bar flex flex-col justify-end'><div class='rounded-t-lg transition-all duration-700 #{idx == @weekly_index ? 'bg-indigo-500 shadow-[0_0_20px_rgba(124,77,255,0.4)]' : 'bg-white/10 group-hover/bar:bg-white/20'}' style='height: #{[h, 15].max}%'></div><div class='absolute -top-10 left-1/2 -translate-x-1/2 bg-slate-900 text-white text-[10px] font-black py-1.5 px-2.5 rounded-lg opacity-0 group-hover/bar:opacity-100 transition-all pointer-events-none z-30 shadow-xl border border-white/10 transform translate-y-2 group-hover/bar:translate-y-0 whitespace-nowrap'>#{label} • #{h}%</div></div>" }.join.html_safe %>`;
                            } else {
                              periodText.innerText = '올해 달성률';
                              mainValue.innerHTML = '<%= @yearly_completion %>% <% if @yearly_growth >= 0 %><span class=&quot;text-xs text-emerald-500 font-bold ml-1&quot;>↑ <%= @yearly_growth %>%</span><% else %><span class=&quot;text-xs text-red-500 font-bold ml-1&quot;>↓ <%= @yearly_growth.abs %>%</span><% end %>';
                              graphContainer.innerHTML = `<%= @yearly_data.zip(@yearly_labels).map.with_index { |(h, label), idx| "<div class='flex-1 h-full relative group/bar flex flex-col justify-end'><div class='rounded-t-lg transition-all duration-700 #{idx == @monthly_index ? 'bg-indigo-500 shadow-[0_0_20px_rgba(124,77,255,0.4)]' : 'bg-white/10 group-hover/bar:bg-white/20'}' style='height: #{[h, 15].max}%'></div><div class='absolute -top-10 left-1/2 -translate-x-1/2 bg-slate-900 text-white text-[10px] font-black py-1.5 px-2.5 rounded-lg opacity-0 group-hover/bar:opacity-100 transition-all pointer-events-none z-30 shadow-xl border border-white/10 transform translate-y-2 group-hover/bar:translate-y-0 whitespace-nowrap'>#{label} • #{h}%</div></div>" }.join.html_safe %>`;
                            }
                          "
                  class="px-3 py-1.5 rounded-lg text-[10px] font-black transition-all <%= i == 0 ? 'bg-indigo-500 text-white' : 'bg-transparent text-slate-500' %>">
            <%= label %>
          </button>
        <% end %>
      </div>
      <% if current_user&.is_rufa_club_member? %>
        <%= link_to achievement_report_path, class: "text-[10px] font-black text-indigo-400 hover:text-indigo-300 transition-colors cursor-pointer ml-auto flex items-center gap-1" do %>
          리포트 상세 보기
          <svg class="w-2.5 h-2.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9 5l7 7-7 7"/></svg>
        <% end %>
      <% else %>
        <button onclick="showPremiumNudge('report')" class="text-[10px] font-black text-indigo-400 hover:text-indigo-300 transition-colors ml-auto flex items-center gap-1 opacity-80 hover:opacity-100">
          리포트 상세 보기
          <span class="w-3 h-3 bg-slate-600 rounded-full flex items-center justify-center text-[7px] text-white">🔒</span>
        </button>
      <% end %>
    </div>

    <div class="app-glass-card p-6 aspect-video bg-gradient-to-br from-indigo-500/5 to-transparent flex flex-col justify-between cursor-pointer active:scale-[0.98] transition-all"
         data-action="click->proto-ui#openModal" 
         data-modal-id="growth-detail"
         onclick="document.getElementById('growth-detail').classList.remove('hidden'); document.body.classList.add('overflow-hidden')">
      <div class="flex justify-between items-start">
        <div class="space-y-1">
          <p class="growth-period-text text-[10px] font-bold text-slate-500">이번 주 달성률</p>
          <h4 class="growth-main-value text-2xl font-black text-white">
            <%= @weekly_completion %>% 
            <% if @weekly_growth > 0 %>
              <span class="text-xs text-emerald-500 font-bold ml-1">↑ <%= @weekly_growth %>%</span>
            <% elsif @weekly_growth < 0 %>
              <span class="text-xs text-red-500 font-bold ml-1">↓ <%= @weekly_growth.abs %>%</span>
            <% else %>
              <span class="text-xs text-slate-500 font-bold ml-1">-</span>
            <% end %>
          </h4>
        </div>
        <div class="w-10 h-10 bg-white/5 rounded-xl flex items-center justify-center text-xl">📈</div>
      </div>
      
      <!-- Real Data Graph -->
      <div class="growth-graph-container flex items-end justify-between h-32 gap-2 mt-4 px-1">
         <% @weekly_data.zip(@weekly_labels).each_with_index do |(h, label), i| %>
           <div class="flex-1 h-full relative group/bar flex flex-col justify-end">
             <div class="rounded-t-lg transition-all duration-700 <%= i == @daily_index ? 'bg-indigo-500 shadow-[0_0_20px_rgba(124,77,255,0.4)]' : 'bg-white/10 group-hover/bar:bg-white/20' %>" style="height: <%= [h, 15].max %>%"></div>
             <!-- Tooltip -->
             <div class="absolute -top-10 left-1/2 -translate-x-1/2 bg-slate-900 text-white text-[10px] font-black py-1.5 px-2.5 rounded-lg opacity-0 group-hover/bar:opacity-100 transition-all pointer-events-none z-30 shadow-xl border border-white/10 transform translate-y-2 group-hover/bar:translate-y-0 whitespace-nowrap">
                <%= label %> • <%= h %>%
             </div>
           </div>
         <% end %>
      </div>
    </div>
  </section>

  <!-- 3. Badge Collection (Premium Redesign) -->
  <section class="space-y-4">
    <div class="flex justify-between items-end px-2">
      <h3 class="text-xs font-black text-slate-500 uppercase tracking-[0.2em]">배지 보관함</h3>
      <span class="text-[10px] font-bold text-indigo-400"><%= @achievements.count %>개 보유 중</span>
    </div>
    
    <div class="app-glass-card p-6 overflow-hidden relative">
      <!-- Background Decor -->
      <div class="absolute -right-12 -top-12 w-48 h-48 bg-indigo-500/5 blur-3xl rounded-full pointer-events-none"></div>

      <!-- 1. Collection Grid -->
      <div class="grid grid-cols-3 gap-3 relative z-10">
        <% if @achievements.any? %>
          <% @achievements.each_with_index do |ub, i| %>
            <button onclick="document.getElementById('badge-detail-<%= i %>').classList.remove('hidden'); document.body.classList.add('overflow-hidden')"
                    class="aspect-square rounded-2xl bg-gradient-to-br from-white/5 to-white/[0.02] border border-white/10 hover:border-indigo-500/40 hover:from-indigo-500/20 hover:to-purple-500/20 transition-all group relative flex flex-col items-center justify-center gap-2 overflow-hidden active:scale-95 ring-1 ring-inset ring-white/5">
              <!-- Glow -->
              <div class="absolute inset-0 bg-indigo-500/20 blur-xl opacity-0 group-hover:opacity-100 transition-opacity"></div>
              
              <span class="text-3xl relative z-10 filter drop-shadow-[0_0_10px_rgba(255,255,255,0.3)] group-hover:scale-110 transition-transform duration-500"><%= ub.badge.icon_path.presence || "🏆" %></span>
              <div class="relative z-10 text-center px-1 w-full">
                <p class="text-[9px] font-bold text-slate-300 truncate group-hover:text-white transition-colors"><%= ub.badge.name %></p>
              </div>
              
              <!-- Level Tag (Absolute) -->
              <div class="absolute top-1.5 right-1.5 w-1.5 h-1.5 rounded-full bg-emerald-400 shadow-[0_0_8px_rgba(52,211,153,0.6)]"></div>
            </button>
          <% end %>
          
          <!-- Empty Slots Filler (Visual Motivation) -->
          <% (6 - @achievements.count).positive? && [(6 - @achievements.count), 3].min.times do %>
            <div class="aspect-square rounded-2xl bg-black/20 border border-white/5 flex items-center justify-center opacity-30">
              <div class="w-8 h-8 rounded-full border border-dashed border-white/20"></div>
            </div>
          <% end %>
        <% else %>
          <!-- Empty State (No badges yet) -->
          <div class="col-span-3 py-10 flex flex-col items-center justify-center text-center gap-3 bg-white/[0.02] rounded-2xl border border-dashed border-white/10">
             <div class="w-12 h-12 bg-white/5 rounded-full flex items-center justify-center text-2xl grayscale opacity-50">🏆</div>
             <div class="space-y-1">
               <p class="text-xs font-bold text-slate-400">아직 획득한 배지가 없습니다.</p>
               <p class="text-[10px] text-slate-600">루틴을 완료하고 첫 배지를 획득해보세요!</p>
             </div>
          </div>
        <% end %>
      </div>

      <!-- 2. Next Milestones (Progress List) -->
      <% if @milestones&.any? %>
        <div class="mt-8 pt-6 border-t border-white/5 space-y-4">
          <p class="text-[10px] font-bold text-slate-500 uppercase tracking-widest pl-1">도전 과제 (Next Goals)</p>
          <div class="space-y-3">
             <% @milestones.first(3).each do |m| %>
                <% progress = (m[:current].to_f / m[:target] * 100).clamp(0, 100).round %>
                <div class="p-4 bg-black/20 rounded-2xl border border-white/5 flex gap-4 items-center group hover:bg-white/[0.02] transition-colors">
                  <!-- Lock Icon Wrapper -->
                  <div class="relative w-10 h-10 shrink-0 flex items-center justify-center">
                     <span class="text-2xl grayscale opacity-50 group-hover:scale-110 transition-transform mix-blend-luminosity"><%= m[:icon] %></span>
                     <div class="absolute -right-1 -bottom-1 w-4 h-4 rounded-full bg-slate-800 border border-white/10 flex items-center justify-center text-[8px]">🔒</div>
                  </div>
                  
                  <div class="flex-1 min-w-0 space-y-2">
                    <div class="flex justify-between items-baseline">
                      <p class="text-[11px] font-bold text-slate-300 truncate"><%= m[:name] %></p>
                      <span class="text-[10px] font-black text-indigo-400"><%= progress %>%</span>
                    </div>
                    <!-- Progress Bar -->
                     <div class="h-1.5 w-full bg-white/5 rounded-full overflow-hidden">
                        <div class="h-full bg-indigo-500 rounded-full shadow-[0_0_10px_rgba(99,102,241,0.5)]" style="width: <%= progress %>%"></div>
                     </div>
                     <p class="text-[9px] font-medium text-slate-600 text-right">목표까지 <%= m[:target] - m[:current] %><%= m[:unit] %> 남음</p>
                  </div>
                </div>
             <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </section>

  <!-- 4. Host Management (Integrated Link) -->
  <section class="space-y-4">
    <% if current_user.host? || current_user.super_admin? %>
      <%= link_to hosted_challenges_path(source: 'prototype'), class: "block w-full" do %>
        <div class="app-glass-card p-6 flex items-center justify-between border-amber-500/10 bg-amber-500/5 group app-tap-active">
          <div class="flex items-center gap-4">
            <div class="w-12 h-12 bg-amber-500/20 rounded-2xl flex items-center justify-center text-2xl">⚡</div>
            <div class="text-left">
              <h4 class="text-sm font-black text-white leading-none">호스트 센터</h4>
              <p class="text-[11px] font-bold text-amber-500/60 mt-2">내가 개설한 챌린지 및 모임 관리</p>
            </div>
          </div>
          <svg class="w-5 h-5 text-amber-500/60 group-hover:text-amber-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
        </div>
      <% end %>
    <% end %>
  </section>

  <!-- 5. Settings List -->
  <section class="app-glass-card divide-y divide-white/5 overflow-hidden">
    <button type="button" 
            data-action="click->proto-ui#openModal" 
            data-modal-id="edit-profile" 
            onclick="document.getElementById('edit-profile').classList.remove('hidden'); document.body.classList.add('overflow-hidden')"
            class="w-full flex items-center justify-between p-5 hover:bg-white/[0.02] active:bg-white/5 transition-colors cursor-pointer">
      <div class="flex items-center gap-4">
        <span class="text-slate-500"><svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/></svg></span>
        <span class="text-sm font-bold text-slate-300">프로필 정보 수정</span>
      </div>
      <svg class="w-4 h-4 text-slate-700" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9 5l7 7-7 7"/></svg>
    </button>

    <% if current_user.super_admin? %>
      <!-- Super Admin Access -->
      <%= link_to prototype_admin_dashboard_path, class: "w-full flex items-center justify-between p-5 bg-rose-500/10 hover:bg-rose-500/20 active:bg-rose-500/30 transition-colors cursor-pointer group" do %>
        <div class="flex items-center gap-4">
          <span class="text-rose-400 group-hover:scale-110 transition-transform">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
          </span>
          <span class="text-sm font-black text-rose-400">시스템 총괄 관리자</span>
        </div>
        <div class="flex items-center gap-2">
          <span class="text-[9px] font-black bg-rose-500 text-white px-1.5 py-0.5 rounded tracking-tighter uppercase">ROOT</span>
          <svg class="w-4 h-4 text-rose-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9 5l7 7-7 7"/></svg>
        </div>
      <% end %>
    <% end %>

    <% if current_user.admin? %>
      <!-- Official Club Management -->
      <% @official_club = RoutineClub.official.first %>
      <% if @official_club %>
        <%= link_to prototype_admin_clubs_path, class: "w-full flex items-center justify-between p-5 bg-indigo-500/10 hover:bg-indigo-500/20 active:bg-indigo-500/30 transition-colors cursor-pointer group" do %>
          <div class="flex items-center gap-4">
            <span class="text-indigo-400 group-hover:scale-110 transition-transform">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/></svg>
            </span>
            <span class="text-sm font-black text-indigo-400">루파 클럽 운영 센터</span>
          </div>
          <div class="flex items-center gap-2">
             <span class="text-[9px] font-black bg-indigo-500 text-white px-1.5 py-0.5 rounded tracking-tighter uppercase">HOST</span>
            <svg class="w-4 h-4 text-indigo-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9 5l7 7-7 7"/></svg>
          </div>
        <% end %>
      <% end %>
    <% end %>


    <%= link_to prototype_pwa_path, class: "w-full flex items-center justify-between p-5 hover:bg-white/[0.02] active:bg-white/5 transition-colors cursor-pointer group" do %>
      <div class="flex items-center gap-4">
        <span class="text-slate-500 group-hover:text-indigo-400 transition-colors">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"/></svg>
        </span>
        <span class="text-sm font-bold text-slate-300">앱 설치 가이드</span>
      </div>
      <div class="flex items-center gap-2">
        <span class="text-[10px] font-bold text-slate-600 uppercase tracking-widest">PWA</span>
        <svg class="w-4 h-4 text-slate-700" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9 5l7 7-7 7"/></svg>
      </div>
    <% end %>

    <%= link_to "https://open.kakao.com/o/sY2xEswh", target: "_blank", rel: "noopener noreferrer", class: "w-full flex items-center justify-between p-5 hover:bg-white/[0.02] active:bg-white/5 transition-colors cursor-pointer group" do %>
      <div class="flex items-center gap-4">
        <span class="text-slate-500 group-hover:text-yellow-400 transition-colors">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/></svg>
        </span>
        <span class="text-sm font-bold text-slate-300">문의하기</span>
      </div>
      <div class="flex items-center gap-2">
        <span class="text-[10px] font-bold text-slate-600 uppercase tracking-widest">Kakao</span>
        <svg class="w-4 h-4 text-slate-700" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9 5l7 7-7 7"/></svg>
      </div>
    <% end %>

    <div data-controller="push-subscription" 
         data-push-subscription-vapid-public-key-value="BOSk9RTXuuwHy1nIfECrhja1c7jy48zRrrTnFczxmPkY7_pfm9uajihHnqvRSObUe7qpoXhNdNxRV62EUvlDBcU="
         class="w-full flex items-center justify-between p-5 hover:bg-white/[0.02] active:bg-white/5 transition-colors cursor-pointer group"
         data-action="click->push-subscription#subscribe">
      <div class="flex items-center gap-4">
        <span class="text-slate-500 group-hover:text-amber-400 transition-colors">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"/></svg>
        </span>
        <div class="text-left">
          <span class="text-sm font-bold text-slate-300 block">앱 푸시 알림 설정</span>
          <p class="text-[9px] font-medium text-slate-500 mt-0.5" data-push-subscription-target="statusText">알림 상태 확인 중...</p>
        </div>
      </div>
      <div class="relative inline-flex items-center cursor-pointer">
        <input type="checkbox" class="sr-only peer" data-push-subscription-target="toggle" disabled>
        <div class="w-9 h-5 bg-white/10 peer-focus:outline-none rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-4 after:w-4 after:transition-all peer-checked:bg-indigo-500"></div>
      </div>
    </div>

    <!-- Push Notification Guide Modal -->
    <div data-push-subscription-target="guideModal" class="hidden fixed inset-0 z-[700]">
      <div class="absolute inset-0 bg-black/90 backdrop-blur-xl" data-action="click->push-subscription#closeGuide"></div>
      <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[90%] max-w-sm app-glass-card border-white/20 p-6 space-y-6 animate-popup">
        <div class="text-center space-y-3">
          <div class="w-16 h-16 bg-gradient-to-br from-indigo-500 to-purple-500 rounded-2xl flex items-center justify-center text-3xl shadow-xl shadow-indigo-500/20 mx-auto mb-4">
            🔔
          </div>
          <h3 class="text-lg font-black text-white">알림 설정 안내</h3>
          <p class="text-xs font-medium text-slate-400 leading-relaxed break-keep">
            루틴 파인더스 알림을 받으시려면<br/>
            휴대폰 설정에서 알림 권한을 켜주세요.
          </p>
        </div>

        <div class="bg-white/5 rounded-xl p-4 space-y-2 text-left">
           <div class="flex gap-3 items-start">
             <span class="w-5 h-5 rounded-full bg-white/10 flex items-center justify-center text-[10px] shrink-0 mt-0.5">1</span>
             <p class="text-[11px] text-slate-300 leading-tight">이 팝업을 닫은 후 나오는<br/><span class="text-indigo-400 font-bold">시스템 권한 요청</span>을 허용해주세요.</p>
           </div>
           <div class="flex gap-3 items-start">
             <span class="w-5 h-5 rounded-full bg-white/10 flex items-center justify-center text-[10px] shrink-0 mt-0.5">2</span>
             <p class="text-[11px] text-slate-300 leading-tight">만약 알림이 오지 않는다면<br/><span class="text-indigo-400 font-bold">휴대폰 설정 > 알림</span>에서 켜주세요.</p>
           </div>
        </div>

        <div class="space-y-2 pt-2">
          <button data-action="click->push-subscription#confirmGuide" class="w-full py-3.5 rounded-xl bg-indigo-500 text-white font-bold text-sm hover:bg-indigo-600 transition-colors">
            확인했어요
          </button>
          <button data-action="click->push-subscription#dontShowAgain" class="w-full py-2 rounded-xl text-slate-500 font-bold text-xs hover:text-slate-400 transition-colors">
            다시 보지 않음
          </button>
        </div>
      </div>


    <%= link_to logout_path, data: { turbo_method: :delete, turbo_confirm: "로그아웃 하시겠습니까?" }, class: "w-full flex items-center justify-between p-5 hover:bg-red-500/5 active:bg-red-500/10 transition-colors cursor-pointer block" do %>
      <div class="flex items-center gap-4">
        <span class="text-red-500/50"><svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/></svg></span>
        <span class="text-sm font-bold text-red-500/80">로그아웃</span>
      </div>
    <% end %>
  </section>

  <!-- 6. Account Withdrawal (Subtle Link) -->
  <section class="flex flex-col items-center gap-6 pt-4 pb-12">
    <div class="h-px w-12 bg-white/5"></div>
    <%= link_to profile_path, data: { turbo_method: :delete, turbo_confirm: "정말로 탈퇴하시겠습니까? 모든 활동 기록과 배지가 영구적으로 삭제됩니다." }, class: "text-[10px] font-bold text-slate-700 hover:text-red-900/40 transition-colors uppercase tracking-[0.2em]" do %>
      회원 탈퇴하기
    <% end %>
  </section>
</div>

<!-- Growth Detail Modal -->
<div id="growth-detail" class="hidden fixed inset-0 z-[600]">
  <div data-action="click->proto-ui#closeModal" 
       onclick="this.closest('.fixed').classList.add('hidden'); document.body.classList.remove('overflow-hidden')"
       class="absolute inset-0 bg-black/90 backdrop-blur-xl cursor-pointer"></div>
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[90%] max-w-md app-glass-card border-white/20 p-8 space-y-6 max-h-[80vh] overflow-y-auto modal-content-safe">
    <div class="flex justify-between items-center">
      <h3 class="text-xl font-black text-white">성장 인텔리전스</h3>
      <button type="button" 
              data-action="click->proto-ui#closeModal" 
              onclick="this.closest('.fixed').classList.add('hidden'); document.body.classList.remove('overflow-hidden')"
              class="w-8 h-8 rounded-full bg-white/5 flex items-center justify-center text-slate-400 hover:text-white transition-colors cursor-pointer">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
      </button>
    </div>
    
    <div class="space-y-4" data-controller="modal-filter">
      <!-- Modal Filter -->
      <div class="flex bg-white/5 p-1 rounded-2xl gap-1">
        <% ['주간', '월간', '연간'].each_with_index do |label, i| %>
          <button onclick="const modal = this.closest('.app-glass-card');
                           modal.querySelectorAll('.modal-filter-btn').forEach(b => b.classList.remove('bg-indigo-500', 'text-white'));
                           modal.querySelectorAll('.modal-filter-btn').forEach(b => b.classList.add('bg-transparent', 'text-slate-500'));
                           this.classList.replace('bg-transparent', 'bg-indigo-500');
                           this.classList.replace('text-slate-500', 'text-white');
                           
                           const detailContent = modal.querySelector('.modal-growth-content');
                           if('<%= ['week', 'month', 'year'][i] %>' === 'week') {
                             detailContent.innerHTML = document.getElementById('mock-week-detail').innerHTML;
                           } else if ('<%= ['week', 'month', 'year'][i] %>' === 'month') {
                             detailContent.innerHTML = document.getElementById('mock-month-detail').innerHTML;
                           } else {
                             detailContent.innerHTML = document.getElementById('mock-year-detail').innerHTML;
                           }
                          "
                  class="modal-filter-btn flex-1 py-3 rounded-xl text-xs font-black transition-all <%= i == 0 ? 'bg-indigo-500 text-white' : 'bg-transparent text-slate-500' %>">
            <%= label %>
          </button>
        <% end %>
      </div>

      <div class="modal-growth-content space-y-6">
        <!-- Initial Week Content -->
        <div class="grid grid-cols-2 gap-4">
          <div class="app-glass-card p-4 bg-white/[0.02]">
            <p class="text-[10px] font-bold text-slate-500 mb-2">주간 달성률</p>
            <p class="text-2xl font-black text-white"><%= @weekly_completion %>%</p>
            <% if @weekly_growth > 0 %>
              <p class="text-xs font-bold text-emerald-500 mt-1">↑ <%= @weekly_growth %>%</p>
            <% elsif @weekly_growth < 0 %>
              <p class="text-xs font-bold text-red-500 mt-1">↓ <%= @weekly_growth.abs %>%</p>
            <% else %>
              <p class="text-xs font-bold text-slate-500 mt-1">-</p>
            <% end %>
          </div>
          <div class="app-glass-card p-4 bg-white/[0.02]">
            <p class="text-[10px] font-bold text-slate-500 mb-2">연속 기록</p>
            <p class="text-2xl font-black text-white"><%= @current_streak %>일</p>
            <p class="text-xs font-bold text-indigo-400 mt-1"><%= @current_streak > 0 ? '🔥 진행중' : '시작 대기' %></p>
          </div>
        </div>
        
        <div class="app-glass-card p-5 bg-white/[0.02] space-y-3">
          <p class="text-xs font-black text-white">이번 주 활동</p>
          <% ['월', '화', '수', '목', '금', '토', '일'].each_with_index do |day, i| %>
            <div class="flex items-center justify-between">
              <span class="text-sm font-bold text-slate-400"><%= day %></span>
              <div class="flex-1 mx-4 h-2 bg-white/5 rounded-full overflow-hidden">
                <div class="h-full bg-indigo-500 rounded-full" style="width: <%= @weekly_data[i] %>%"></div>
              </div>
              <span class="text-xs font-black text-white tabular-nums"><%= @weekly_data[i] %>%</span>
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <!-- Real Data for Modal Switching -->
    <template id="mock-week-detail">
        <div class="grid grid-cols-2 gap-4">
          <div class="app-glass-card p-4 bg-white/[0.02]">
            <p class="text-[10px] font-bold text-slate-500 mb-2">주간 달성률</p>
            <p class="text-2xl font-black text-white"><%= @weekly_completion %>%</p>
            <% if @weekly_growth > 0 %>
              <p class="text-xs font-bold text-emerald-500 mt-1">↑ <%= @weekly_growth %>%</p>
            <% elsif @weekly_growth < 0 %>
              <p class="text-xs font-bold text-red-500 mt-1">↓ <%= @weekly_growth.abs %>%</p>
            <% else %>
              <p class="text-xs font-bold text-slate-500 mt-1">-</p>
            <% end %>
          </div>
          <div class="app-glass-card p-4 bg-white/[0.02]">
            <p class="text-[10px] font-bold text-slate-500 mb-2">연속 기록</p>
            <p class="text-2xl font-black text-white"><%= @current_streak %>일</p>
            <p class="text-xs font-bold text-indigo-400 mt-1"><%= @current_streak > 0 ? '🔥 진행중' : '시작 대기' %></p>
          </div>
        </div>
        <div class="app-glass-card p-5 bg-white/[0.02] space-y-3">
          <p class="text-xs font-black text-white">이번 주 활동</p>
          <% ['월', '화', '수', '목', '금', '토', '일'].each_with_index do |day, i| %>
            <div class="flex items-center justify-between">
              <span class="text-sm font-bold text-slate-400"><%= day %></span>
              <div class="flex-1 mx-4 h-2 bg-white/5 rounded-full overflow-hidden">
                <div class="h-full bg-indigo-500 rounded-full" style="width: <%= @weekly_data[i] %>%"></div>
              </div>
              <span class="text-xs font-black text-white tabular-nums"><%= @weekly_data[i] %>%</span>
            </div>
          <% end %>
        </div>
    </template>

    <template id="mock-month-detail">
        <div class="grid grid-cols-2 gap-4">
          <div class="app-glass-card p-4 bg-white/[0.02]">
            <p class="text-[10px] font-bold text-slate-500 mb-2">월간 달성률</p>
            <p class="text-2xl font-black text-white"><%= @monthly_completion %>%</p>
            <% if @monthly_growth > 0 %>
              <p class="text-xs font-bold text-emerald-500 mt-1">↑ <%= @monthly_growth %>%</p>
            <% elsif @monthly_growth < 0 %>
              <p class="text-xs font-bold text-red-500 mt-1">↓ <%= @monthly_growth.abs %>%</p>
            <% else %>
              <p class="text-xs font-bold text-slate-500 mt-1">-</p>
            <% end %>
          </div>
          <div class="app-glass-card p-4 bg-white/[0.02]">
            <p class="text-[10px] font-bold text-slate-500 mb-2">누적 포인트</p>
            <p class="text-2xl font-black text-white"><%= number_with_delimiter(@current_month_points) %>P</p>
            <p class="text-[10px] font-bold text-slate-600 mt-1"><%= current_user&.is_rufa_club_member? ? '클럽 멤버십 혜택' : '이번 달 점수' %></p>
          </div>
        </div>
        <div class="app-glass-card p-5 bg-white/[0.02] space-y-3">
          <p class="text-xs font-black text-white">최근 4주 리포트</p>
          <% ['1주차', '2주차', '3주차', '4주차'].each_with_index do |week, i| %>
            <div class="flex items-center justify-between">
              <span class="text-sm font-bold text-slate-400"><%= week %></span>
              <div class="flex-1 mx-4 h-2 bg-white/5 rounded-full overflow-hidden">
                <div class="h-full bg-indigo-500 rounded-full" style="width: <%= @monthly_data[i] %>%"></div>
              </div>
              <span class="text-xs font-black text-white tabular-nums"><%= @monthly_data[i] %>%</span>
            </div>
          <% end %>
        </div>
    </template>

    <template id="mock-year-detail">
        <div class="grid grid-cols-2 gap-4">
          <div class="app-glass-card p-4 bg-white/[0.02]">
            <p class="text-[10px] font-bold text-slate-500 mb-2">올해 달성률</p>
            <p class="text-2xl font-black text-white"><%= @yearly_completion %>%</p>
            <% if @yearly_growth > 0 %>
              <p class="text-xs font-bold text-emerald-500 mt-1">↑ <%= @yearly_growth %>%</p>
            <% elsif @yearly_growth < 0 %>
              <p class="text-xs font-bold text-red-500 mt-1">↓ <%= @yearly_growth.abs %>%</p>
            <% else %>
              <p class="text-xs font-bold text-slate-500 mt-1">신규 진입</p>
            <% end %>
          </div>
          <div class="app-glass-card p-4 bg-white/[0.02]">
            <p class="text-[10px] font-bold text-slate-500 mb-2">획득한 배지</p>
            <p class="text-2xl font-black text-white"><%= @achievements.count %>개</p>
            <p class="text-[10px] font-bold text-slate-600 mt-1">Identity Wallet</p>
          </div>
        </div>
        <div class="app-glass-card p-5 bg-white/[0.02] space-y-3">
          <p class="text-xs font-black text-white">월별 히스토리</p>
          <% (1..Date.current.month).each do |month_num| %>
            <div class="flex items-center justify-between">
              <span class="text-sm font-bold text-slate-400"><%= month_num %>월</span>
              <div class="flex-1 mx-4 h-2 bg-white/5 rounded-full overflow-hidden">
                <div class="h-full bg-indigo-500 rounded-full" style="width: <%= @yearly_data[month_num - 1] %>%"></div>
              </div>
              <span class="text-xs font-black text-white tabular-nums"><%= @yearly_data[month_num - 1] %>%</span>
            </div>
          <% end %>
        </div>
    </template>
  </div>
</div>

<!-- Host Center Modal -->
<div id="host-center" class="hidden fixed inset-0 z-[300]">
  <div data-action="click->proto-ui#closeModal" 
       onclick="this.closest('.fixed').classList.add('hidden'); document.body.classList.remove('overflow-hidden')"
       class="absolute inset-0 bg-black/90 backdrop-blur-xl cursor-pointer"></div>
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[90%] max-w-md app-glass-card border-white/20 p-8 space-y-6">
    <div class="flex justify-between items-center">
      <div>
        <h3 class="text-xl font-black text-white">호스트 센터</h3>
        <p class="text-xs font-bold text-amber-400 mt-1">내가 개설한 챌린지 관리</p>
      </div>
      <button onclick="this.closest('.fixed').classList.add('hidden'); document.body.classList.remove('overflow-hidden')" class="w-8 h-8 rounded-full bg-white/5 flex items-center justify-center text-slate-400 hover:text-white transition-colors cursor-pointer">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
      </button>
    </div>
    
    <div class="space-y-3">
      <div class="app-glass-card p-5 bg-amber-500/5 border border-amber-500/20">
        <div class="flex items-center justify-between mb-3">
          <p class="text-sm font-black text-white">진행 중인 챌린지</p>
          <span class="px-2 py-1 bg-emerald-500/20 text-emerald-400 text-[10px] font-black rounded-full">운영중</span>
        </div>
        <p class="text-xs font-bold text-slate-400">현재 호스팅 중인 챌린지가 없습니다</p>
      </div>
      
      <%= link_to prototype_challenge_builder_path, class: "block w-full p-4 rounded-2xl bg-gradient-to-r from-amber-500 to-orange-500 text-white font-black text-sm flex items-center justify-center gap-2 active:scale-95 transition-all" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/></svg>
        새 챌린지 만들기
      <% end %>
      
      <p class="text-[10px] font-bold text-slate-500 text-center leading-relaxed">
        챌린지를 개설하면 이곳에서<br/>참여자 관리와 인증 확인을 할 수 있습니다
      </p>
    </div>
  </div>
</div>

<% @achievements.each_with_index do |ub, i| %>
  <div id="badge-detail-<%= i %>" class="hidden fixed inset-0 z-[600]">
    <div onclick="this.closest('.fixed').classList.add('hidden'); document.body.classList.remove('overflow-hidden')"
         class="absolute inset-0 bg-black/90 backdrop-blur-xl cursor-pointer"></div>
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[90%] max-w-sm app-glass-card border-white/20 p-8 flex flex-col items-center justify-between text-center overflow-hidden">
       <!-- Shimmer Effect -->
       <div class="absolute inset-0 bg-gradient-to-tr from-transparent via-white/10 to-transparent -translate-x-full animate-shimmer"></div>
       
       <div class="space-y-6 relative z-10">
         <div class="w-24 h-24 rounded-[32px] bg-white/5 border border-white/10 flex items-center justify-center text-5xl mx-auto shadow-2xl shadow-indigo-500/20">
           <%= ub.badge.icon_path.presence || "🏆" %>
         </div>
         <div class="space-y-2">
           <h4 class="text-2xl font-black text-white"><%= ub.badge.name %></h4>
           <p class="text-xs font-bold text-indigo-400 uppercase tracking-widest"><%= ub.badge.level %> 등급 배지</p>
         </div>
         <p class="text-sm font-bold text-slate-400 leading-relaxed text-center px-4">
           "<%= ub.badge.description %>"
         </p>
       </div>

       <div class="w-full space-y-4 relative z-10 pt-8">
          <div class="h-px w-full bg-white/10"></div>
          <div class="flex justify-around">
             <div class="text-center">
               <p class="text-xs font-black text-white"><%= ub.granted_at&.strftime("%Y.%m.%d") || "2024.12.24" %></p>
                <p class="text-[8px] font-bold text-slate-600 uppercase tracking-widest">획득일</p>
             </div>
             <div class="text-center">
               <p class="text-xs font-black text-white">Lv.<%= ub.badge.requirement_value.to_i %></p>
                <p class="text-[8px] font-bold text-slate-600 uppercase tracking-widest">달성 기준</p>
             </div>
          </div>
          <button onclick="if(navigator.share) { 
                            navigator.share({ 
                              title: '루틴 파인더스 성취 기록', 
                              text: '<%= ub.badge.name %> 배지를 획득했습니다! 함께 루틴을 만들어봐요.', 
                              url: '<%= root_url %>'
                            }).catch(console.error);
                          } else {
                            const btn = this;
                            const originalContent = btn.innerHTML;
                            btn.innerHTML = '✅ 대기열에 추가됨';
                            btn.classList.replace('bg-white', 'bg-emerald-500');
                            btn.classList.add('text-white');
                            setTimeout(() => {
                              btn.innerHTML = originalContent;
                              btn.classList.replace('bg-emerald-500', 'bg-white');
                              btn.classList.remove('text-white');
                              alert('공유 이미지가 생성되었습니다. 인스타그램 스토리에 공유해보세요!');
                            }, 1000);
                          }"
                  class="w-full py-4 rounded-2xl bg-white text-black font-black text-sm flex items-center justify-center gap-2 active:scale-95 transition-all">
            <span class="text-lg">📤</span> 공유하기
          </button>
       </div>
    </div>
  </div>
<% end %>

<!-- Edit Goals Modal -->
<div id="edit-goals" class="hidden fixed inset-0 z-[600]">
  <div onclick="this.closest('.fixed').classList.add('hidden'); document.body.classList.remove('overflow-hidden')"
       class="absolute inset-0 bg-black/90 backdrop-blur-xl cursor-pointer"></div>
  
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[90%] max-w-md app-glass-card border-white/20 p-8 space-y-6 max-h-[80vh] overflow-y-auto modal-content-safe">
    <div class="flex justify-between items-center">
      <h3 class="text-xl font-black text-white">목표 설정</h3>
      <button type="button" 
              onclick="this.closest('.fixed').classList.add('hidden'); document.body.classList.remove('overflow-hidden')"
              class="w-8 h-8 rounded-full bg-white/5 flex items-center justify-center text-slate-400 hover:text-white transition-colors cursor-pointer">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
      </button>
    </div>
    
    <%= form_with url: prototype_update_goals_path, method: :patch, class: "space-y-5" do |f| %>
      <!-- Weekly Goal -->
      <div>
        <label class="text-[10px] font-black text-slate-500 uppercase tracking-wider pl-4 mb-2 block flex items-center gap-2">
          <span>🎯</span> 주간 목표
        </label>
        <%= f.text_area :weekly_goal, 
                        value: current_user.weekly_goal,
                        rows: 2,
                        placeholder: "이번 주에 이루고 싶은 목표를 적어보세요",
                        class: "w-full bg-black/40 border border-white/5 rounded-2xl p-4 text-white text-sm font-bold focus:border-indigo-500/50 outline-none transition-all resize-none" %>
        <p class="text-[9px] font-bold text-slate-600 mt-2 pl-4">매주 월요일마다 새로운 목표를 설정해보세요</p>
      </div>

      <!-- Monthly Goal -->
      <div>
        <label class="text-[10px] font-black text-slate-500 uppercase tracking-wider pl-4 mb-2 block flex items-center gap-2">
          <span>🌙</span> 월간 목표
        </label>
        <%= f.text_area :monthly_goal, 
                        value: current_user.monthly_goal,
                        rows: 2,
                        placeholder: "이번 달에 달성하고 싶은 목표를 적어보세요",
                        class: "w-full bg-black/40 border border-white/5 rounded-2xl p-4 text-white text-sm font-bold focus:border-indigo-500/50 outline-none transition-all resize-none" %>
        <p class="text-[9px] font-bold text-slate-600 mt-2 pl-4">매월 1일마다 새로운 목표를 설정해보세요</p>
      </div>

      <!-- Yearly Goal -->
      <div>
        <label class="text-[10px] font-black text-slate-500 uppercase tracking-wider pl-4 mb-2 block flex items-center gap-2">
          <span>⭐</span> 연간 목표
        </label>
        <%= f.text_area :yearly_goal, 
                        value: current_user.yearly_goal,
                        rows: 3,
                        placeholder: "올해 이루고 싶은 큰 목표를 적어보세요",
                        class: "w-full bg-black/40 border border-white/5 rounded-2xl p-4 text-white text-sm font-bold focus:border-indigo-500/50 outline-none transition-all resize-none" %>
        <p class="text-[9px] font-bold text-slate-600 mt-2 pl-4">매년 1월 1일마다 새로운 목표를 설정해보세요</p>
      </div>

      <%= f.submit "저장하기", class: "w-full py-4 rounded-2xl bg-indigo-500 text-white font-black text-sm active:scale-95 transition-all cursor-pointer" %>
    <% end %>
  </div>
</div>

<!-- Edit Profile Modal -->
<div id="edit-profile" class="hidden fixed inset-0 z-[600]">
  <div onclick="this.closest('.fixed').classList.add('hidden'); document.body.classList.remove('overflow-hidden')"
       class="absolute inset-0 bg-black/90 backdrop-blur-xl cursor-pointer"></div>
  
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[90%] max-w-md app-glass-card border-white/20 p-8 space-y-6 max-h-[85vh] overflow-y-auto modal-content-safe">
    <div class="flex justify-between items-center">
      <h3 class="text-xl font-black text-white">프로필 수정</h3>
      <button type="button" 
              onclick="this.closest('.fixed').classList.add('hidden'); document.body.classList.remove('overflow-hidden')"
              class="w-8 h-8 rounded-full bg-white/5 flex items-center justify-center text-slate-400 hover:text-white transition-colors cursor-pointer">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
      </button>
    </div>
    
    <%= form_with url: prototype_update_profile_path, method: :patch, multipart: true, class: "space-y-6" do |f| %>
      <!-- Profile Image Section -->
      <div class="flex justify-center">
        <div class="relative">
          <div class="w-24 h-24 rounded-[32px] border-4 border-indigo-500 overflow-hidden bg-white/5">
            <img id="profile-preview" src="<%= current_user&.profile_image %>" class="w-full h-full object-cover">
          </div>
          <input type="file" name="user[profile_image]" id="profile-upload" class="hidden" accept="image/*" 
                 onchange="const reader = new FileReader(); reader.onload = (e) => { document.getElementById('profile-preview').src = e.target.result; }; reader.readAsDataURL(this.files[0]);">
          
          <button type="button" 
                  onclick="document.getElementById('profile-upload').click()"
                  class="absolute -bottom-2 -right-2 w-10 h-10 rounded-full bg-indigo-500 text-white flex items-center justify-center shadow-lg active:scale-90 transition-transform cursor-pointer">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 9a2 2 0 012-2h.93a2 2 0 001.664-.89l.812-1.22A2 2 0 0110.07 4h3.86a2 2 0 011.664.89l.812 1.22A2 2 0 0018.07 7H19a2 2 0 012 2v9a2 2 0 01-2 2H5a2 2 0 01-2-2V9z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 13a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
          </button>
        </div>
      </div>
      
      <!-- Basic Info Section -->
      <div class="space-y-4">
        <div>
          <label class="text-[10px] font-black text-slate-500 uppercase tracking-wider pl-4 mb-2 block">닉네임</label>
          <input type="text" name="user[nickname]" value="<%= current_user&.nickname %>" 
                 class="w-full bg-black/40 border border-white/10 rounded-2xl p-4 text-white text-sm font-bold focus:border-indigo-500/50 outline-none transition-all" />
        </div>
        
        <div>
          <label class="text-[10px] font-black text-slate-500 uppercase tracking-wider pl-4 mb-2 block">자기소개</label>
          <textarea name="user[bio]" rows="3" placeholder="나를 표현하는 한 줄" 
                    class="w-full bg-black/40 border border-white/10 rounded-2xl p-4 text-white text-sm font-bold focus:border-indigo-500/50 outline-none transition-all resize-none"><%= current_user&.bio %></textarea>
        </div>

        <!-- SNS Links Section -->
        <div>
          <div class="flex justify-between items-center mb-3">
            <label class="text-[10px] font-black text-slate-500 uppercase tracking-wider pl-4">SNS 링크</label>
            <button type="button" 
                    onclick="const container = this.closest('form').querySelector('.sns-links-container');
                             const template = this.closest('form').querySelector('.sns-link-template');
                             const clone = template.content.cloneNode(true);
                             container.appendChild(clone);
                             const empty = container.querySelector('.sns-empty-state');
                             if(empty) empty.classList.add('hidden');"
                    class="text-xs font-black text-indigo-400 hover:text-indigo-300 flex items-center gap-1 cursor-pointer">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/></svg>
              추가
            </button>
          </div>
          
          <div class="sns-links-container space-y-2">
            <!-- SNS Link Template (hidden) -->
            <template class="sns-link-template">
              <div class="flex items-center gap-2 group">
                <div class="w-10 h-10 rounded-xl bg-white/5 flex items-center justify-center shrink-0">
                  <svg class="w-5 h-5 text-slate-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"/></svg>
                </div>
                <input type="url" name="user[sns_links][]" placeholder="SNS 링크 URL 입력" 
                       class="flex-1 bg-black/40 border border-white/5 rounded-xl p-3 text-white text-sm font-bold focus:border-indigo-500/50 outline-none transition-all" />
                <button type="button" 
                        onclick="this.closest('.group').remove(); if(this.closest('.sns-links-container').querySelectorAll('.group').length === 0) this.closest('.sns-links-container').querySelector('.sns-empty-state').classList.remove('hidden');"
                        class="w-8 h-8 rounded-lg bg-red-500/10 text-red-400 hover:bg-red-500/20 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity cursor-pointer">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
                </button>
              </div>
            </template>
            
            <!-- Existing SNS Links -->
            <% (current_user&.sns_links || {}).each do |platform, url| %>
              <div class="flex items-center gap-2 group">
                <div class="w-10 h-10 rounded-xl bg-indigo-500/10 flex items-center justify-center shrink-0">
                  <% icon = case platform; when 'instagram'; '📱'; when 'threads'; '🧵'; when 'youtube'; '📺'; when 'twitter'; '🐦'; when 'blog'; '📝'; else; '🔗'; end %>
                  <span class="text-sm"><%= icon %></span>
                </div>
                <input type="url" name="user[sns_links][]" value="<%= url %>" 
                       class="flex-1 bg-black/40 border border-white/5 rounded-xl p-3 text-white text-sm font-bold focus:border-indigo-500/50 outline-none transition-all" />
                <button type="button" 
                        onclick="this.closest('.group').remove(); if(this.closest('.sns-links-container').querySelectorAll('.group').length === 0) this.closest('.sns-links-container').querySelector('.sns-empty-state').classList.remove('hidden');"
                        class="w-8 h-8 rounded-lg bg-red-500/10 text-red-400 hover:bg-red-500/20 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity cursor-pointer">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
                </button>
              </div>
            <% end %>

            <!-- Initial empty state -->
            <p class="sns-empty-state text-xs font-bold text-slate-600 text-center py-4 <%= (current_user&.sns_links || {}).any? ? 'hidden' : '' %>">
              SNS 링크를 추가해보세요
            </p>
          </div>
        </div>
      </div>
      
      <button type="submit" class="w-full py-4 rounded-2xl bg-indigo-500 text-white font-black text-sm active:scale-95 transition-all cursor-pointer shadow-lg shadow-indigo-500/20">
        저장하기
      </button>
    <% end %>
  </div>
</div>
