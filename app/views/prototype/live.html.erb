<div class="fixed inset-0 bg-[#07060A] z-0 overflow-hidden">
  <!-- Dynamic Glow Background -->
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[150%] aspect-square bg-gradient-radial from-indigo-500/10 via-transparent to-transparent animate-slow-pulse"></div>
  <div class="absolute -top-40 -left-40 w-96 h-96 bg-purple-500/5 blur-[120px] rounded-full"></div>
  <div class="absolute -bottom-40 -right-40 w-96 h-96 bg-indigo-500/5 blur-[120px] rounded-full"></div>
</div>

<div class="relative z-10 pt-10 pb-20 flex flex-col min-h-screen">
  <!-- Header -->
  <header class="flex items-center justify-between px-2 mb-12">
    <%= link_to prototype_home_path, class: "w-10 h-10 rounded-full bg-white/5 flex items-center justify-center text-white" do %>
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M15 19l-7-7 7-7"/></svg>
    <% end %>
    <div class="flex items-center gap-2 px-4 py-1.5 bg-rose-600/20 rounded-full border border-rose-500/30">
       <div class="w-1.5 h-1.5 rounded-full bg-rose-500 animate-ping"></div>
       <span class="text-[10px] font-black text-rose-500 uppercase tracking-widest">Live Room</span>
    </div>
    <button class="w-10 h-10 rounded-full bg-white/5 flex items-center justify-center text-white"><span class="text-xl">⚙️</span></button>
  </header>

  <!-- Live Content -->
  <main class="flex-1 flex flex-col items-center justify-center text-center space-y-12">
    <!-- Active Member Orbits -->
    <div class="relative w-72 h-72 flex items-center justify-center">
      <!-- Orbit Rings -->
      <div class="absolute inset-0 border border-white/5 rounded-full"></div>
      <div class="absolute inset-6 border border-white/5 rounded-full"></div>
      
      <!-- Center Text -->
      <div class="relative z-20 space-y-1">
        <h2 class="text-4xl font-black text-white"><%= @active_members.count %></h2>
        <p class="text-[10px] font-bold text-slate-500 uppercase tracking-widest">Active Now</p>
      </div>

      <!-- Floating Member Avatars -->
      <% @active_members.each_with_index do |user, i| %>
        <% 
            angle = (360 / @active_members.count) * i
            radius = i.even? ? 130 : 100
        %>
        <div class="absolute transition-all duration-1000 animate-float" 
             style="transform: rotate(<%= angle %>deg) translate(<%= radius %>px) rotate(-<%= angle %>deg)">
           <div class="relative group">
             <div class="absolute inset-0 bg-indigo-500 blur-lg opacity-0 group-hover:opacity-40 transition-opacity"></div>
             <img src="<%= user.profile_image %>" class="w-10 h-10 rounded-2xl border-2 border-white/10 shadow-2xl relative z-10 transition-transform hover:scale-125">
             <div class="absolute -bottom-1 -right-1 w-3 h-3 bg-emerald-500 rounded-full border-2 border-[#07060A]"></div>
           </div>
        </div>
      <% end %>
    </div>

    <div class="space-y-4 px-8">
      <h3 class="text-2xl font-black text-white leading-tight">함께 몰입하는<br/><span class="text-indigo-400">루파 클럽 <%= @current_club&.generation_number %>기</span></h3>
      <p class="text-xs font-bold text-slate-500 leading-relaxed">혼자라면 포기했을 새벽 6시,<br/>지금 <%= @active_members.count %>명의 메이트가 당신과 함께하고 있습니다.</p>
    </div>
  </main>

  <!-- Actions -->
  <footer class="mt-12 space-y-6">
    <div class="flex flex-col gap-4 px-4">
      <button class="w-full py-5 rounded-[28px] bg-white text-[#07060A] font-black text-lg flex items-center justify-center gap-3 shadow-[0_20px_40px_rgba(255,255,255,0.1)] active:scale-95 transition-all">
        <span class="text-2xl">📹</span>
        줌(Zoom) 라이브 입장하기
      </button>
      
      <button class="w-full py-5 rounded-[28px] bg-indigo-500/10 border border-indigo-500/30 text-indigo-400 font-black text-lg flex items-center justify-center gap-3 active:scale-95 transition-all">
        <span class="text-2xl">🎧</span>
        루틴 집중 음악 듣기
      </button>
    </div>
    
    <div class="text-center">
      <p class="text-[10px] font-bold text-slate-700 uppercase tracking-[0.2em]">Live Session Ends in <span class="text-white">42:15</span></p>
    </div>
  </footer>
</div>

<style>
  @keyframes slow-pulse {
    0%, 100% { transform: translate(-50%, -50%) scale(1); opacity: 0.1; }
    50% { transform: translate(-50%, -50%) scale(1.1); opacity: 0.2; }
  }
  .animate-slow-pulse {
    animation: slow-pulse 8s ease-in-out infinite;
  }
  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
  .animate-float {
    /* Each member gets a slightly different delay via JS or inline later if needed */
    animation: float 4s ease-in-out infinite;
  }
</style>
