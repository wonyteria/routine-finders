<div class="min-h-screen bg-gradient-to-br from-purple-50 via-white to-indigo-50/30">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    
    <!-- Header -->
    <header class="mb-12">
      <div class="flex items-center justify-between">
        <div>
          <%= link_to routine_club_path(@routine_club), class: "inline-flex items-center gap-2 text-slate-600 hover:text-slate-900 font-bold transition-colors mb-4" do %>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
            </svg>
            클럽으로 돌아가기
          <% end %>
          <h1 class="text-4xl font-black text-slate-900">클럽 관리</h1>
          <p class="text-lg text-slate-600 font-medium mt-2"><%= @routine_club.title %></p>
        </div>
        
        <div class="flex gap-3">
          <%= link_to "클럽 수정", edit_routine_club_path(@routine_club), class: "px-6 py-3 bg-slate-100 text-slate-700 rounded-2xl font-black hover:bg-slate-200 transition-colors" %>
        </div>
      </div>
    </header>

    <!-- Stats Overview -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-12">
      <div class="bg-white border border-slate-200 rounded-3xl p-6">
        <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-2">총 멤버</p>
        <p class="text-4xl font-black text-indigo-600"><%= @routine_club.current_members %></p>
        <p class="text-sm text-slate-500 font-medium mt-1">/ <%= @routine_club.max_members %>명</p>
      </div>
      
      <div class="bg-white border border-slate-200 rounded-3xl p-6">
        <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-2">입금 대기</p>
        <p class="text-4xl font-black text-amber-600"><%= @pending_payments.count %></p>
        <p class="text-sm text-slate-500 font-medium mt-1">확인 필요</p>
      </div>
      
      <div class="bg-white border border-slate-200 rounded-3xl p-6">
        <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-2">평균 출석률</p>
        <p class="text-4xl font-black text-emerald-600">
          <%= @routine_club.average_attendance_rate.to_i %>%
        </p>
      </div>
      
      <div class="bg-white border border-slate-200 rounded-3xl p-6">
        <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-2">경고 발생</p>
        <p class="text-4xl font-black text-red-600"><%= @routine_club.total_penalties %></p>
        <p class="text-sm text-slate-500 font-medium mt-1">총 경고 횟수</p>
      </div>
    </div>

    <!-- Tabs -->
    <div class="mb-8">
      <div class="bg-white border border-slate-200 rounded-3xl p-2 inline-flex gap-2">
        <button onclick="switchManageTab('pending')" 
                id="manage-tab-pending"
                class="manage-tab-btn px-6 py-3 rounded-2xl font-black text-sm transition-all bg-indigo-600 text-white">
          입금 대기 <% if @pending_payments.any? %><span class="ml-1 bg-white/20 px-2 py-0.5 rounded-full text-xs"><%= @pending_payments.count %></span><% end %>
        </button>
        <button onclick="switchManageTab('members')" 
                id="manage-tab-members"
                class="manage-tab-btn px-6 py-3 rounded-2xl font-black text-sm transition-all text-slate-400 hover:text-slate-600">
          멤버 관리
        </button>
        <button onclick="switchManageTab('penalties')" 
                id="manage-tab-penalties"
                class="manage-tab-btn px-6 py-3 rounded-2xl font-black text-sm transition-all text-slate-400 hover:text-slate-600">
          패널티 관리
        </button>
      </div>
    </div>

    <!-- Pending Payments Tab -->
    <div id="manage-content-pending" class="manage-tab-content">
      <div class="bg-white rounded-[40px] p-8 border border-slate-200 shadow-sm">
        <h2 class="text-2xl font-black text-slate-900 mb-6">입금 확인 대기</h2>
        
        <% if @pending_payments.any? %>
          <div class="space-y-4">
            <% @pending_payments.each do |membership| %>
              <div class="bg-amber-50 border-2 border-amber-200 rounded-3xl p-6">
                <div class="flex items-start justify-between gap-6">
                  <div class="flex items-start gap-4 flex-1">
                    <div class="w-16 h-16 rounded-full bg-gradient-to-br from-amber-400 to-orange-500 flex items-center justify-center text-white text-2xl font-black flex-shrink-0">
                      <%= membership.user.nickname[0].upcase %>
                    </div>
                    
                    <div class="flex-1">
                      <h3 class="text-xl font-black text-slate-900 mb-2">
                        <%= membership.user.nickname %>
                      </h3>
                      
                      <div class="grid grid-cols-2 gap-4 mb-4">
                        <div>
                          <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-1">입금자명</p>
                          <p class="text-sm font-bold text-slate-700"><%= membership.depositor_name %></p>
                        </div>
                        <div>
                          <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-1">입금액</p>
                          <p class="text-sm font-bold text-slate-700">
                            <%= number_to_currency(membership.paid_amount, unit: "₩", precision: 0) %>
                          </p>
                        </div>
                        <div>
                          <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-1">신청일</p>
                          <p class="text-sm font-bold text-slate-700">
                            <%= membership.created_at.strftime("%Y-%m-%d %H:%M") %>
                          </p>
                        </div>
                        <div>
                          <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-1">이메일</p>
                          <p class="text-sm font-bold text-slate-700"><%= membership.user.email %></p>
                        </div>
                        <div>
                          <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-1">연락처</p>
                          <p class="text-sm font-black text-emerald-600"><%= membership.contact_info.presence || "미입력" %></p>
                        </div>
                      </div>
                      
                      <div class="bg-white border border-amber-200 rounded-2xl p-4">
                        <p class="text-xs font-black text-amber-900 mb-2">입금 계좌 정보</p>
                        <p class="text-sm text-amber-700 font-medium">
                          <%= @routine_club.bank_name %> <%= @routine_club.account_number %> (예금주: <%= @routine_club.account_holder %>)
                        </p>
                      </div>
                    </div>
                  </div>
                  
                  <div class="flex flex-col gap-2">
                    <%= button_to "승인", confirm_payment_routine_club_path(@routine_club, member_id: membership.id), method: :post, class: "px-6 py-3 bg-emerald-600 text-white rounded-2xl font-black hover:bg-emerald-700 transition-colors whitespace-nowrap", data: { confirm: "#{membership.user.nickname}님의 입금을 승인하시겠습니까?" } %>
                    
                    <%= button_to "거부", reject_payment_routine_club_path(@routine_club, member_id: membership.id), method: :post, class: "px-6 py-3 bg-red-100 text-red-600 rounded-2xl font-black hover:bg-red-200 transition-colors whitespace-nowrap", data: { confirm: "#{membership.user.nickname}님의 입금을 거부하시겠습니까?" } %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="bg-slate-50 rounded-3xl p-20 text-center">
            <div class="text-6xl mb-4 opacity-20">✅</div>
            <p class="text-slate-400 font-bold">입금 확인 대기 중인 멤버가 없습니다</p>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Members Tab -->
    <div id="manage-content-members" class="manage-tab-content hidden">
      <div class="bg-white rounded-[40px] p-8 border border-slate-200 shadow-sm">
        <h2 class="text-2xl font-black text-slate-900 mb-6">멤버 관리</h2>
        
        <% if @members.any? %>
          <div class="space-y-4">
            <% @members.each do |membership| %>
              <div class="bg-slate-50 border border-slate-200 rounded-3xl p-6">
                <div class="flex items-start justify-between gap-6">
                  <div class="flex items-start gap-4 flex-1">
                    <div class="w-16 h-16 rounded-full bg-gradient-to-br from-indigo-400 to-purple-500 flex items-center justify-center text-white text-2xl font-black flex-shrink-0">
                      <%= membership.user.nickname[0].upcase %>
                    </div>
                    
                    <div class="flex-1">
                      <div class="flex items-center gap-3 mb-3">
                        <h3 class="text-xl font-black text-slate-900">
                          <%= membership.user.nickname %>
                        </h3>
                        
                        <% if membership.status_warned? %>
                          <span class="bg-red-100 text-red-600 px-3 py-1 rounded-full text-xs font-black">경고</span>
                        <% elsif membership.status_active? %>
                          <span class="bg-emerald-100 text-emerald-600 px-3 py-1 rounded-full text-xs font-black">정상</span>
                        <% end %>
                        
                        <% if membership.is_moderator %>
                          <span class="bg-purple-100 text-purple-600 px-3 py-1 rounded-full text-xs font-black">운영진</span>
                        <% end %>
                      </div>
                      
                      <div class="grid grid-cols-4 gap-4">
                        <div>
                          <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-1">출석률</p>
                          <p class="text-lg font-black text-emerald-600"><%= membership.attendance_rate.to_i %>%</p>
                        </div>
                        <div>
                          <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-1">출석</p>
                          <p class="text-lg font-black text-indigo-600"><%= membership.attendance_count %>일</p>
                        </div>
                        <div>
                          <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-1">결석</p>
                          <p class="text-lg font-black text-orange-600"><%= membership.absence_count %>일</p>
                        </div>
                        <div>
                          <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-1">경고</p>
                          <p class="text-lg font-black text-red-600"><%= membership.penalty_count %>회</p>
                        </div>
                        <div class="col-span-4 mt-2 pt-2 border-t border-slate-100">
                          <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-1">연락처</p>
                          <p class="text-sm font-bold text-indigo-600"><%= membership.contact_info.presence || "미입력" %></p>
                        </div>
                      </div>
                    </div>
                  </div>
                  
                  <div class="flex flex-col gap-2">
                    <%= button_to "강퇴", kick_member_routine_club_path(@routine_club, member_id: membership.id), method: :post, class: "px-6 py-3 bg-red-600 text-white rounded-2xl font-black hover:bg-red-700 transition-colors whitespace-nowrap", data: { confirm: "#{membership.user.nickname}님을 강퇴하시겠습니까? 이 작업은 되돌릴 수 없습니다." } %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="bg-slate-50 rounded-3xl p-20 text-center">
            <div class="text-6xl mb-4 opacity-20">👥</div>
            <p class="text-slate-400 font-bold">아직 참여한 멤버가 없습니다</p>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Penalties Tab -->
    <div id="manage-content-penalties" class="manage-tab-content hidden">
      <div class="bg-white rounded-[40px] p-8 border border-slate-200 shadow-sm">
        <h2 class="text-2xl font-black text-slate-900 mb-6">패널티 관리</h2>
        
        <div class="bg-slate-50 rounded-3xl p-20 text-center">
          <div class="text-6xl mb-4 opacity-20">⚠️</div>
          <p class="text-slate-400 font-bold">패널티 기능은 곧 추가될 예정입니다</p>
        </div>
      </div>
    </div>

  </div>
</div>

<script>
  function switchManageTab(tab) {
    // Update buttons
    document.querySelectorAll('.manage-tab-btn').forEach(btn => {
      btn.classList.remove('bg-indigo-600', 'text-white');
      btn.classList.add('text-slate-400', 'hover:text-slate-600');
    });
    
    const activeBtn = document.getElementById('manage-tab-' + tab);
    activeBtn.classList.remove('text-slate-400', 'hover:text-slate-600');
    activeBtn.classList.add('bg-indigo-600', 'text-white');
    
    // Update content
    document.querySelectorAll('.manage-tab-content').forEach(content => {
      content.classList.add('hidden');
    });
    document.getElementById('manage-content-' + tab).classList.remove('hidden');
  }
</script>
