<style>
  @keyframes marquee {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
  .marquee-content {
    display: flex;
    width: max-content;
    animation: marquee 30s linear infinite;
  }
  .marquee-content:hover {
    animation-play-state: paused;
  }
</style>

<div class="min-h-screen bg-[#0C0B12] flex flex-col justify-center py-12">
  <!-- Top Background Decor -->
  <div class="fixed top-0 left-0 w-full h-1/2 bg-gradient-to-b from-indigo-600/20 to-transparent pointer-events-none"></div>
  <div class="fixed top-20 right-[-10%] w-80 h-80 bg-purple-600/10 rounded-full blur-[100px] pointer-events-none"></div>

  <div class="relative z-10 space-y-12 w-full">
    <!-- Brand Info -->
    <div class="text-center space-y-6 animate-fade-in max-w-sm mx-auto px-6">
      <div class="w-20 h-20 bg-white rounded-[32px] flex items-center justify-center mx-auto shadow-[0_0_40px_rgba(255,255,255,0.1)]">
        <img src="<%= asset_path('rf_symbol_v9.png') %>" class="w-12 h-12" alt="Routine Finders">
      </div>
      <div class="space-y-2">
        <h1 class="text-3xl font-black text-white tracking-tight">반가워요, 루파님!</h1>
        <p class="text-slate-400 font-medium leading-relaxed">
          어제보다 더 나은 나를 만드는 여정,<br/>
          루틴 파인더스에서 다시 시작해볼까요?
        </p>
      </div>
    </div>

    <!-- Features Marquee (자동으로 흐르는 카드들) -->
    <div class="relative overflow-hidden py-4 animate-fade-in" style="animation-delay: 0.3s">
      <div class="marquee-content gap-4 px-4">
        <% 
          features = [
            { icon: "📊", title: "데이터 축적", desc: "나만의 루틴 성장을 데이터로 기록하세요." },
            { icon: "🏆", title: "배지 시스템", desc: "성취를 증명하는 배지를 모아보세요." },
            { icon: "⚡", title: "시너지 피드", desc: "멤버들의 열정을 실시간으로 공유받으세요." },
            { icon: "🔥", title: "스트릭 유지", desc: "끊기지 않는 기록으로 습관을 만듭니다." },
            { icon: "👏", title: "실시간 응원", desc: "박수로 서로의 성취를 축하해줍니다." },
            { icon: "🌿", title: "성실함 지수", desc: "나의 성실도를 등급으로 확인하세요." }
          ]
          # Duplicate for seamless infinite scroll
          items = features + features
        %>
        <% items.each do |item| %>
          <div class="shrink-0 w-[180px] p-5 bg-white/5 rounded-3xl border border-white/5 space-y-3 backdrop-blur-sm">
            <div class="text-2xl"><%= item[:icon] %></div>
            <div>
              <p class="text-xs font-black text-white"><%= item[:title] %></p>
              <p class="text-[10px] font-bold text-slate-500 mt-1 leading-relaxed"><%= item[:desc] %></p>
            </div>
          </div>
        <% end %>
      </div>
      <!-- Gradation Overlays -->
      <div class="absolute inset-y-0 left-0 w-20 bg-gradient-to-r from-[#0C0B12] to-transparent z-10"></div>
      <div class="absolute inset-y-0 right-0 w-20 bg-gradient-to-l from-[#0C0B12] to-transparent z-10"></div>
    </div>

    <!-- Login Options -->
    <div class="space-y-4 animate-slide-up max-w-sm mx-auto w-full px-6" style="animation-delay: 0.2s">
      <!-- Kakao Login -->
      <%= button_to "/auth/kakao", data: { turbo: false }, class: "w-full py-4 bg-[#FEE500] hover:bg-[#FEE500]/90 rounded-2xl flex items-center justify-center gap-3 active:scale-95 transition-all shadow-lg" do %>
        <svg class="w-5 h-5" viewBox="0 0 24 24" fill="currentColor">
          <path d="M12 3c-4.97 0-9 3.18-9 7.11 0 2.55 1.7 4.79 4.27 6.05-.17.59-.62 2.13-.71 2.45-.11.4.15.39.31.28.13-.08 2.05-1.39 2.87-1.95.73.2 1.5.31 2.26.31 4.97 0 9-3.18 9-7.11S16.97 3 12 3z"/>
        </svg>
        <span class="text-[#3C1E1E] font-black text-[15px]">카카오로 3초만에 시작하기</span>
      <% end %>

      <!-- Google Login -->
      <%= button_to "/auth/google_oauth2", data: { turbo: false }, class: "w-full py-4 bg-white/5 hover:bg-white/10 border border-white/10 rounded-2xl flex items-center justify-center gap-3 active:scale-95 transition-all text-white font-black text-[15px]" do %>
        <svg class="w-5 h-5" viewBox="0 0 24 24">
          <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z" fill="#4285F4"/>
          <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z" fill="#34A853"/>
          <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l3.66-2.84z" fill="#FBBC05"/>
          <path d="M12 5.38c1.62 0 3.06.56 4.21 1.66l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z" fill="#EA4335"/>
        </svg>
        구글로 로그인
      <% end %>

      <% if Rails.env.development? %>
        <!-- Dev Login (Auto Login) -->
        <%= button_to "/dev_login", class: "w-full py-4 bg-indigo-600/20 text-indigo-400 border border-indigo-500/20 rounded-2xl font-black text-[15px] active:scale-95 transition-all mt-6" do %>
          개발자 계정으로 자동 로그인
        <% end %>
      <% end %>
    </div>

    <!-- Back to Explore -->
    <div class="pt-8 text-center animate-fade-in max-w-sm mx-auto w-full px-6" style="animation-delay: 0.6s">
      <%= link_to prototype_home_path, class: "text-xs font-bold text-slate-600 hover:text-indigo-400 transition-colors inline-flex items-center gap-1.5" do %>
        <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M10 19l-7-7 7-7m8 14l-7-7 7-7"/></svg>
        로그인 없이 둘러보기
      <% end %>
    </div>
  </div>
</div>
