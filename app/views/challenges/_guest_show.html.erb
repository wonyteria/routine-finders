<% content_for :hide_nav, true %>

<style>
  /* Premium Dark Mode Overrides */
  .detail-content-env h2, .detail-content-env h3 { color: #ffffff !important; font-weight: 900 !important; }
  .detail-content-env p, .detail-content-env li, .detail-content-env span { color: #a1a1aa !important; }
  .detail-content-env .bg-white, .detail-content-env .bg-slate-50, .detail-content-env .bg-indigo-50, .detail-content-env .bg-orange-50, .detail-content-env .bg-emerald-50, .detail-content-env .bg-amber-50 { 
    background-color: rgba(255, 255, 255, 0.03) !important; 
    border: 1px solid rgba(255, 255, 255, 0.08) !important;
    border-radius: 24px !important;
  }
  .detail-content-env .text-slate-900, .detail-content-env .text-orange-900, .detail-content-env .text-emerald-900, .detail-content-env .text-amber-900 { color: #ffffff !important; }
  .detail-content-env .text-slate-600, .detail-content-env .text-slate-700, .detail-content-env .text-emerald-600, .detail-content-env .text-amber-600 { color: #a1a1aa !important; }

  /* Hide Scrollbar */
  .no-scrollbar::-webkit-scrollbar { display: none; }
  .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
</style>

<div class="relative -mx-4 min-h-screen bg-[#0C0B12]">
  <!-- 1. Immersive Hero (Fixed Aspect) -->
  <section class="relative h-[260px] w-full overflow-hidden">
    <% if @challenge.thumbnail_image.attached? %>
      <%= image_tag @challenge.thumbnail_image, class: "w-full h-full object-cover" %>
    <% else %>
      <img src="<%= @challenge.thumbnail %>" class="w-full h-full object-cover" alt="">
    <% end %>
    
    <div class="absolute inset-0 bg-gradient-to-b from-black/40 via-transparent to-[#0C0B12]"></div>
    
    <!-- Top Nav -->
    <div class="absolute top-10 left-6 right-6 flex justify-between items-center z-50">
      <%= link_to prototype_explore_path, class: "w-10 h-10 rounded-full bg-black/40 backdrop-blur-xl border border-white/10 flex items-center justify-center text-white app-tap-active" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M15 19l-7-7 7-7"/></svg>
      <% end %>
      <div class="flex items-center gap-2">
        <% if current_user&.admin? %>
          <%= link_to challenge_path(@challenge), 
                      method: :delete,
                      data: { 
                        turbo_method: :delete,
                        turbo_confirm: "'#{@challenge.title}' 챌린지를 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다." 
                      },
                      class: "w-10 h-10 rounded-full bg-black/40 backdrop-blur-xl border border-red-500/30 flex items-center justify-center text-red-400 hover:bg-red-500/20 transition-all app-tap-active" do %>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/></svg>
          <% end %>
        <% end %>
        <button 
          data-controller="share"
          data-share-title-value="<%= @challenge.title %>"
          data-share-text-value="루틴 파인더에서 '<%= @challenge.title %>' 도전에 함께해요! 🌿"
          data-share-url-value="<%= challenge_url(@challenge) %>"
          data-action="click->share#share"
          class="w-10 h-10 rounded-full bg-black/40 backdrop-blur-xl border border-white/10 flex items-center justify-center text-white app-tap-active">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z"/></svg>
        </button>
      </div>
    </div>

    <!-- Title Info -->
    <div class="absolute bottom-6 left-6 right-6 z-20 space-y-2">
      <div class="flex gap-2">
        <span class="px-2 py-0.5 rounded bg-indigo-500 text-[10px] font-black text-white uppercase tracking-tighter shadow-lg shadow-indigo-500/30">
          <%= @challenge.offline? ? 'Gathering' : 'Challenge' %>
        </span>
      </div>
      <h1 class="text-xl font-bold text-white leading-tight drop-shadow-md"><%= @challenge.title %></h1>
    </div>
  </section>

  <!-- 2. Smart Category Switcher (Sticky Control) -->
  <div class="sticky top-0 z-40 bg-[#0C0B12]/80 backdrop-blur-3xl border-b border-white/5 py-4">
    <div class="flex justify-between items-center px-4 gap-1">
      <% 
        if @challenge.gathering?
          rules_label = @challenge.virtual_gathering? ? "접속안내" : "장소안내"
          main_tabs = [
            { id: "intro", label: "상세설명" },
            { id: "rules", label: rules_label },
            { id: "guide", label: "참여안내" },
            { id: "community", label: "커뮤니티" }
          ]
        else
          main_tabs = [
            { id: "intro", label: "상세설명" },
            { id: "rules", label: "진행방법" },
            { id: "rewards", label: "리워드" },
            { id: "community", label: "커뮤니티" }
          ]
        end
      %>
      <% main_tabs.each do |t| %>
        <%= link_to challenge_path(@challenge, tab: t[:id], source: 'prototype'), 
                    class: "flex-1 px-1 py-3 rounded-2xl font-bold text-sm whitespace-nowrap transition-all flex items-center justify-center border #{@tab == t[:id] ? 'bg-white text-[#0C0B12] border-white shadow-lg' : 'text-slate-400 border-white/5 bg-white/[0.01]'}" do %>
          <%= t[:label] %>
        <% end %>
      <% end %>
    </div>
  </div>

  <!-- 3. Dynamic Content Area -->
  <section class="px-6 py-8 pb-44 detail-content-env">
    <% case @tab %>
    <% when "intro" %>
      <div class="space-y-6 animate-fade-in">
        <!-- Host Preview Card -->
        <%= link_to prototype_user_card_path(@challenge.host || 0), data: { turbo_frame: "user-profile-modal-frame" }, class: "app-glass-card p-5 border-white/5 bg-white/[0.01] flex items-center gap-4 hover:border-indigo-500/30 transition-all active:scale-95 group" do %>
          <div class="relative shrink-0">
            <img src="<%= @challenge.host&.profile_image || asset_path('rf_official_logo.png') %>" class="w-14 h-14 rounded-2xl border-2 border-indigo-500/30 object-cover bg-white/5 transition-transform group-hover:scale-105">
            <div class="absolute -right-1 -bottom-1 w-5 h-5 bg-indigo-500 rounded-full border-2 border-[#0C0B12] flex items-center justify-center">
               <svg class="w-3 h-3 text-white" fill="currentColor" viewBox="0 0 20 20"><path d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z"/></svg>
            </div>
          </div>
          <div class="flex-1 min-w-0">
            <p class="text-[10px] font-bold text-slate-500 uppercase">Challenge Host</p>
            <div class="flex items-center gap-1.5 mt-0.5">
              <% if @challenge.host&.is_rufa_club_member? %>
                <span class="shrink-0 w-3.5 h-3.5 rounded-full bg-indigo-500 flex items-center justify-center text-[8px] font-black text-white shadow-[0_0_10px_rgba(99,102,241,0.5)]">R</span>
              <% end %>
              <h3 class="text-base font-black text-white truncate group-hover:text-indigo-400 transition-colors"><%= @challenge.host_name || "플랫폼 운영자" %></h3>
            </div>
          </div>
        <% end %>
        <%= render "challenges/tabs/intro", challenge: @challenge %>
      </div>

    <% when "rules", "records" %>
      <div class="space-y-8 animate-fade-in">
        <div class="p-6 bg-indigo-500/10 border border-indigo-500/20 rounded-3xl">
           <p class="text-indigo-300 text-xs font-bold leading-relaxed">
             💡 <%= @challenge.offline? ? "모임 시간과 장소를 사전에 숙지하여 노쇼가 발생하지 않도록 주의해주세요." : "매일 정해진 가이드에 맞춰 인증샷을 남겨주시면 성실함 지수가 올라갑니다." %>
           </p>
        </div>
        <%= render "challenges/tabs/rules", challenge: @challenge %>
        <% if @challenge.physical_gathering? %>
        <div class="space-y-4">
             <h3 class="text-sm font-bold text-slate-400 uppercase tracking-widest px-2">장소 정보</h3>
             <div class="w-full h-56 bg-slate-800 rounded-3xl overflow-hidden border border-white/10 shadow-2xl relative group">
                <% if @challenge.meeting_info&.address.present? %>
                  <iframe
                    width="100%"
                    height="100%"
                    frameborder="0" style="border:0;"
                    src="https://www.google.com/maps/embed/v1/place?key=<%= ENV['GOOGLE_MAPS_API_KEY'] || 'AIzaSyA_placeholder' %>&q=<%= CGI.escape(@challenge.meeting_info.address) %>"
                    allowfullscreen>
                  </iframe>
                  <!-- Overlay for Interaction -->
                  <a href="https://www.google.com/maps/search/?api=1&query=<%= CGI.escape(@challenge.meeting_info.address) %>" 
                     target="_blank"
                     class="absolute inset-0 bg-transparent z-10"></a>
                <% else %>
                  <div class="flex items-center justify-center h-full text-slate-500 font-medium">
                    등록된 장소 주소가 없습니다.
                  </div>
                <% end %>
             </div>
             <div class="flex items-center justify-between px-2">
               <p class="text-xs font-bold text-white"><%= @challenge.meeting_info&.address %></p>
               <a href="https://www.google.com/maps/search/?api=1&query=<%= CGI.escape(@challenge.meeting_info&.address || '') %>" 
                  target="_blank"
                  class="text-[10px] font-black text-indigo-400 underline decoration-indigo-400/30 underline-offset-4">
                 큰 지도로 보기
               </a>
             </div>
          </div>
        <% end %>
      </div>

    <% when "rewards", "guide" %>
      <div class="animate-fade-in space-y-8">
        <% if @challenge.offline? %>
          <!-- Preparation Items -->
          <div class="space-y-4">
             <h3 class="text-sm font-bold text-slate-400 uppercase tracking-widest px-2">준비물 및 유의사항</h3>
             <div class="p-6 bg-amber-500/5 border border-amber-500/10 rounded-3xl">
                <% if @challenge.preparation_items.present? %>
                  <p class="text-sm font-medium text-slate-200 leading-relaxed whitespace-pre-wrap"><%= @challenge.preparation_items %></p>
                <% else %>
                  <p class="text-xs font-bold text-slate-500 italic text-center py-4 text-white">특별한 준비물 없이 편하게 참석해 주세요!</p>
                <% end %>
             </div>
          </div>

          <!-- Application Question -->
          <% if @challenge.requires_application_message? %>
            <div class="space-y-4">
               <h3 class="text-sm font-bold text-slate-400 uppercase tracking-widest px-2">신청 시 질문</h3>
               <div class="p-6 bg-white/[0.02] border border-white/5 rounded-3xl">
                  <p class="text-xs font-black text-indigo-400 mb-2 leading-relaxed">신청 시 아래 질문에 대한 답변을 함께 적어주셔야 합니다.</p>
                  <p class="text-sm font-bold text-white italic">"<%= @challenge.application_question.presence || "모임에 참여하고 싶은 이유를 간단히 적어주세요." %>"</p>
               </div>
            </div>
          <% end %>

          <!-- Refund Policy -->
          <div class="space-y-4">
             <h3 class="text-sm font-bold text-slate-400 uppercase tracking-widest px-2">환불 및 취소 규정</h3>
             <div class="p-6 bg-white/[0.02] border border-white/5 rounded-3xl">
                <p class="text-xs font-medium text-slate-400 leading-relaxed whitespace-pre-wrap"><%= @challenge.refund_policy.presence || "모임 시작 24시간 전까지 100% 환불 가능하며, 이후에는 장소 예약 등의 사유로 환불이 어려울 수 있습니다." %></p>
             </div>
          </div>
        <% else %>
          <%= render "challenges/tabs/rewards", challenge: @challenge %>
        <% end %>
      </div>

     <% when "community" %>
      <div class="space-y-10 animate-fade-in">
         <div class="space-y-4">
            <h3 class="text-sm font-bold text-slate-400 uppercase tracking-widest px-2">참여 멤버</h3>
            <%= render "challenges/tabs/participants", challenge: @challenge, participants: @participants %>
         </div>
         <div class="space-y-4">
            <h3 class="text-sm font-bold text-slate-400 uppercase tracking-widest px-2">참여 후기 <span class="text-xs text-indigo-400 ml-1"><%= @reviews.count %></span></h3>
            <%= render "challenges/tabs/reviews", challenge: @challenge, reviews: @reviews, can_write_review: @can_write_review %>
         </div>
      </div>
    <% end %>
  </section>

  <!-- 4. Fixed Final Action Bar -->
  <div class="fixed bottom-8 left-1/2 -translate-x-1/2 w-[90%] max-w-sm z-[500]">
    <div class="app-glass-card p-3 !rounded-[28px] border-white/5 bg-[#16151D]/98 backdrop-blur-3xl shadow-2xl flex items-center gap-4">
      <div class="flex-1 pl-4">
        <p class="text-[9px] font-bold text-slate-500 uppercase tracking-widest mb-0.5 leading-none">Total Fee</p>
        <div class="flex items-baseline gap-1">
          <span class="text-base font-bold text-white"><%= @challenge.cost_type_free? ? '0' : number_with_delimiter(@challenge.total_payment_amount) %></span>
          <span class="text-[9px] font-bold text-slate-500">원</span>
        </div>
      </div>
      
      <% if @is_host %>
         <%= link_to hosted_challenge_path(@challenge, source: params[:source]), class: "px-8 py-3.5 app-fab-gradient rounded-[20px] text-xs font-bold text-white active:scale-95 transition-all" do %>
           관리하기
         <% end %>
      <% elsif @is_joined %>
         <%= link_to challenge_path(@challenge, tab: 'records', source: 'prototype'), class: "px-8 py-3.5 bg-emerald-600 rounded-[20px] text-xs font-bold text-white active:scale-95 transition-all" do %>
           대시보드
         <% end %>
      <% else %>
        <% if logged_in? %>
          <button data-action="click->modal#open" class="px-8 py-3.5 app-fab-gradient rounded-[20px] text-xs font-bold text-white active:scale-95 transition-all cursor-pointer">
            지금 참여
          </button>
        <% else %>
          <%= link_to prototype_login_path, 
                      class: "px-8 py-3.5 app-fab-gradient rounded-[20px] text-xs font-bold text-white active:scale-95 transition-all text-center cursor-pointer flex items-center justify-center" do %>
            지금 참여
          <% end %>
        <% end %>
      <% end %>
    </div>
  </div>

  <!-- Redesigned Modal -->
  <div data-modal-target="container" 
       data-action="click->modal#closeOnBackground" 
       class="fixed inset-0 z-[600] hidden bg-black/80 backdrop-blur-xl flex items-center justify-center p-6">
    <div class="app-glass-card w-full max-w-md bg-[#1B1A24] border-white/10 overflow-hidden animate-slide-up">
      <div class="p-8 text-center space-y-6">
        <div class="w-20 h-20 rounded-[32px] bg-indigo-500/10 flex items-center justify-center text-4xl mx-auto border border-white/5">
          <%= @challenge.category == 'HEALTH' ? '💪' : '🌱' %>
        </div>
        <h2 class="text-2xl font-black text-white tracking-tight"><%= @challenge.title %></h2>
        <p class="text-sm font-bold text-slate-500">함께 성취를 만들어볼까요?</p>

        <div class="pt-4 space-y-3">
          <% if @challenge.cost_type_free? %>
            <% if logged_in? %>
              <%= button_to join_challenge_path(@challenge, source: 'prototype'), 
                            method: :post, 
                            class: "w-full py-4.5 app-fab-gradient text-white rounded-2xl font-black text-sm active:scale-95 transition-all cursor-pointer" do %>
                지금 시작하기
              <% end %>
            <% else %>
              <%= link_to prototype_login_path, 
                          class: "block w-full py-4.5 app-fab-gradient text-white rounded-2xl font-black text-sm active:scale-95 transition-all text-center cursor-pointer" do %>
                지금 시작하기
              <% end %>
            <% end %>
          <% else %>
            <% if logged_in? %>
              <%= link_to new_challenge_application_path(@challenge, source: 'prototype'), 
                          class: "block w-full py-4.5 app-fab-gradient text-white rounded-2xl font-black text-sm active:scale-95 transition-all text-center cursor-pointer" do %>
                신청 정보 확인
              <% end %>
            <% else %>
              <%= link_to prototype_login_path, 
                          class: "block w-full py-4.5 app-fab-gradient text-white rounded-2xl font-black text-sm active:scale-95 transition-all text-center cursor-pointer" do %>
                신청 정보 확인
              <% end %>
            <% end %>
          <% end %>
          <button data-action="click->modal#close" class="w-full py-2 text-slate-600 font-bold text-[10px] tracking-widest cursor-pointer">Close</button>
        </div>
      </div>
    </div>
  </div>
</div>
