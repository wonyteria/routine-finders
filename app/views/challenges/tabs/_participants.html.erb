<div class="space-y-10">
  <div class="flex items-center justify-between px-1">
    <h2 class="text-xs font-bold text-slate-300 uppercase tracking-[0.2em] flex items-center gap-2">
      <span class="w-1 h-3 bg-indigo-400 rounded-full"></span>
      참여자 목록 (<%= participants.count %>)
    </h2>
    <div class="flex items-center gap-2">
      <span class="w-1.5 h-1.5 bg-emerald-500 rounded-full animate-pulse"></span>
      <span class="text-[10px] font-bold text-slate-400 uppercase tracking-widest">Active Members</span>
    </div>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
    <% participants.each do |p| %>
      <%= link_to prototype_user_card_path(p.user), data: { turbo_frame: "user-profile-modal-frame" }, class: "group block" do %>
        <div class="p-6 bg-white/[0.04] border border-white/5 rounded-3xl hover:border-indigo-500/30 transition-all group overflow-hidden relative active:scale-95 flex flex-col items-center text-center">
          <div class="relative shrink-0 mb-4">
            <!-- Profile Image -->
            <div class="w-20 h-20 rounded-[28px] overflow-hidden border-2 border-white/5 shadow-2xl transition-transform group-hover:scale-105">
              <img src="<%= p.user.profile_image %>" class="w-full h-full object-cover bg-white/5">
            </div>
            
            <!-- RF Badge (Top-Right) -->
            <% if p.user.is_rufa_club_member? %>
              <div class="absolute -top-2 -right-2 w-8 h-8 rounded-full bg-[#0C0B12] border-2 border-indigo-500 flex items-center justify-center shadow-[0_0_15px_rgba(99,102,241,0.4)] z-10 transition-transform group-hover:rotate-12">
                <span class="text-[10px] font-black text-white italic tracking-tighter">RF</span>
              </div>
            <% end %>

            <!-- Level Badge (Bottom-Right) -->
            <div class="absolute -bottom-1 -right-1 px-2.5 py-1 bg-white rounded-full border border-black/10 flex items-center justify-center shadow-lg z-10">
              <span class="text-[10px] font-black text-black leading-none italic">Lv.<%= p.user.level %></span>
            </div>
          </div>

          <div class="w-full min-w-0">
            <h3 class="text-base font-black text-white truncate group-hover:text-indigo-400 transition-colors"><%= p.user.nickname %></h3>
            <% if challenge.offline? %>
              <p class="text-[10px] font-medium text-slate-500 mt-1 line-clamp-1 italic">
                <%= p.user.bio.present? ? p.user.bio : "루틴으로 변화를 꿈꾸는 중" %>
              </p>
            <% else %>
              <div class="mt-2 inline-flex items-center gap-1.5 px-3 py-1 rounded-full bg-indigo-500/10 border border-indigo-500/20">
                <span class="text-[10px] font-black text-indigo-400">🔥 <%= p.current_streak %>일 연속</span>
              </div>
            <% end %>
          </div>
        </div>
      <% end %>
    <% end %>

    <% if participants.empty? %>
      <div class="col-span-full py-20 text-center space-y-4">
        <div class="w-20 h-20 bg-slate-100 rounded-[32px] flex items-center justify-center mx-auto text-slate-300">
          <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"/></svg>
        </div>
        <p class="text-slate-400 font-bold italic">아직 참여자가 없습니다.<br>이 챌린지의 첫 번째 주인공이 되어보세요!</p>
      </div>
    <% end %>
  </div>
</div>
