<%= form_with model: [challenge, announcement], local: true, class: "bg-white rounded-[40px] border border-slate-200 p-10 shadow-sm space-y-8" do |f| %>
  <% if announcement.errors.any? %>
    <div class="p-4 bg-red-50 rounded-2xl border border-red-200">
      <p class="text-sm font-bold text-red-600"><%= announcement.errors.full_messages.join(", ") %></p>
    </div>
  <% end %>

  <div class="space-y-8">
    <h2 class="text-xl font-black text-slate-900 flex items-center gap-2">
      <svg class="w-6 h-6 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5.882V19.24a1.76 1.76 0 01-3.417.592l-2.147-6.15M18 13a3 3 0 100-6M5.436 13.683A4.001 4.001 0 017 6h1.832c4.1 0 7.625-1.234 9.168-3v14c-1.543-1.766-5.067-3-9.168-3H7a3.988 3.988 0 01-1.564-.317z"/></svg>
      공지사항 내용
    </h2>

    <div class="space-y-2">
      <%= f.label :title, class: "text-[10px] font-black text-slate-400 uppercase tracking-widest ml-1" do %>제목<% end %>
      <%= f.text_field :title, placeholder: "공지사항 제목을 입력하세요", class: "w-full bg-slate-50 border-none rounded-2xl p-5 text-lg font-bold focus:ring-2 focus:ring-indigo-500" %>
    </div>

    <div class="space-y-2">
      <%= f.label :content, class: "text-[10px] font-black text-slate-400 uppercase tracking-widest ml-1" do %>내용 (선택)<% end %>
      <%= f.text_area :content, placeholder: "공지사항 상세 내용을 입력하세요", rows: 6, class: "w-full bg-slate-50 border-none rounded-[24px] p-6 font-medium focus:ring-2 focus:ring-indigo-500" %>
    </div>
  </div>

  <div class="p-6 bg-slate-50 rounded-3xl border border-slate-100 flex items-start gap-3">
    <svg class="w-5 h-5 text-indigo-600 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
    <p class="text-xs font-bold text-slate-500 leading-relaxed">
      공지사항을 등록하면 모든 참가자에게 알림이 발송됩니다.
    </p>
  </div>

  <div class="pt-6">
    <%= f.submit announcement.persisted? ? "수정하기" : "등록하기", class: "w-full py-6 bg-indigo-600 text-white rounded-2xl font-black text-xl shadow-xl hover:bg-indigo-700 transition-all cursor-pointer" %>
  </div>
<% end %>
