<% 
  is_premium = @permission.is_premium_member?
  is_proto = true # Always use prototype experience
  
  # PROTOTYPE STYLE: Force dark theme
  is_proto_style = true
  
  app_bg = "bg-[#0C0B12]"
  text_color = "text-white"
%>

<div class="<%= app_bg %> <%= text_color %> <%= is_proto ? "" : "-mx-4 -mt-10 px-4 pt-10" %> min-h-screen transition-all duration-700">
  
  <div class="<%= is_proto ? "max-w-md mx-auto" : "max-w-7xl mx-auto" %> space-y-12 pb-24">
    
    <!-- 1. Unified Achievement Dashboard (Top Section) -->
    <%= render 'unified_dashboard_header', is_proto_style: is_proto_style %>

    <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-12 items-start">
      <!-- Left Column: Goals & Routines -->
      <div class="lg:col-span-8 space-y-10">
        
        <!-- 2. Goal Architecture (Melted into top of list) -->
        <%= render 'unified_goal_architecture', is_proto_style: is_proto_style %>

        <!-- 3. Routine List Section -->
        <div class="space-y-6">
          <div class="flex items-center justify-between px-2">
            <div class="space-y-1">
              <h2 class="text-xl md:text-2xl font-black tracking-tight">나의 성취 루틴</h2>
              <p class="<%= is_proto_style ? "text-indigo-400" : "text-indigo-600" %> text-xs font-bold uppercase tracking-widest">Daily Success Path</p>
            </div>
            
            <!-- Item Slot (Game Element) -->
            <% if is_premium %>
              <div class="flex items-center gap-3">
                <div class="flex items-center gap-2 px-4 py-2 bg-white/5 backdrop-blur-md rounded-2xl border border-white/10 shadow-xl group cursor-help" title="휴식권: 실패를 1회 면제해주는 아이템">
                  <span class="text-lg">🎟️</span>
                  <span class="text-sm font-black tabular-nums"><%= @my_membership.remaining_passes %></span>
                </div>
                <div class="flex items-center gap-2 px-4 py-2 bg-white/5 backdrop-blur-md rounded-2xl border border-white/10 shadow-xl group cursor-help" title="성장 포인트: 챌린지 개설 등에 사용">
                  <span class="text-lg text-amber-400">⚡</span>
                  <span class="text-sm font-black tabular-nums"><%= @my_membership.growth_points %>P</span>
                </div>
              </div>
            <% else %>
              <div class="<%= is_proto_style ? "opacity-40 grayscale" : "opacity-40 grayscale" %> flex items-center gap-3">
                <div class="flex items-center gap-2 px-4 py-2 <%= is_proto_style ? "bg-white/5 border border-white/10" : "bg-slate-200 border border-slate-300" %> rounded-2xl">
                  <span class="text-lg">🔒</span>
                  <span class="text-[10px] font-bold">휴식권</span>
                </div>
              </div>
            <% end %>
          </div>

          <!-- Unified Routine Grid -->
          <div class="<%= is_proto ? "" : (is_proto_style ? "bg-indigo-600/90" : "bg-indigo-600") %> <%= is_proto ? "" : "rounded-[40px] p-6 md:p-10 shadow-2xl" %> relative overflow-hidden">
            <% if !is_proto %>
              <div class="absolute top-0 right-0 w-80 h-80 bg-white/5 rounded-full -mr-40 -mt-40 blur-3xl"></div>
              <% if is_proto_style %>
                <div class="absolute bottom-0 left-0 w-64 h-64 bg-indigo-400/10 rounded-full -ml-32 -mb-32 blur-3xl animate-pulse"></div>
              <% end %>
            <% end %>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-4 md:gap-6 relative z-10">
              <% if @todays_routines.any? %>
                <% @todays_routines.each do |routine| %>
                  <%= render 'unified_routine_card', routine: routine, is_proto_style: is_proto_style %>
                <% end %>
              <% else %>
                <div class="col-span-full py-20 text-center space-y-4">
                  <div class="text-5xl opacity-20">✨</div>
                  <p class="<%= is_proto_style ? "text-white/40" : "text-slate-400" %> font-bold">오늘 예정된 루틴이 없습니다. 새로운 습관을 추가해보세요!</p>
                </div>
              <% end %>
            </div>
          </div>
        </div>

        <!-- 4. Quick Actions (Melted) -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
           <!-- New Routine Form (Enhanced) -->
           <div class="<%= is_proto_style ? "bg-[#1B1A24] border border-white/5" : "bg-white border border-slate-100" %> rounded-[40px] p-8 space-y-6">
             <div class="flex items-center justify-between">
                <div class="space-y-1">
                  <h3 class="text-lg font-black italic">NEW ROUTINE</h3>
                  <p class="text-[10px] font-bold opacity-50 uppercase tracking-widest">Add Engineering</p>
                </div>
                <div class="w-12 h-12 <%= is_proto_style ? "bg-indigo-500/20 text-indigo-400" : "bg-indigo-50 text-indigo-600" %> rounded-2xl flex items-center justify-center text-2xl">📐</div>
             </div>
             <%= render 'form', routine: PersonalRoutine.new %>
           </div>

           <!-- Total Overview (Compact) -->
           <div class="<%= is_proto_style ? "bg-[#1B1A24] border border-white/5" : "bg-white border border-slate-100" %> rounded-[40px] p-8 space-y-6 flex flex-col">
              <div class="flex items-center justify-between">
                <div class="space-y-1">
                  <h3 class="text-lg font-black italic">ALL ARCHIVES</h3>
                  <p class="text-[10px] font-bold opacity-50 uppercase tracking-widest">Total Manage</p>
                </div>
                <div class="w-12 h-12 <%= is_proto_style ? "bg-slate-500/20 text-slate-400" : "bg-slate-50 text-slate-500" %> rounded-2xl flex items-center justify-center text-2xl">📋</div>
              </div>
              <div class="flex-1 space-y-2 max-h-[280px] overflow-y-auto custom-scrollbar pr-2">
                <% current_user.personal_routines.where(deleted_at: nil).order(created_at: :desc).each do |routine| %>
                  <%= render 'total_routine_item', routine: routine %>
                <% end %>
              </div>
           </div>
        </div>
      </div>

      <!-- Right Column: Sidebar (Synergy & Ranking) -->
      <aside class="lg:col-span-4 space-y-8 sticky top-12">
        
        <!-- 5. Real-time Synergy Feed (Inlay) -->
        <%= render 'unified_synergy_widget', is_proto_style: is_proto_style %>
  
        <!-- 6. Ranking & Hall of Fame (Aspirational) -->
        <%= render 'unified_ranking_widget', is_proto_style: is_proto_style %>
  
        <!-- 7. Premium Pass Button (Only for Membership) -->
        <% if is_premium %>
           <%= render 'unified_premium_actions', is_proto_style: is_proto_style %>
        <% end %>

      </aside>
    </div>
  </div>
</div>

<!-- Premium Check-in Effects Holder (JS will inject here) -->
<div id="premium-effects-container" class="fixed inset-0 pointer-events-none z-[200]"></div>

<script>
  // Shared logic for unified dashboard
  function toggleDropdown(id) {
    document.querySelectorAll('[id^="dropdown-"]').forEach(dropdown => {
      if (dropdown.id !== `dropdown-${id}`) dropdown.classList.add('hidden');
    });
    const dropdown = document.getElementById(`dropdown-${id}`);
    if (dropdown) dropdown.classList.toggle('hidden');
  }

  // Animation for check-in
  document.addEventListener('turbo:load', () => {
    // Premium specific animations
    if (<%= is_premium %>) {
      console.log("RUFA Premium Active");
    }
  });
</script>
