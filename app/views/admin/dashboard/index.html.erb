<div class="max-w-7xl mx-auto">
  <!-- Header -->
  <header class="flex justify-between items-end mb-12">
    <div>
      <h1 class="text-4xl font-black tracking-tight mb-2">System Dashboard</h1>
      <p class="text-slate-400 font-medium">서비스의 전반적인 성장 지표와 활동을 모니터링합니다.</p>
    </div>
    <div class="hidden lg:flex items-center gap-4 bg-white/5 p-1 rounded-2xl border border-white/10">
      <button class="px-4 py-2 bg-white/10 rounded-xl text-sm font-bold">1D</button>
      <button class="px-4 py-2 text-sm font-bold text-slate-500 hover:text-white transition-colors">1W</button>
      <button class="px-4 py-2 text-sm font-bold text-slate-500 hover:text-white transition-colors">1M</button>
    </div>
  </header>

  <!-- Stats Grid -->
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-12">
    <div class="admin-stat-card admin-glass">
      <div class="accent-glow"></div>
      <p class="text-[10px] uppercase font-black text-slate-500 tracking-[0.2em] mb-4">Total Users</p>
      <div class="flex items-baseline gap-2">
        <h3 class="text-4xl font-black"><%= number_with_delimiter @stats[:total_users] %></h3>
        <span class="text-emerald-500 text-xs font-bold">+<%= @stats[:new_users_today] %> today</span>
      </div>
      <div class="mt-6 h-1.5 w-full bg-white/5 rounded-full overflow-hidden">
        <div class="h-full bg-indigo-500 w-[65%] shadow-[0_0_10px_rgba(99,102,241,0.5)]"></div>
      </div>
    </div>

    <div class="admin-stat-card admin-glass">
      <div class="accent-glow"></div>
      <p class="text-[10px] uppercase font-black text-slate-500 tracking-[0.2em] mb-4">Active Challenges</p>
      <div class="flex items-baseline gap-2">
        <h3 class="text-4xl font-black"><%= @stats[:active_challenges] %></h3>
        <span class="text-slate-500 text-xs font-bold">running now</span>
      </div>
      <div class="mt-6 flex -space-x-3">
        <% 4.times do %>
          <div class="w-8 h-8 rounded-full border-2 border-[#1a1c2e] bg-slate-800"></div>
        <% end %>
        <div class="w-8 h-8 rounded-full border-2 border-[#1a1c2e] bg-indigo-600 flex items-center justify-center text-[10px] font-bold">+12</div>
      </div>
    </div>

    <div class="admin-stat-card admin-glass">
      <div class="accent-glow"></div>
      <p class="text-[10px] uppercase font-black text-slate-500 tracking-[0.2em] mb-4">Today Completions</p>
      <div class="flex items-baseline gap-2">
        <h3 class="text-4xl font-black"><%= number_with_delimiter @stats[:today_completions] %></h3>
        <span class="text-indigo-400 text-xs font-bold">tasks</span>
      </div>
      <p class="mt-6 text-xs font-medium text-slate-400">전일 대비 <span class="text-emerald-400 font-bold">12.5%</span> 상승</p>
    </div>

    <div class="admin-stat-card admin-glass bg-indigo-600/10 border-indigo-500/20">
      <div class="accent-glow opacity-30"></div>
      <p class="text-[10px] uppercase font-black text-indigo-400 tracking-[0.2em] mb-4">Estimated Revenue</p>
      <div class="flex items-baseline gap-1">
        <span class="text-xl font-bold text-indigo-300">₩</span>
        <h3 class="text-3xl font-black tracking-tight"><%= number_to_human @revenue_data[:this_month], units: { thousand: 'k', million: 'm' } %></h3>
      </div>
      <div class="mt-6 py-2 px-3 rounded-xl bg-indigo-600/20 inline-flex items-center gap-2">
        <div class="w-2 h-2 rounded-full bg-indigo-400 animate-pulse"></div>
        <span class="text-xs font-black text-indigo-300">Growth +<%= @revenue_data[:growth] %>%</span>
      </div>
    </div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
    <!-- Recent Users -->
    <section class="lg:col-span-2 admin-glass rounded-[32px] overflow-hidden">
      <div class="p-8 border-b border-white/5 flex justify-between items-center">
        <h2 class="text-xl font-black">최근 가입 유저</h2>
        <a href="/admin/users" class="text-xs font-bold text-indigo-400 hover:text-indigo-300 transition-colors uppercase tracking-widest">View All</a>
      </div>
      <div class="overflow-x-auto">
        <table class="w-full text-left border-collapse">
          <thead>
            <tr class="text-[10px] uppercase tracking-widest text-slate-500">
              <th class="px-8 py-4 font-black">User</th>
              <th class="px-8 py-4 font-black">Role</th>
              <th class="px-8 py-4 font-black">Joined At</th>
              <th class="px-8 py-4 font-black">Actions</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-white/5">
            <% @recent_users.each do |user| %>
              <tr class="group hover:bg-white/[0.02] transition-colors">
                <td class="px-8 py-5">
                  <div class="flex items-center gap-3">
                    <div class="w-10 h-10 rounded-xl bg-slate-800 border border-white/5 flex items-center justify-center font-bold text-indigo-400 group-hover:scale-110 transition-transform">
                      <%= user.nickname.first %>
                    </div>
                    <div>
                      <p class="text-sm font-black"><%= user.nickname %></p>
                      <p class="text-[11px] text-slate-500 font-medium"><%= user.email %></p>
                    </div>
                  </div>
                </td>
                <td class="px-8 py-5">
                  <span class="px-2.5 py-1 rounded-lg text-[10px] font-black uppercase tracking-wider <%= user.super_admin? ? 'bg-indigo-500/20 text-indigo-400 border border-indigo-500/20' : 'bg-slate-500/10 text-slate-400 border border-white/10' %>">
                    <%= user.role %>
                  </span>
                </td>
                <td class="px-8 py-5 text-xs text-slate-400 font-medium">
                  <%= time_ago_in_words(user.created_at) %> ago
                </td>
                <td class="px-8 py-5">
                  <button class="p-2 hover:bg-white/10 rounded-lg transition-all text-slate-500 hover:text-white">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 5v.01M12 12v.01M12 19v.01M12 6a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2z"/></svg>
                  </button>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </section>

    <!-- Side Panel: System Status -->
    <section class="flex flex-col gap-6">
      <div class="admin-glass rounded-[32px] p-8">
        <h2 class="text-xl font-black mb-6">시스템 현황</h2>
        <div class="space-y-6">
          <div class="flex items-center justify-between">
            <div class="flex items-center gap-3">
              <div class="w-2 h-2 rounded-full bg-emerald-500 shadow-[0_0_10px_#10b981]"></div>
              <span class="text-sm font-black">Server Status</span>
            </div>
            <span class="text-xs font-bold text-slate-500">Stabe</span>
          </div>
          <div class="flex items-center justify-between">
            <div class="flex items-center gap-3">
              <div class="w-2 h-2 rounded-full bg-emerald-500 shadow-[0_0_10px_#10b981]"></div>
              <span class="text-sm font-black">Database</span>
            </div>
            <span class="text-xs font-bold text-slate-500">Connected</span>
          </div>
          <div class="flex items-center justify-between">
            <div class="flex items-center gap-3">
              <div class="w-2 h-2 rounded-full bg-amber-500 shadow-[0_0_10px_#f59e0b]"></div>
              <span class="text-sm font-black">Memory Usage</span>
            </div>
            <span class="text-xs font-bold text-slate-500">68.4%</span>
          </div>
        </div>
      </div>

      <div class="admin-glass rounded-[32px] p-8 bg-gradient-to-br from-indigo-600/20 to-transparent">
        <h2 class="text-xl font-black mb-2">관리 가이드</h2>
        <p class="text-xs text-slate-400 mb-6 leading-relaxed">
          새로운 챌린지 승인 및 비정상 유저 제재는 사용자 관리 메뉴를 이용하세요. 문의사항은 시스템 관리팀에 연락 바랍니다.
        </p>
        <button class="w-full py-4 bg-indigo-600 hover:bg-indigo-500 rounded-2xl text-sm font-black transition-all shadow-lg shadow-indigo-600/20">
          시스템 리포트 다운로드
        </button>
      </div>
    </section>
  </div>
</div>
