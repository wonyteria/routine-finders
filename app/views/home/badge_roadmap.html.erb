<div class="max-w-[1200px] mx-auto pb-32 space-y-12 md:space-y-24 px-4 md:px-6">
  <!-- Header Section -->
  <div class="flex flex-col md:flex-row md:items-end justify-between gap-8 pt-10 border-b border-slate-100 pb-12">
    <div class="space-y-6">
      <%= link_to achievement_report_path, class: "inline-flex items-center gap-2 text-indigo-600 font-extrabold text-xs uppercase tracking-widest group" do %>
        <i data-feather="arrow-left" class="w-4 h-4 transition-transform group-hover:-translate-x-1"></i>
        분석 리포트로 돌아가기
      <% end %>
      <div class="space-y-2">
        <h2 class="text-3xl md:text-6xl font-black text-slate-900 tracking-tighter leading-none">
          뱃지 로드맵
        </h2>
        <p class="text-slate-500 font-bold text-base md:text-xl">루틴의 성장이 뱃지로 기록되는 도전의 여정입니다.</p>
      </div>
    </div>
    
    <div class="relative group/prog">
      <div class="absolute inset-0 bg-indigo-600 blur-2xl opacity-10 group-hover/prog:opacity-20 transition-opacity"></div>
      <div class="relative bg-white border border-slate-100 rounded-[32px] p-6 md:p-8 shadow-xl shadow-indigo-100/20 flex items-center gap-6">
        <div class="w-16 h-16 bg-indigo-600 rounded-2xl flex items-center justify-center text-white shadow-lg">
          <i data-feather="award" class="w-8 h-8"></i>
        </div>
        <div>
          <p class="text-[10px] font-black text-indigo-600 uppercase tracking-[0.2em] mb-1">My Progression</p>
          <div class="flex items-baseline gap-1.5">
            <span class="text-3xl font-black text-slate-900"><%= @user_badge_ids.count %></span>
            <span class="text-sm font-bold text-slate-400">/ <%= Badge.count %> 획득</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Roadmaps -->
  <div class="space-y-20 md:space-y-32">
    <% @badges_by_type.each do |type, badges| %>
      <div class="space-y-8 animate-in fade-in slide-in-from-bottom-8 duration-700">
        <div class="flex items-center justify-between">
          <div class="flex items-center gap-4">
            <div class="w-12 h-12 bg-white rounded-2xl shadow-sm border border-slate-100 flex items-center justify-center text-2xl">
              <%= case type 
                  when 'achievement_rate' then "🎯"
                  when 'verification_count' then "✨"
                  when 'max_streak' then "🔥"
                  else "🌟" end %>
            </div>
            <div>
              <div class="flex items-center gap-2">
                <h3 class="text-xl md:text-3xl font-black text-slate-900"><%= type.to_s.humanize %></h3>
                <% 
                  tip_content = case type
                    when 'achievement_rate' then "평균 달성률 상위 랭커를 위한 증표"
                    when 'verification_count' then "꾸준한 실천 횟수를 증명하는 훈장"
                    when 'max_streak' then "멈추지 않는 연속 실천의 불꽃"
                    else "" end
                %>
                <% if tip_content.present? %>
                  <span class="px-2 py-0.5 bg-indigo-50 text-indigo-600 text-[9px] font-black rounded-md uppercase tracking-wider md:inline hidden">
                    <%= tip_content %>
                  </span>
                <% end %>
              </div>
              <p class="text-[10px] md:text-xs text-slate-400 font-bold uppercase tracking-widest"><%= badges.first.description.split(' ')[0..2].join(' ') %> 시리즈</p>
            </div>
          </div>
          
          <div class="md:hidden">
            <span class="text-[9px] font-black text-slate-400 uppercase flex items-center gap-1 animate-pulse">
              Swipe <i data-feather="arrow-right" class="w-3 h-3"></i>
            </span>
          </div>
        </div>

        <div class="relative -mx-4 md:mx-0 px-4 md:px-0">
          <!-- Horizontal Scroll Container on Mobile, Grid on Desktop -->
          <div class="flex md:grid md:grid-cols-5 gap-6 md:gap-4 overflow-x-auto md:overflow-visible pb-8 md:pb-0 scroll-smooth no-scrollbar snap-x relative z-10 min-h-[220px]">
            
            <!-- Connector Line Background (Desktop) -->
            <div class="absolute top-[56px] left-[56px] right-[56px] h-1 bg-slate-100 z-0 hidden md:block rounded-full"></div>

            <% badges.each_with_index do |badge, index| %>
              <% is_earned = @user_badge_ids.include?(badge.id) %>
              <% is_next = !is_earned && (index == 0 || @user_badge_ids.include?(badges[index-1].id)) %>
              
              <div class="flex-shrink-0 w-[240px] md:w-auto snap-center flex flex-col items-center gap-6 group relative">
                
                <!-- Connector (Desktop Active) -->
                <% if index > 0 && is_earned %>
                  <div class="absolute right-[calc(50%+56px)] top-[56px] left-[calc(-50%+56px)] h-1 bg-indigo-500 z-0 hidden md:block"></div>
                <% end %>

                <div class="relative">
                  <div class="w-28 h-28 md:w-28 md:h-28 rounded-[40px] flex items-center justify-center text-4xl md:text-5xl transition-all duration-700 relative z-10 
                    <%= is_earned ? 'bg-white shadow-2xl shadow-indigo-100 ring-1 ring-slate-100 ring-offset-4 ring-offset-indigo-500' : 
                        (is_next ? 'bg-indigo-50 border-2 border-dashed border-indigo-200' : 'bg-slate-50 opacity-40 grayscale filter blur-[0.5px]') %>">
                    <%= is_earned || is_next ? badge.icon_path : "🔒" %>
                  </div>

                  <% if is_earned %>
                    <div class="absolute -top-2 -right-2 w-8 h-8 bg-indigo-600 text-white rounded-full flex items-center justify-center shadow-lg transform rotate-12">
                      <i data-feather="check" class="w-5 h-5 stroke-[4]"></i>
                    </div>
                  <% end %>
                  
                  <% if is_next %>
                    <div class="absolute -top-3 left-1/2 -translate-x-1/2 px-3 py-1 bg-amber-400 text-slate-900 text-[9px] font-black rounded-full shadow-lg whitespace-nowrap animate-bounce uppercase tracking-tighter">
                      Next Target
                    </div>
                  <% end %>
                </div>

                <div class="text-center space-y-3 px-4">
                  <div>
                    <h4 class="text-base md:text-lg font-black tracking-tight <%= is_earned ? 'text-slate-900' : (is_next ? 'text-indigo-600' : 'text-slate-300') %>">
                      <%= badge.name %>
                    </h4>
                    <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest mt-1">LV.<%= badge.level %></p>
                  </div>
                  
                  <div class="p-3 bg-white border border-slate-100 rounded-2xl shadow-sm space-y-1 group-hover:border-indigo-100 transition-colors">
                    <p class="text-[9px] font-bold text-slate-400 leading-tight">
                      <%= badge.description %>
                    </p>
                    <div class="flex items-center justify-center gap-1.5 pt-1.5 border-t border-slate-50 mt-1.5">
                      <span class="text-[10px] font-black text-slate-900 tracking-tight"><%= badge.requirement_value %></span>
                      <span class="text-[8px] font-bold text-slate-400 uppercase tracking-wider">Required</span>
                    </div>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <!-- Legend Section -->
  <div class="bg-indigo-50/30 rounded-[48px] p-8 md:p-12 flex flex-col md:flex-row items-center justify-center gap-8 md:gap-16 border border-indigo-100/50 relative overflow-hidden">
    <div class="absolute top-0 left-0 w-32 h-32 bg-indigo-500/5 rounded-full blur-3xl -ml-16 -mt-16"></div>
    <div class="flex items-center gap-4 group cursor-default">
      <div class="w-8 h-8 rounded-2xl bg-white shadow-lg ring-1 ring-indigo-500 ring-offset-2 flex items-center justify-center text-indigo-500 group-hover:scale-110 transition-transform">
        <i data-feather="check" class="w-4 h-4 stroke-[3]"></i>
      </div>
      <span class="text-slate-900 font-extrabold text-sm tracking-tight">달성 완료</span>
    </div>
    <div class="flex items-center gap-4 group cursor-default">
      <div class="w-8 h-8 rounded-2xl bg-indigo-50 border-2 border-dashed border-indigo-200 flex items-center justify-center text-indigo-300 group-hover:scale-110 transition-transform">
        <i data-feather="target" class="w-4 h-4"></i>
      </div>
      <span class="text-slate-900 font-extrabold text-sm tracking-tight">다음 목표</span>
    </div>
    <div class="flex items-center gap-4 group cursor-default opacity-50 grayscale transition-all hover:opacity-100 hover:grayscale-0">
      <div class="w-8 h-8 rounded-2xl bg-slate-100 flex items-center justify-center text-slate-400">
        <i data-feather="lock" class="w-4 h-4"></i>
      </div>
      <span class="text-slate-900 font-extrabold text-sm tracking-tight">잠금 상태</span>
    </div>
  </div>
</div>
</div>
