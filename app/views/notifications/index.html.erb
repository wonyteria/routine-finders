<div class="max-w-2xl mx-auto space-y-8 pb-32">
  <!-- Header -->
  <div class="flex items-center justify-between">
    <div>
      <h1 class="text-3xl font-black text-slate-900">알림</h1>
      <p class="text-slate-500 font-bold mt-1">새로운 소식을 확인하세요</p>
    </div>
    <% if @notifications.unread.any? %>
      <%= button_to mark_all_as_read_notifications_path, method: :post, class: "px-6 py-3 bg-slate-100 text-slate-600 rounded-xl font-bold text-sm hover:bg-slate-200 transition-all" do %>
        모두 읽음 처리
      <% end %>
    <% end %>
  </div>

  <!-- Notifications List -->
  <div class="bg-white rounded-[40px] border border-slate-200 shadow-sm overflow-hidden">
    <% if @notifications.any? %>
      <% @notifications.each do |notification| %>
        <div class="flex items-start gap-5 p-6 border-b border-slate-100 last:border-b-0 <%= notification.is_read ? 'opacity-60' : '' %>">
          <div class="w-12 h-12 rounded-2xl flex items-center justify-center text-2xl
            <% case notification.notification_type %>
            <% when 'settlement' %>
              bg-green-50
            <% when 'system' %>
              bg-indigo-50
            <% when 'reminder' %>
              bg-yellow-50
            <% when 'deadline' %>
              bg-red-50
            <% else %>
              bg-slate-50
            <% end %>
          ">
            <% case notification.notification_type %>
            <% when 'settlement' %>
              💰
            <% when 'system' %>
              🎯
            <% when 'reminder' %>
              ⏰
            <% when 'deadline' %>
              ⚠️
            <% when 'approval' %>
              ✅
            <% when 'rejection' %>
              ❌
            <% else %>
              📢
            <% end %>
          </div>
          <div class="flex-1">
            <div class="flex items-start justify-between gap-4">
              <div>
                <p class="font-black text-slate-900"><%= notification.title %></p>
                <p class="text-sm text-slate-500 mt-1"><%= notification.content %></p>
              </div>
              <% unless notification.is_read %>
                <%= button_to mark_as_read_notification_path(notification), method: :post, class: "p-2 text-slate-300 hover:text-indigo-600 transition-all" do %>
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
                <% end %>
              <% end %>
            </div>
            <p class="text-xs text-slate-400 mt-2"><%= time_ago_in_words(notification.created_at) %> 전</p>
          </div>
        </div>
      <% end %>
    <% else %>
      <div class="p-12 text-center">
        <div class="w-16 h-16 bg-slate-100 rounded-full flex items-center justify-center text-3xl mx-auto mb-4">🔔</div>
        <p class="text-slate-400 font-bold">새로운 알림이 없습니다.</p>
      </div>
    <% end %>
  </div>
</div>
