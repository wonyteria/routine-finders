<!DOCTYPE html>
<html class="scroll-smooth">
  <head>
    <title><%= content_for(:title) || "Routine Finders" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="<%= content_for(:description) || "함께 습관을 만들어가는 루틴 챌린지 플랫폼" %>">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Routine Finders">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#4F46E5">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Open Graph / SNS 공유 -->
    <meta property="og:type" content="website">
    <meta property="og:title" content="<%= content_for(:title) || "Routine Finders" %>">
    <meta property="og:description" content="<%= content_for(:description) || "함께 습관을 만들어가는 루틴 챌린지 플랫폼" %>">
    <meta property="og:image" content="<%= content_for(:og_image) || "#{request.base_url}/og-image.png" %>">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:site_name" content="Routine Finders">

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= content_for(:title) || "Routine Finders" %>">
    <meta name="twitter:description" content="<%= content_for(:description) || "함께 습관을 만들어가는 루틴 챌린지 플랫폼" %>">
    <meta name="twitter:image" content="<%= content_for(:og_image) || "#{request.base_url}/og-image.png" %>">

    <%= yield :head %>

    <!-- PWA -->
    <link rel="manifest" href="/manifest.json">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="루파">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png">
    <meta name="theme-color" content="#7C4DFF">

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/icon.svg">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">


    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@100..900&display=swap" rel="stylesheet">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.9.2/dist/confetti.browser.min.js"></script>
    
    <!-- Kakao SDK -->
    <script src="https://t1.kakaocdn.net/kakao_js_sdk/2.7.0/kakao.min.js" crossorigin="anonymous"></script>
    <script>
      document.addEventListener('turbo:load', function() {
        if (typeof Kakao !== 'undefined' && !Kakao.isInitialized()) {
          Kakao.init('f959f8ada6c21d791ef5be7f4257e19e');
        }
      });
    </script>

    <style>
      /* Mobile App-like Experience Globals */
      * {
        -webkit-tap-highlight-color: transparent;
        -webkit-touch-callout: none;
      }
      
      body {
        overscroll-behavior-y: none; /* Prevent rubber-band effect on body if needed, or keep for native feel */
      }

      .active-scale {
        transition: transform 0.1s ease-out;
      }
      .active-scale:active {
        transform: scale(0.95);
      }

      /* Bottom Sheet Animation */
      @keyframes slide-up {
        from { transform: translateY(100%); }
        to { transform: translateY(0); }
      }
      .animate-slide-up {
        animation: slide-up 0.4s cubic-bezier(0.16, 1, 0.3, 1);
      }

      @keyframes slide-out-down {
        from { transform: translateY(0); opacity: 1; }
        to { transform: translateY(20px); opacity: 0; }
      }
      .animate-slide-out-down {
        animation: slide-out-down 0.4s cubic-bezier(0.16, 1, 0.3, 1) forwards;
      }
    </style>
  </head>

  <body class="min-h-screen bg-[#0C0B12]" 
        data-controller="modal inapp-browser toast" 
        data-modal-auto-open-value="<%= alert == '로그인이 필요합니다.' %>"
        style="font-family: 'Outfit', sans-serif;">
    
    <%= render 'shared/welcome_modal' %>
    

    <%# ORIGINAL WEB NAVIGATION REMOVED FOR PROTOTYPE CONSISTENCY %>

    <!-- Mobile Bottom Navigation -->
    <%= render "shared/bottom_nav" %>


    <!-- Unified Toast Notifications (JS Controlled) -->
    <meta name="flash-notice" content="<%= notice %>">
    <meta name="flash-alert" content="<%= alert %>">

    <main class="flex-1 w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 pb-32 lg:pb-8">
      <%= yield %>
    </main>


    <!-- Auth Modal -->
    <% unless current_user %>
      <%= render "shared/auth_modal" %>
    <% end %>

    <!-- Badge Award Modal -->
    <%= render "shared/badge_award" if logged_in? %>
    
    <!-- Hoisted Modals -->
    <%= yield :modal_container %>
  </body>
</html>
