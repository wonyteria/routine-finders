<div class="space-y-8">
  <div class="flex items-center justify-between">
    <div>
      <h3 class="text-xs font-black text-indigo-400 uppercase tracking-widest leading-none mb-2">Real-time Synergy</h3>
      <h2 class="text-2xl font-black text-white">시너지 피드</h2>
    </div>
    <div class="flex items-center gap-2 px-4 py-2 bg-indigo-500/10 rounded-full border border-indigo-500/20">
      <span class="w-2 h-2 bg-emerald-500 rounded-full animate-pulse"></span>
      <span class="text-[10px] font-black text-white uppercase tracking-widest"><%= @total_active_metes || 0 %>명 활동 중</span>
    </div>
  </div>

  <div class="space-y-4">
    <% if @recent_activities.present? %>
      <% @recent_activities.each do |activity| %>
        <div class="bg-white/5 border border-white/5 rounded-3xl p-5 flex items-start gap-4 hover:bg-white/[0.08] transition-all group">
          <div class="relative shrink-0">
             <img src="<%= activity.user.profile_image %>" class="w-12 h-12 rounded-2xl object-cover border border-white/10 group-hover:scale-105 transition-transform">
             <div class="absolute -bottom-1 -right-1 w-5 h-5 bg-indigo-600 rounded-full flex items-center justify-center text-[10px] border-2 border-slate-900">
               <%= activity.activity_type == 'routine_record' ? '✅' : '💪' %>
             </div>
          </div>
          <div class="flex-1 min-w-0 space-y-1">
            <div class="flex items-center justify-between">
              <span class="text-sm font-black text-white"><%= activity.user.nickname %></span>
              <span class="text-[10px] font-bold text-slate-500"><%= time_ago_in_words(activity.created_at) %>전</span>
            </div>
            <p class="text-[13px] font-medium text-slate-300 leading-relaxed font-pretendard">
              <%= activity.body %>
            </p>
            <div class="pt-2 flex items-center gap-3">
               <!-- 응원하기 버튼 제거됨 -->
            </div>
          </div>
        </div>
      <% end %>
    <% else %>
      <div class="py-20 text-center">
        <p class="text-slate-500 font-bold text-sm">아직 활동 소식이 없습니다.<br/>오늘의 첫 번째 기록을 남겨보세요!</p>
      </div>
    <% end %>
  </div>
</div>
