<div class="max-w-[1240px] mx-auto pb-40 pt-12 px-4" data-controller="tabs" data-tabs-active-class="bg-slate-900 text-white shadow-lg" data-tabs-inactive-class="text-slate-400 hover:bg-slate-50">
  <!-- Header -->
  <div class="flex flex-col md:flex-row md:items-end justify-between gap-8 mb-16">
    <div class="space-y-4">
      <div class="inline-flex items-center gap-2 px-4 py-2 bg-indigo-50 text-indigo-600 rounded-full font-black text-xs uppercase tracking-widest">
        Routine Achievement Ranking
      </div>
      <h2 class="text-4xl md:text-6xl font-[900] text-slate-900 tracking-tight leading-none italic">루틴 히어로즈 랭킹</h2>
    </div>

    <!-- Tab Filter -->
    <div class="bg-white border border-slate-100 p-2 rounded-[28px] flex items-center shadow-sm">
      <button data-tabs-target="tab" data-action="click->tabs#change" data-id="weekly" class="px-8 py-3.5 rounded-[22px] font-black text-sm transition-all flex items-center gap-2">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"/></svg>
        주간 랭킹
      </button>
      <button data-tabs-target="tab" data-action="click->tabs#change" data-id="hall" class="px-8 py-3.5 rounded-[22px] font-black text-sm transition-all flex items-center gap-2">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 15l-3 3m3-3l3 3m-3-3v-8m4.5-2.5a11.955 11.955 0 01-4.5 3.04a11.955 11.955 0 01-4.5-3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/></svg>
        명예의 전당
      </button>
    </div>
  </div>

  <div class="flex flex-col lg:flex-row gap-10">
    <!-- Ranking List Section -->
    <div class="flex-1 space-y-8">
      <!-- Search Bar -->
      <div class="relative group">
        <div class="absolute inset-y-0 left-8 flex items-center pointer-events-none">
          <svg class="w-5 h-5 text-slate-300 group-focus-within:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/></svg>
        </div>
        <input type="text" placeholder="루틴러의 닉네임을 검색하세요" class="w-full bg-slate-50 border-none rounded-[32px] py-6 pl-16 pr-8 text-lg font-bold text-slate-900 placeholder:text-slate-300 focus:ring-4 focus:ring-indigo-100 transition-all">
      </div>

      <!-- Ranking Items -->
      <div class="bg-white rounded-[56px] border border-slate-100 shadow-2xl p-6 space-y-4">
        <% @rankings.each_with_index do |host, index| %>
          <%= link_to user_profile_path(host), class: "block group" do %>
            <div class="flex items-center justify-between p-6 md:p-8 rounded-[40px] hover:bg-slate-50 transition-all border border-transparent hover:border-slate-100 relative overflow-hidden">
              <div class="flex items-center gap-6 md:gap-10">
                <!-- Rank -->
                <div class="w-12 flex items-center justify-center shrink-0">
                  <% if index < 3 %>
                    <span class="text-3xl"><%= index == 0 ? "👑" : (index == 1 ? "🥈" : "🥉") %></span>
                  <% else %>
                    <span class="text-2xl font-black text-slate-200"><%= index + 1 %></span>
                  <% end %>
                </div>

                <!-- Profile Info -->
                <div class="flex items-center gap-6">
                  <div class="relative w-16 h-16 md:w-20 md:h-20 shrink-0">
                    <img src="<%= host.profile_image || "https://api.dicebear.com/7.x/lorelei/svg?seed=#{host.nickname}&backgroundColor=b6e3f4,c0aede,d1d4f9,ffd5dc,ffdfbf" %>" class="w-full h-full object-cover rounded-[30px] border-2 border-white shadow-md" alt="">
                    <div class="absolute -bottom-1 -right-1 bg-indigo-600 text-white font-black text-[9px] px-2 py-1 rounded-lg">LV.<%= host.level %></div>
                  </div>
                  <div>
                    <div class="flex items-center gap-2">
                      <h4 class="text-xl font-black text-slate-900 leading-tight group-hover:text-indigo-600 transition-colors"><%= host.nickname %></h4>
                      <% if host.is_featured_host? %>
                        <span class="bg-indigo-600 text-white text-[8px] font-black px-2 py-1 rounded-md shadow-sm">PICK</span>
                      <% end %>
                    </div>
                    <p class="text-[10px] uppercase font-black tracking-widest text-slate-400 mt-1">POINTS: <%= number_with_delimiter(host.host_score.to_i) %></p>
                  </div>
                </div>
              </div>

              <!-- Stats Group -->
              <div class="hidden md:flex items-center gap-12 lg:gap-20 pr-10">
                <div class="text-center group-hover:scale-110 transition-transform">
                  <p class="text-[9px] font-black text-slate-400 uppercase tracking-widest mb-1.5">STREAK</p>
                  <p class="text-xl font-black text-orange-600 flex items-center justify-center gap-1 italic">
                    <span class="text-lg">🔥</span> <%= host.participations.map(&:current_streak).max || 0 %>
                  </p>
                </div>
                <div class="text-center group-hover:scale-110 transition-transform">
                  <p class="text-[9px] font-black text-slate-400 uppercase tracking-widest mb-1.5">COMPLETED</p>
                  <p class="text-2xl font-[900] text-indigo-600 italic"><%= host.host_completed_challenges %></p>
                </div>
                <div class="text-slate-300 group-hover:text-indigo-500 transition-colors">
                  <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 5l7 7-7 7"/></svg>
                </div>
              </div>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>

    <!-- Right Sidebar: Highlights -->
    <aside class="w-full lg:w-[380px] shrink-0 space-y-8">
      <!-- Weekly Challenge Info -->
      <div class="bg-slate-900 rounded-[56px] p-12 text-white space-y-10 shadow-2xl relative overflow-hidden group">
        <div class="absolute top-0 right-0 w-48 h-48 bg-indigo-500/20 rounded-full -mr-24 -mt-24 blur-3xl group-hover:scale-150 transition-transform duration-1000"></div>
        
        <div class="space-y-6 relative z-10">
          <span class="text-yellow-400 text-3xl">🏅</span>
          <h3 class="text-3xl font-[900] leading-tight tracking-tight italic">주간 챌린지</h3>
          <p class="text-white/60 font-bold leading-relaxed italic">
            "오늘의 성취가 내일의 당신을 만듭니다. 랭킹은 단순한 숫자가 아닌 당신의 증명입니다."
          </p>
        </div>

        <div class="bg-white/10 rounded-[32px] p-8 space-y-3 relative z-10 backdrop-blur-md border border-white/10">
          <p class="text-[10px] font-black text-indigo-300 uppercase tracking-widest">Next Reward In</p>
          <div class="flex items-center justify-between">
            <p class="text-3xl font-[900] italic">4일 12시간</p>
            <div class="text-indigo-400">
              <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
            </div>
          </div>
        </div>
      </div>

      <!-- Secret Box -->
      <div class="bg-white rounded-[56px] border border-slate-100 p-12 space-y-10 shadow-xl">
        <div class="flex items-center gap-3">
          <span class="text-2xl">🏆</span>
          <h4 class="text-lg font-black text-slate-900 leading-tight">상위 1%의 비결</h4>
        </div>
        <div class="space-y-8">
          <% [
            { label: "평균 기상 시간", value: "오전 6:12" },
            { label: "일일 평균 루틴", value: "8.4개" },
            { label: "주말 인증률", value: "94.2%" }
          ].each do |stat| %>
            <div class="flex items-center justify-between">
              <p class="text-sm font-bold text-slate-400"><%= stat[:label] %></p>
              <p class="font-black text-slate-900 tracking-tight"><%= stat[:value] %></p>
            </div>
          <% end %>
        </div>
      </div>
    </aside>
  </div>
</div>
