<div class="fixed inset-0 bg-[#0C0B12]/95 backdrop-blur-2xl z-0 overflow-hidden">
  <!-- Dynamic Aura Background -->
  <div class="absolute top-[20%] left-[-10%] w-[60%] aspect-square bg-indigo-500/10 blur-[120px] rounded-full animate-pulse-slow"></div>
  <div class="absolute bottom-[20%] right-[-10%] w-[60%] aspect-square bg-purple-500/10 blur-[120px] rounded-full animate-pulse-slow" style="animation-delay: 2s;"></div>
</div>

<div class="relative z-10 px-6 pt-12 pb-24 flex flex-col min-h-screen">
  <!-- Close Button -->
  <header class="flex justify-end mb-8">
    <%= link_to :back, class: "w-12 h-12 rounded-full bg-white/5 border border-white/10 flex items-center justify-center text-white active:scale-95 transition-all shadow-xl" do %>
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M6 18L18 6M6 6l12 12"/></svg>
    <% end %>
  </header>

  <!-- Title Section -->
  <div class="space-y-2 mb-12 text-center">
    <h2 class="text-3xl font-black text-white leading-tight">새로운 여정을<br/><span class="text-indigo-500">시작해보세요</span></h2>
    <p class="text-sm font-bold text-slate-500">성장하는 당신을 위한 완벽한 시작 도구함</p>
  </div>

  <!-- Launchpad Grid -->
  <div class="grid grid-cols-1 gap-4 flex-1">
    <!-- 1. Add Routine -->
    <%= link_to prototype_routine_builder_path, class: "group relative app-glass-card p-6 flex items-center gap-6 border-white/5 bg-white/[0.02] hover:bg-white/[0.05] transition-all active:scale-[0.98] overflow-hidden" do %>
      <div class="absolute inset-0 bg-gradient-to-r from-emerald-500/10 to-transparent opacity-0 group-hover:opacity-100 transition-opacity"></div>
      <div class="w-16 h-16 rounded-2xl bg-emerald-500/10 border border-emerald-500/20 flex items-center justify-center text-3xl shadow-lg shadow-emerald-500/10 group-hover:scale-110 transition-transform">⚡</div>
      <div class="text-left flex-1">
        <h3 class="text-lg font-black text-white mb-1">나만의 루틴 만들기</h3>
        <p class="text-[11px] font-bold text-slate-500 leading-relaxed uppercase tracking-wider">Add New Routine</p>
      </div>
      <div class="w-10 h-10 rounded-full flex items-center justify-center text-white/20 group-hover:text-emerald-500 transition-colors">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9 5l7 7-7 7"/></svg>
      </div>
    <% end %>

    <!-- 2. Create Challenge -->
    <%= link_to prototype_challenge_builder_path, class: "group relative app-glass-card p-6 flex items-center gap-6 border-white/5 bg-white/[0.02] hover:bg-white/[0.05] transition-all active:scale-[0.98] overflow-hidden" do %>
      <div class="absolute inset-0 bg-gradient-to-r from-indigo-500/10 to-transparent opacity-0 group-hover:opacity-100 transition-opacity"></div>
      <div class="w-16 h-16 rounded-2xl bg-indigo-500/10 border border-indigo-500/20 flex items-center justify-center text-3xl shadow-lg shadow-indigo-500/10 group-hover:scale-110 transition-transform">🏆</div>
      <div class="text-left flex-1">
        <h3 class="text-lg font-black text-white mb-1">함께하는 챌린지 개설</h3>
        <p class="text-[11px] font-bold text-slate-500 leading-relaxed uppercase tracking-wider">Start a Challenge</p>
      </div>
      <div class="w-10 h-10 rounded-full flex items-center justify-center text-white/20 group-hover:text-indigo-500 transition-colors">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9 5l7 7-7 7"/></svg>
      </div>
    <% end %>

    <!-- 3. Start Gathering -->
    <%= link_to prototype_gathering_builder_path, class: "group relative app-glass-card p-6 flex items-center gap-6 border-white/5 bg-white/[0.02] hover:bg-white/[0.05] transition-all active:scale-[0.98] overflow-hidden" do %>
      <div class="absolute inset-0 bg-gradient-to-r from-teal-500/10 to-transparent opacity-0 group-hover:opacity-100 transition-opacity"></div>
      <div class="w-16 h-16 rounded-2xl bg-teal-500/10 border border-teal-500/20 flex items-center justify-center text-3xl shadow-lg shadow-teal-500/10 group-hover:scale-110 transition-transform">☕️</div>
      <div class="text-left flex-1">
        <h3 class="text-lg font-black text-white mb-1">온/오프라인 모임 개설</h3>
        <p class="text-[11px] font-bold text-slate-500 leading-relaxed uppercase tracking-wider">Start a Meetup</p>
      </div>
      <div class="w-10 h-10 rounded-full flex items-center justify-center text-white/20 group-hover:text-teal-500 transition-colors">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9 5l7 7-7 7"/></svg>
      </div>
    <% end %>

    <!-- 4. Quick Record (Trigger Modal or Page) -->
    <button data-action="click->proto-ui#openModal" data-modal-id="record-sheet" class="group relative app-glass-card p-6 flex items-center gap-6 border-white/5 bg-white/[0.02] hover:bg-white/[0.05] transition-all active:scale-[0.98] overflow-hidden">
      <div class="absolute inset-0 bg-gradient-to-r from-rose-500/10 to-transparent opacity-0 group-hover:opacity-100 transition-opacity"></div>
      <div class="w-16 h-16 rounded-2xl bg-rose-500/10 border border-rose-500/20 flex items-center justify-center text-3xl shadow-lg shadow-rose-500/10 group-hover:scale-110 transition-transform">✍️</div>
      <div class="text-left flex-1">
        <h3 class="text-lg font-black text-white mb-1">오늘의 기록 남기기</h3>
        <p class="text-[11px] font-bold text-slate-500 leading-relaxed uppercase tracking-wider">Quick Note</p>
      </div>
      <div class="w-10 h-10 rounded-full flex items-center justify-center text-white/20 group-hover:text-rose-500 transition-colors">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9 5l7 7-7 7"/></svg>
      </div>
    </button>
  </div>

  <!-- Footer Info -->
  <div class="mt-8 text-center p-6 bg-white/[0.02] rounded-3xl border border-white/5">
     <p class="text-[10px] font-bold text-slate-600 leading-loose uppercase tracking-[0.2em]">All of your creations are shared with<br/><span class="text-white">RUFA CLUB MEMBER</span> by default.</p>
  </div>
</div>

<style>
  @keyframes pulse-slow {
    0%, 100% { opacity: 0.1; transform: scale(1); }
    50% { opacity: 0.15; transform: scale(1.1); }
  }
  .animate-pulse-slow {
    animation: pulse-slow 10s ease-in-out infinite;
  }
</style>
