<div class="space-y-12">
  
  <% unless current_user.is_rufa_club_member? %>
    <!-- Premium Promotion View for Non-members -->
    <section class="relative overflow-hidden bg-slate-900 rounded-[64px] p-12 lg:p-24 text-white shadow-2xl">
      <div class="absolute inset-0 bg-gradient-to-br from-indigo-600/30 via-slate-900 to-purple-900/30 z-0"></div>
      <div class="absolute -right-40 -top-40 w-[600px] h-[600px] bg-indigo-500/10 rounded-full blur-[120px]"></div>
      
      <div class="relative z-10 max-w-4xl mx-auto text-center space-y-12">
        <div class="inline-flex items-center gap-3 bg-white/5 backdrop-blur-xl px-6 py-3 rounded-full border border-white/10">
          <span class="w-2.5 h-2.5 bg-amber-400 rounded-full animate-pulse shadow-[0_0_15px_rgba(251,191,36,0.8)]"></span>
          <span class="text-xs font-black uppercase tracking-[0.3em] text-indigo-100 italic">Routine Finders Premium</span>
        </div>

        <div class="space-y-6">
          <h2 class="text-5xl md:text-8xl font-black leading-[1.05] tracking-tighter">
            단순한 기록을 넘어,<br/>
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-amber-200 via-yellow-400 to-orange-400">당신의 정체성을 설계하다</span>
          </h2>
          <p class="text-xl md:text-2xl text-slate-400 font-medium max-w-2xl mx-auto leading-relaxed">
            루파 클럽은 성실함의 가치를 아는 상위 1%를 위한<br/>프리미엄 루틴 성장 시스템입니다.
          </p>
        </div>

        <!-- Benefits Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 text-left pt-12">
          <% [
            ["📊", "성장 리포트", "매월 객관적인 지표로 분석하는 루파 인텔리전스"],
            ["🤝", "오프라인 모임", "서울·경기 지역 정기 오프라인 성장 네트워킹"],
            ["🔱", "VIP 인증", "디지털 VIP 카드와 상위 0.1%를 위한 특별 혜택"],
            ["🔥", "시너지 피드", "검증된 멤버들과 나누는 압도적인 성취 에너지"]
          ].each do |icon, title, desc| %>
            <div class="bg-white/5 backdrop-blur-md p-8 rounded-[40px] border border-white/5 hover:border-white/20 transition-all group">
              <div class="text-4xl mb-6 group-hover:scale-125 transition-transform duration-500"><%= icon %></div>
              <h4 class="text-lg font-black text-white mb-2"><%= title %></h4>
              <p class="text-sm text-slate-500 font-medium leading-relaxed"><%= desc %></p>
            </div>
          <% end %>
        </div>

        <div class="pt-12 flex flex-col items-center gap-8">
          <% if @official_club %>
            <%= link_to routine_club_path(@official_club), class: "group relative px-16 py-8 bg-white text-slate-900 rounded-[32px] font-black text-xl hover:bg-amber-400 transition-all shadow-[0_20px_60px_rgba(255,255,255,0.1)] overflow-hidden" do %>
              <span class="relative z-10">지금 루파 클럽 참여하기</span>
              <div class="absolute inset-0 bg-gradient-to-r from-amber-300 to-orange-400 opacity-0 group-hover:opacity-100 transition-opacity"></div>
            <% end %>
          <% end %>
          <div class="flex items-center gap-3 text-slate-500">
            <span class="text-sm font-bold">월 5,000원으로 누리는 모든 권한</span>
            <div class="w-1 h-1 bg-slate-700 rounded-full"></div>
            <span class="text-sm font-bold">중도 해지 및 환불 불가</span>
          </div>
        </div>
      </div>
    </section>

  <% else %>
    <!-- Premium Member Dashboard with Sub-navigation -->
    <div class="flex flex-col lg:flex-row gap-12" id="rufa-premium-board">
      
      <!-- Sticky Sidebar Navigation -->
      <aside class="lg:w-72 shrink-0">
        <div class="sticky top-12 space-y-8">
          <div class="px-6 py-4 bg-slate-900 rounded-[32px] text-white shadow-xl shadow-indigo-100 flex items-center gap-4 group cursor-pointer" onclick="toggleVipCard()">
             <div class="w-10 h-10 bg-white/10 rounded-xl flex items-center justify-center text-xl">🔱</div>
             <div>
               <p class="text-[9px] font-black text-indigo-200 uppercase tracking-widest">Rufa VIP</p>
               <p class="text-sm font-black"><%= current_user.nickname %></p>
             </div>
          </div>

          <nav class="space-y-2">
            <% [
              ["dashboard", "🏠", "대시보드"],
              ["architecture", "📐", "목표 설계"],
              ["synergy", "✨", "루파 시너지"],
              ["insight", "💎", "루파 인사이트"]
            ].each do |id, icon, label| %>
              <button onclick="switchRufaTab('<%= id %>')" id="nav-<%= id %>" class="rufa-nav-btn w-full flex items-center gap-4 px-8 py-5 rounded-[28px] font-black text-sm transition-all <%= id == 'dashboard' ? 'bg-indigo-600 text-white shadow-lg' : 'text-slate-400 hover:bg-white hover:text-slate-900' %>">
                <span class="text-xl"><%= icon %></span>
                <%= label %>
              </button>
            <% end %>
          </nav>

          <!-- Quick Stats Sidebar -->
          <div class="bg-white rounded-[32px] p-8 border border-slate-100 space-y-6 shadow-sm">
            <div class="space-y-1">
              <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest">Growth Identity</p>
              <h4 class="text-base font-black text-slate-900"><%= current_user.routine_club_members.active.first&.identity_title %></h4>
            </div>
            <%= link_to routine_club_reports_path, class: "flex items-center justify-between p-4 bg-indigo-50 text-indigo-600 rounded-2xl hover:bg-indigo-600 hover:text-white transition-all group" do %>
               <span class="text-xs font-black">월간 성실함 리포트</span>
               <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M14 5l7 7-7 7m-7 7H3"/></svg>
            <% end %>
          </div>
        </div>
      </aside>

      <!-- Sub-tab Content Area -->
      <main class="flex-1 space-y-12">
        
        <!-- 1. Dashboard Tab -->
        <div id="tab-dashboard" class="rufa-content-tab space-y-12">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
             <!-- Status Card: Log Rate -->
             <div class="bg-white rounded-[48px] p-10 border border-slate-100 shadow-sm space-y-8 relative overflow-hidden group">
               <div class="absolute -right-4 -top-4 w-24 h-24 bg-indigo-50 rounded-full blur-2xl group-hover:scale-110 transition-transform"></div>
               <div class="relative z-10 flex justify-between items-start">
                 <div class="space-y-1">
                   <p class="text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Log Rate</p>
                   <h3 class="text-3xl font-black text-slate-900">당신의 성실함</h3>
                 </div>
                 <div class="text-right">
                   <p class="text-4xl font-black text-indigo-600 tracking-tighter"><%= @current_log_rate.to_i %><span class="text-lg text-slate-300 ml-1">%</span></p>
                 </div>
               </div>
               <div class="relative z-10 w-full h-8 bg-slate-50 rounded-2xl border border-slate-100 p-1.5 overflow-hidden">
                 <div class="h-full bg-indigo-600 rounded-xl transition-all duration-1000 shadow-lg shadow-indigo-100" style="width: <%= @current_log_rate %>%"></div>
               </div>
               <p class="text-[11px] font-bold text-slate-400 px-1">최근 30일간의 루틴 기록률입니다. 70% 이상 유지를 권장합니다.</p>
             </div>

             <!-- Status Card: Achievement -->
             <div class="bg-slate-900 rounded-[48px] p-10 shadow-2xl space-y-8 relative overflow-hidden group">
                <div class="absolute -right-4 -top-4 w-24 h-24 bg-indigo-600/20 rounded-full blur-2xl group-hover:scale-110 transition-transform"></div>
                <div class="relative z-10 flex justify-between items-start">
                  <div class="space-y-1 text-white">
                    <p class="text-[10px] font-black text-indigo-300 uppercase tracking-[0.2em]">Achievement</p>
                    <h3 class="text-3xl font-black">실천의 결과</h3>
                  </div>
                  <div class="text-right">
                    <p class="text-4xl font-black text-amber-400 tracking-tighter"><%= @current_achievement_rate.to_i %><span class="text-lg text-indigo-300/50 ml-1">%</span></p>
                  </div>
                </div>
                <div class="relative z-10 w-full h-8 bg-white/5 rounded-2xl border border-white/10 p-1.5 overflow-hidden">
                  <div class="h-full bg-amber-400 rounded-xl transition-all duration-1000 shadow-lg shadow-amber-900/40" style="width: <%= @current_achievement_rate %>%"></div>
                </div>
                <!-- Sparkline Trend -->
                <div class="pt-4 flex items-center justify-between border-t border-white/5">
                   <p class="text-[9px] font-black text-slate-500 uppercase tracking-widest">7-Day Trend</p>
                   <svg viewBox="0 0 140 30" class="w-24 h-6">
                     <% 
                       pts = @achievement_trend.each_with_index.map { |v, i| "#{i * 20 + 10},#{30 - (v * 0.25)}" }.join(" ")
                     %>
                     <polyline points="<%= pts %>" fill="none" stroke="#fbbf24" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" />
                   </svg>
                </div>
             </div>

             <!-- Status Card: Ranking Benchmark -->
             <div class="col-span-1 md:col-span-2 bg-indigo-50/50 rounded-[48px] p-10 border border-indigo-100 shadow-sm space-y-8 relative overflow-hidden group">
                <div class="flex flex-col md:flex-row md:items-center justify-between gap-8">
                  <div class="space-y-1">
                    <p class="text-[10px] font-black text-indigo-400 uppercase tracking-widest">Global Benchmark</p>
                    <h3 class="text-2xl font-black text-slate-900">당신은 상위권에 얼마나 가까운가요?</h3>
                  </div>
                  <div class="flex items-center gap-8">
                    <div class="text-center">
                      <p class="text-[10px] font-bold text-slate-400 uppercase mb-1">Top 10 Avg</p>
                      <p class="text-2xl font-black text-slate-900"><%= @top_avg_score %></p>
                    </div>
                    <div class="w-px h-8 bg-slate-200"></div>
                    <div class="text-center">
                      <p class="text-[10px] font-bold text-indigo-400 uppercase mb-1">My Score</p>
                      <p class="text-2xl font-black text-indigo-600"><%= @my_score %></p>
                    </div>
                  </div>
                </div>
                
                <div class="space-y-4">
                  <div class="relative w-full h-4 bg-white rounded-full overflow-hidden border border-indigo-100 p-0.5">
                    <!-- Top Avg Indicator -->
                    <div class="absolute top-0 bottom-0 w-1 bg-slate-300 z-10" style="left: <%= @top_avg_score %>%"></div>
                    <!-- My Progress -->
                    <div class="h-full bg-indigo-600 rounded-full transition-all duration-1000 shadow-lg" style="width: <%= @my_score %>%"></div>
                  </div>
                  <div class="flex justify-between px-1">
                    <p class="text-[10px] font-bold text-slate-400">0 PTS</p>
                    <p class="text-[10px] font-black text-indigo-600">성장 진행 중</p>
                    <p class="text-[10px] font-bold text-slate-400">100 PTS</p>
                  </div>
                </div>
             </div>
          </div>

          <!-- Growth Radar: Visualizing Category Distribution -->
          <div class="bg-white rounded-[48px] p-12 border border-slate-100 shadow-sm space-y-12">
            <div class="flex items-center justify-between">
              <div class="space-y-1">
                <h3 class="text-2xl font-black text-slate-900 tracking-tight">성장 레이더</h3>
                <p class="text-xs font-bold text-slate-400">카테고리별 실천 밸런스를 확인해보세요</p>
              </div>
              <div class="flex gap-4">
                <% ["HEALTH", "LIFE", "MIND", "STUDY", "HOBBY"].each do |cat| %>
                  <div class="flex items-center gap-1.5">
                    <div class="w-1.5 h-1.5 rounded-full <%= case cat when 'HEALTH' then 'bg-rose-500' when 'LIFE' then 'bg-emerald-500' when 'MIND' then 'bg-indigo-500' when 'STUDY' then 'bg-purple-500' else 'bg-orange-500' end %>"></div>
                    <span class="text-[9px] font-black text-slate-400 tracking-widest uppercase"><%= cat %></span>
                  </div>
                <% end %>
              </div>
            </div>

            <div class="flex flex-col md:flex-row items-center gap-16 py-8">
              <div class="shrink-0 w-80 h-80 relative flex items-center justify-center">
                <svg viewBox="0 0 200 200" class="w-full h-full transform -rotate-18">
                  <!-- Radar background circles -->
                  <% [40, 70, 100].each do |r| %>
                    <circle cx="100" cy="100" r="<%= r %>" fill="none" stroke="#f1f5f9" stroke-width="1" />
                  <% end %>
                  <!-- Radar axes -->
                  <% 5.times do |i| %>
                    <% angle = (i * 72 * Math::PI / 180) %>
                    <line x1="100" y1="100" x2="<%= 100 + 100 * Math.sin(angle) %>" y2="<%= 100 - 100 * Math.cos(angle) %>" stroke="#f1f5f9" stroke-width="1" />
                  <% end %>
                  
                  <!-- Growth Shape -->
                  <% 
                    pts = []
                    cats = ["HEALTH", "LIFE", "MIND", "STUDY", "HOBBY"]
                    max_val = @category_stats.values.max.to_f
                    max_val = 1.0 if max_val == 0
                    cats.each_with_index do |cat, i|
                      val = @category_stats[cat]
                      scaled = (val / max_val) * 100
                      angle = (i * 72 * Math::PI / 180)
                      pts << "#{100 + scaled * Math.sin(angle)},#{100 - scaled * Math.cos(angle)}"
                    end
                  %>
                  <polygon points="<%= pts.join(' ') %>" fill="rgba(79, 70, 229, 0.15)" stroke="#4f46e5" stroke-width="3" stroke-linejoin="round" />
                  <% pts.each do |p| %>
                    <circle cx="<%= p.split(',').first %>" cy="<%= p.split(',').last %>" r="4" fill="#4f46e5" />
                  <% end %>
                </svg>
              </div>

              <div class="flex-1 space-y-8">
                <h4 class="text-xl font-black text-slate-900 leading-tight">
                  <%= case @category_stats.max_by{|k,v| v}&.first
                      when "HEALTH" then "당신의 아침은 건강으로 탄탄해지고 있습니다."
                      when "LIFE" then "생활 전반의 균형을 잘 잡아가고 계시네요."
                      when "MIND" then "지치지 않는 멘탈을 가진 강력한 마인드 파인더입니다."
                      when "STUDY" then "끊임없이 학습하며 성장하는 학구파적인 면모가 보입니다."
                      else "생산적인 하루를 통해 꿈에 실질적으로 접근하고 있습니다."
                      end %>
                </h4>
                <div class="grid grid-cols-2 gap-6">
                  <% ["HEALTH", "LIFE", "MIND", "STUDY", "HOBBY"].each do |cat| %>
                    <div class="space-y-2">
                       <div class="flex justify-between items-baseline">
                         <span class="text-[10px] font-black text-slate-400 tracking-widest uppercase"><%= cat %></span>
                         <span class="text-sm font-black text-slate-900"><%= @category_stats[cat] %>회</span>
                       </div>
                       <div class="h-1.5 bg-slate-50 rounded-full overflow-hidden">
                         <div class="h-full <%= case cat when 'HEALTH' then 'bg-rose-400' when 'LIFE' then 'bg-emerald-400' when 'MIND' then 'bg-indigo-400' when 'STUDY' then 'bg-purple-400' else 'bg-orange-400' end %>" style="width: <%= (@category_stats[cat].to_f / max_val * 100).to_i %>%"></div>
                       </div>
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
          </div>

          <!-- Today's Check-in (Prominent on dashboard) -->
          <div class="bg-white rounded-[48px] p-12 border border-slate-100 shadow-sm space-y-10">
             <div class="flex items-center justify-between">
               <h3 class="text-2xl font-black text-slate-900 tracking-tight">오늘의 루틴 체크인</h3>
               <span class="text-[10px] font-black text-indigo-500 uppercase tracking-widest"><%= Date.current.strftime("%A, %B %d") %></span>
             </div>
             <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
               <% todays_routines = @personal_routines.select { |r| (r.days || []).include?(Date.current.wday.to_s) } %>
               <% if todays_routines.any? %>
                 <% todays_routines.each do |routine| %>
                   <%= button_to toggle_personal_routine_path(routine), method: :post, class: "flex items-center justify-between p-6 rounded-3xl border-2 transition-all hover:scale-[1.02] #{routine.completed_today? ? 'bg-indigo-50 border-indigo-200 ring-4 ring-indigo-50' : 'bg-slate-50 border-transparent hover:border-indigo-100 group'}" do %>
                      <div class="flex items-center gap-4">
                        <span class="text-2xl transform group-hover:scale-125 transition-transform"><%= routine.icon %></span>
                        <div class="text-left">
                          <p class="text-[15px] font-black text-slate-900 <%= 'line-through text-slate-400' if routine.completed_today? %>"><%= routine.title %></p>
                          <p class="text-[10px] font-bold text-slate-400"><%= routine.category %></p>
                        </div>
                      </div>
                      <% if routine.completed_today? %>
                        <div class="w-10 h-10 bg-indigo-600 rounded-2xl flex items-center justify-center text-white">
                          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"/></svg>
                        </div>
                      <% else %>
                        <div class="w-10 h-10 bg-white rounded-2xl flex items-center justify-center text-slate-200 border border-slate-100 group-hover:bg-indigo-100 group-hover:text-indigo-600 transition-all">
                          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"/></svg>
                        </div>
                      <% end %>
                   <% end %>
                 <% end %>
               <% else %>
                 <div class="col-span-2 py-16 text-center bg-slate-50 rounded-[40px] border border-dashed border-slate-200">
                    <p class="text-sm font-bold text-slate-400 mb-4">오늘 설정된 전용 루틴이 없습니다.</p>
                    <%= link_to "루틴 설정하러 가기", personal_routines_path(tab: "free"), class: "text-xs font-black text-indigo-600 hover:underline" %>
                 </div>
               <% end %>
             </div>
          </div>
        </div>

        <!-- 2. Architecture Tab (Goals) -->
        <div id="tab-architecture" class="rufa-content-tab hidden space-y-12">
           <div class="bg-white rounded-[48px] p-12 lg:p-20 border border-slate-100 shadow-sm space-y-12">
            <div class="flex flex-col md:flex-row md:items-center justify-between gap-6">
              <div class="space-y-1">
                <h3 class="text-3xl font-black text-slate-900 tracking-tight">Rufa Goal Architecture</h3>
                <p class="text-base text-slate-500 font-medium italic">"원대한 꿈을 쪼개어 오늘의 루틴으로 만드세요."</p>
              </div>
              <div class="flex -space-x-3">
                 <div class="w-12 h-12 bg-purple-500 rounded-2xl border-4 border-white shadow-sm flex items-center justify-center text-white font-black text-xs">L</div>
                 <div class="w-12 h-12 bg-indigo-500 rounded-2xl border-4 border-white shadow-sm flex items-center justify-center text-white font-black text-xs">M</div>
                 <div class="w-12 h-12 bg-emerald-500 rounded-2xl border-4 border-white shadow-sm flex items-center justify-center text-white font-black text-xs">S</div>
              </div>
            </div>

            <%= form_with url: update_goals_personal_routines_path, method: :post, class: "space-y-10" do %>
              <div class="space-y-12">
                <% [[:long_term, "Long Term (1년~)", "bg-purple-500", @long_term_goal, "내년에 나는 어떤 사람이 되어있을까요?"], 
                    [:mid_term, "Mid Term (3~6개월)", "bg-indigo-500", @mid_term_goal, "어떤 중간 성취를 통해 증명하시겠습니까?"], 
                    [:short_term, "Short Term (1개월)", "bg-emerald-500", @short_term_goal, "이번 달 가장 집중해야 할 한 가지는?"]].each do |name, label, color, val, ph| %>
                  <div class="space-y-4">
                    <div class="flex items-center gap-3">
                      <div class="w-6 h-6 rounded-lg <%= color %> flex items-center justify-center text-[10px] text-white font-black"><%= label[0] %></div>
                      <label class="text-xs font-black text-slate-900 uppercase tracking-widest"><%= label %></label>
                    </div>
                    <input type="text" name="<%= name %>" value="<%= val %>" placeholder="<%= ph %>" class="w-full px-10 py-8 bg-slate-50 border-none rounded-[32px] font-black text-2xl text-slate-700 placeholder-slate-200 focus:ring-8 focus:bg-white ring-indigo-50 transition-all">
                  </div>
                <% end %>
              </div>
              <div class="pt-10">
                <button type="submit" class="w-full py-8 bg-slate-900 text-white rounded-[32px] font-black text-xl hover:bg-indigo-600 transition-all shadow-2xl flex items-center justify-center gap-4 group">
                  정체성 업데이트하기
                  <svg class="w-6 h-6 group-hover:translate-x-2 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M14 5l7 7m0 0l-7 7m7-7H3"/></svg>
                </button>
              </div>
            <% end %>
          </div>
        </div>

        <!-- 3. Synergy Tab (Feed) -->
        <div id="tab-synergy" class="rufa-content-tab hidden space-y-12">
           <div class="bg-white rounded-[48px] p-12 border border-slate-100 shadow-sm space-y-12">
              <div class="flex items-center justify-between">
                <div class="space-y-1">
                  <h3 class="text-2xl font-black text-slate-900 tracking-tight">루파 시너지 피드</h3>
                  <p class="text-sm font-medium text-slate-400">성실함의 파동이 당신에게 닿길 바랍니다.</p>
                </div>
                <div class="flex items-center gap-4 bg-slate-50 px-6 py-3 rounded-2xl">
                   <div class="flex -space-x-3">
                     <% @rufa_rankings.take(3).each do |rank| %>
                       <img src="<%= rank[:user].profile_image %>" class="w-8 h-8 rounded-full border-2 border-white" />
                     <% end %>
                   </div>
                   <span class="text-xs font-black text-slate-900"><%= @rufa_rankings.count %>명의 멤버 정진 중</span>
                </div>
              </div>

              <div class="space-y-8">
                <% if @rufa_activities.any? %>
                  <% @rufa_activities.each do |activity| %>
                    <div class="flex gap-8 group">
                       <div class="shrink-0">
                         <img src="<%= activity.user.profile_image %>" class="w-16 h-16 rounded-[24px] shadow-sm border border-slate-50" />
                       </div>
                       <div class="flex-1 space-y-4 pb-12 group-last:pb-0 border-l border-slate-100 pl-8 relative">
                          <div class="absolute -left-[5px] top-6 w-2.5 h-2.5 bg-slate-200 rounded-full group-hover:bg-indigo-500 transition-colors"></div>
                          <div class="bg-slate-50/50 p-8 rounded-[40px] border border-slate-100/50 group-hover:bg-white group-hover:shadow-xl transition-all">
                            <div class="flex justify-between items-center mb-4">
                              <span class="text-sm font-black text-slate-900"><%= activity.user.nickname %></span>
                              <span class="text-[10px] font-black text-slate-300 uppercase"><%= time_ago_in_words(activity.created_at) %> ago</span>
                            </div>
                            <p class="text-lg font-bold text-slate-600 leading-relaxed"><%= activity.body %></p>
                            
                            <div class="mt-6 flex items-center justify-between">
                               <%= button_to rufa_activity_claps_path(activity), method: :post, class: "flex items-center gap-3 px-6 py-3 bg-white hover:bg-amber-100 rounded-full transition-all border border-slate-100 group/clap" do %>
                                 <span class="text-2xl group-hover/clap:scale-150 transition-transform duration-500"><%= activity.claps_count.to_i > 0 ? "👏" : "🙌" %></span>
                                 <span class="text-sm font-black <%= activity.claps_count.to_i > 0 ? 'text-amber-600' : 'text-slate-400' %>"><%= activity.claps_count.to_i %></span>
                               <% end %>
                               <div class="flex gap-1">
                                 <% activity.claps.take(5).each do |clap| %>
                                   <div class="w-2 h-2 bg-amber-400 rounded-full"></div>
                                 <% end %>
                               </div>
                            </div>
                          </div>
                       </div>
                    </div>
                  <% end %>
                <% else %>
                  <div class="py-32 text-center text-slate-200 font-black text-3xl">첫 번째 파장을 기다리고 있습니다.</div>
                <% end %>
              </div>
           </div>
        </div>

        <!-- 4. Insight Tab (Templates) -->
        <div id="tab-insight" class="rufa-content-tab hidden space-y-12">
            <div class="flex flex-col md:flex-row md:items-center justify-between gap-6 px-4">
               <div class="space-y-1">
                 <h3 class="text-3xl font-black text-slate-900 tracking-tight">루파 인사이트</h3>
                 <p class="text-base text-slate-500 font-medium">검증된 최고 수준의 루틴 템플릿 라이브러리</p>
               </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-10">
               <% @routine_templates.each do |template| %>
                 <div class="group bg-white rounded-[56px] p-12 border border-slate-100 shadow-sm hover:shadow-2xl hover:-translate-y-2 transition-all relative overflow-hidden">
                    <div class="absolute -right-10 -top-10 w-48 h-48 bg-indigo-50 rounded-full blur-[60px] opacity-0 group-hover:opacity-100 transition-opacity duration-1000"></div>
                    
                    <div class="relative z-10 space-y-10 text-center lg:text-left">
                       <div class="flex flex-col lg:flex-row items-center justify-between gap-6">
                          <div class="w-20 h-20 bg-slate-50 rounded-[28px] flex items-center justify-center text-5xl transform group-hover:rotate-12 transition-transform duration-700">
                            <%= template.icon %>
                          </div>
                          <div class="px-5 py-2 bg-indigo-600 text-white text-[10px] font-black rounded-full uppercase tracking-widest"><%= template.category %></div>
                       </div>

                       <div class="space-y-3">
                         <p class="text-[10px] font-black text-indigo-500 uppercase tracking-[0.3em]">Mastered by <%= template.author_name %></p>
                         <h4 class="text-3xl font-black text-slate-900 leading-tight"><%= template.title %></h4>
                         <p class="text-base font-medium text-slate-500 leading-relaxed max-w-sm"><%= template.description %></p>
                       </div>

                       <div class="pt-6">
                         <%= button_to apply_routine_template_path(template), method: :post, class: "w-full py-6 bg-slate-900 text-white rounded-[24px] font-black text-sm hover:bg-indigo-600 transition-all flex items-center justify-center gap-4 shadow-xl" do %>
                           <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="4" d="M12 4v16m8-8H4"/></svg>
                           내 루틴으로 복제하기
                         <% end %>
                       </div>
                    </div>
                 </div>
               <% end %>
            </div>
        </div>

      </main>
    </div>
  <% end %>
</div>

<!-- Digital VIP Card Modal (Kept same logic, updated style slightly) -->
<div id="vip-card-modal" class="fixed inset-0 z-[110] hidden items-center justify-center p-6 text-white">
  <div class="absolute inset-0 bg-slate-900/98 backdrop-blur-3xl" onclick="toggleVipCard()"></div>
  <div class="relative z-20 w-full max-w-md bg-gradient-to-br from-indigo-700 via-slate-900 to-black rounded-[72px] p-16 border border-white/10 shadow-[0_0_100px_rgba(79,70,229,0.4)] space-y-16 transform scale-90 opacity-0 transition-all duration-700" id="vip-card-content">
     <div class="flex justify-between items-center">
        <div class="space-y-4">
          <div class="px-4 py-1.5 bg-amber-400 text-slate-900 rounded-full w-fit">
            <p class="text-[10px] font-black tracking-[0.4em] uppercase">Rufa Official</p>
          </div>
          <h2 class="text-6xl font-black text-white italic tracking-tighter">RUFA VIP</h2>
        </div>
        <div class="w-20 h-20 bg-white rounded-3xl flex items-center justify-center text-slate-900 text-4xl shadow-glow shadow-indigo-500/50">🔱</div>
     </div>

     <div class="space-y-12">
        <div class="flex items-center gap-10">
           <img src="<%= current_user.profile_image %>" class="w-32 h-32 rounded-[48px] border-4 border-white/20 shadow-2xl" />
           <div class="space-y-3">
             <p class="text-xs font-black text-indigo-300/50 uppercase tracking-[0.3em]">Platinum Member</p>
             <h3 class="text-4xl font-black text-white tracking-tight"><%= current_user.nickname %></h3>
           </div>
        </div>

        <div class="grid grid-cols-2 gap-12 border-t border-white/5 pt-12">
           <div class="space-y-2">
             <p class="text-[10px] font-black text-white/30 uppercase tracking-[0.2em]">Current Identity</p>
             <p class="text-lg font-black text-white"><%= current_user.routine_club_members.active.first&.identity_title %></p>
           </div>
           <div class="space-y-2">
             <p class="text-[10px] font-black text-white/30 uppercase tracking-[0.2em]">Authenticated</p>
             <p class="text-lg font-black text-amber-400"><%= current_user.routine_club_members.active.first&.joined_at&.strftime("%Y.%m.%d") %></p>
           </div>
        </div>
     </div>

     <div class="pt-12 flex justify-between items-end border-t border-white/5">
        <div class="text-[11px] font-bold text-slate-500 uppercase leading-relaxed text-left max-w-[200px]">
          Authenticated and issued by Routine Finders Headquarters. This pass grants exclusive offline benefits in 2026.
        </div>
        <div class="w-28 h-28 bg-white p-4 rounded-[40px] select-none shadow-2xl">
           <img src="https://api.qrserver.com/v1/create-qr-code/?size=200x200&data=RUFA_VIP_<%= current_user.id %>" class="w-full h-full grayscale" />
        </div>
     </div>
  </div>
</div>

<script>
  function switchRufaTab(id) {
    // Hide all tabs
    document.querySelectorAll('.rufa-content-tab').forEach(tab => tab.classList.add('hidden'));
    // Show selected tab
    document.getElementById('tab-' + id).classList.remove('hidden');
    
    // Update navigation styles
    document.querySelectorAll('.rufa-nav-btn').forEach(btn => {
      btn.classList.remove('bg-indigo-600', 'text-white', 'shadow-lg');
      btn.classList.add('text-slate-400', 'hover:bg-white', 'hover:text-slate-900');
    });
    const activeBtn = document.getElementById('nav-' + id);
    activeBtn.classList.remove('text-slate-400', 'hover:bg-white', 'hover:text-slate-900');
    activeBtn.classList.add('bg-indigo-600', 'text-white', 'shadow-lg');

    // Scroll to top of content area on mobile
    if (window.innerWidth < 1024) {
      window.scrollTo({ top: document.getElementById('rufa-premium-board').offsetTop - 20, behavior: 'smooth' });
    }
  }

  function toggleVipCard() {
    const modal = document.getElementById('vip-card-modal');
    const content = document.getElementById('vip-card-content');
    
    if (modal.classList.contains('hidden')) {
      modal.classList.remove('hidden');
      modal.classList.add('flex');
      setTimeout(() => {
        content.classList.remove('scale-90', 'opacity-0');
        content.classList.add('scale-100', 'opacity-100');
      }, 50);
    } else {
      content.classList.remove('scale-100', 'opacity-100');
      content.classList.add('scale-90', 'opacity-0');
      setTimeout(() => {
        modal.classList.add('hidden');
        modal.classList.remove('flex');
      }, 600);
    }
  }
</script>
