<div class="space-y-12">
  
  <% if !((@my_membership.present? && @my_membership.status_active?) || current_user.admin?) %>
    <!-- Premium Promotion View for Non-members (Strictly for Official Club) -->
    <% 
      is_recruiting = @official_club&.recruitment_open?
      next_quarter_start = Date.current.beginning_of_quarter + 3.months
      recruitment_start = next_quarter_start - 14.days
    %>

    <% unless is_recruiting %>
      <!-- Recruitment Notice Banner -->
      <div class="mb-12 bg-white rounded-[40px] p-8 lg:p-12 border border-slate-200 shadow-xl shadow-slate-200/50 flex flex-col lg:flex-row items-center justify-between gap-6 relative overflow-hidden group">
        <div class="absolute right-0 top-0 w-64 h-64 bg-indigo-50 rounded-full -mr-32 -mt-32 blur-3xl group-hover:bg-indigo-100 transition-colors"></div>
        
        <div class="relative z-10 flex flex-col lg:flex-row items-center gap-8 text-center lg:text-left">
          <div class="w-20 h-20 bg-slate-900 rounded-[28px] flex items-center justify-center text-4xl shadow-2xl">📢</div>
          <div class="space-y-2">
            <div class="inline-flex items-center gap-2 px-3 py-1 bg-indigo-50 text-indigo-600 rounded-full text-[10px] font-black uppercase tracking-widest mb-2">Notice</div>
            <h3 class="text-2xl lg:text-3xl font-black text-slate-900 tracking-tight">지금은 정기 모집 기간이 아닙니다.</h3>
            <p class="text-xs lg:text-base text-slate-500 font-bold leading-relaxed break-keep">
              루파 클럽은 최상의 시너지를 위해 분기별 기수제로 운영됩니다.
              <span class="block lg:inline">다음 모집은 <span class="text-indigo-600 font-black"><%= recruitment_start.strftime("%m월 %d일") %></span>에 시작될 예정입니다.</span>
            </p>
          </div>
        </div>

        <% if @official_club %>
          <%= link_to routine_club_path(@official_club, anchor: "join-form"), class: "relative z-10 shrink-0 px-6 lg:px-8 py-3 lg:py-4 bg-gradient-to-r from-orange-500 to-pink-500 hover:from-orange-600 hover:to-pink-600 text-white rounded-2xl font-black text-sm lg:text-base transition-all shadow-xl hover:shadow-2xl active:scale-95 whitespace-nowrap flex items-center gap-2" do %>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
            </svg>
            기존 멤버 참여
          <% end %>
        <% end %>

      </div>
    <% end %>

    <section class="relative overflow-hidden bg-slate-900 rounded-[48px] lg:rounded-[64px] px-6 py-16 lg:p-24 text-white shadow-2xl" style="background-color: #0f172a !important; color: white !important;">

      <!-- Decorative backgrounds commented out for safety -->
      <!-- <div class="absolute inset-0 bg-gradient-to-br from-indigo-600/30 via-slate-900 to-purple-900/30 z-0"></div> -->
      <!-- <div class="absolute -right-40 -top-40 w-[600px] h-[600px] bg-indigo-500/10 rounded-full blur-[120px]"></div> -->
      
      <div class="relative z-10 max-w-4xl mx-auto text-center space-y-12">
        <div class="inline-flex items-center gap-3 bg-white/5 backdrop-blur-xl px-6 py-3 rounded-full border border-white/10">
          <span class="w-2.5 h-2.5 bg-amber-400 rounded-full animate-pulse shadow-[0_0_15px_rgba(251,191,36,0.8)]"></span>
          <span class="text-xs font-black uppercase tracking-[0.3em] text-indigo-100 italic" style="color: #e0e7ff;">루틴파인더스 프리미엄</span>
        </div>

          <h2 class="text-[32px] lg:text-7xl font-black leading-[1.15] lg:leading-[1.1] tracking-tighter break-keep" style="color: white;">
            단순한 기록을 넘어,<br/>
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-amber-200 via-yellow-400 to-orange-400" style="color: #fbbf24;">당신의 정체성을 설계하다</span>
          </h2>
          <p class="text-lg lg:text-2xl text-slate-400 font-medium max-w-2xl mx-auto leading-relaxed break-keep px-4 lg:px-0" style="color: #94a3b8;">
            루파 클럽은 성실함의 가치를 아는 상위 1%를 위한 프리미엄 루틴 성장 시스템입니다.
          </p>

        <!-- Benefits Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 lg:gap-6 text-center lg:text-left pt-8 lg:pt-12">
          <% [
            ["📊", "성장 리포트", "매월 객관적인 지표로 분석하는 루파 인텔리전스"],
            ["🤝", "오프라인 모임", "서울·경기 지역 정기 오프라인 성장 네트워킹"],
            ["🔱", "VIP 인증", "디지털 VIP 카드와 상위 0.1%를 위한 특별 혜택"],
            ["🔥", "시너지 피드", "검증된 멤버들과 나누는 압도적인 성취 에너지"]
          ].each do |icon, title, desc| %>
            <div class="bg-white/5 backdrop-blur-md p-8 rounded-[40px] border border-white/5 hover:border-white/20 transition-all group">
              <div class="text-4xl mb-6 group-hover:scale-125 transition-transform duration-500"><%= icon %></div>
              <h4 class="text-lg font-black text-white mb-2"><%= title %></h4>
              <p class="text-sm text-slate-500 font-medium leading-relaxed"><%= desc %></p>
            </div>
          <% end %>
        </div>

        <div class="pt-12 flex flex-col items-center gap-8">
          <% if @official_club&.persisted? %>
            <% if @official_club.recruitment_open? %>
              <%= link_to routine_club_path(@official_club), class: "group relative w-full sm:w-auto px-8 lg:px-16 py-6 lg:py-8 bg-white text-slate-900 rounded-[28px] lg:rounded-[32px] font-black text-lg lg:text-xl hover:bg-amber-400 transition-all shadow-xl overflow-hidden" do %>
                <span class="relative z-10">지금 루파 클럽 <%= @official_club.generation_number %>기 합류하기</span>
                <div class="absolute inset-0 bg-gradient-to-r from-amber-300 to-orange-400 opacity-0 group-hover:opacity-100 transition-opacity"></div>
              <% end %>
            <% else %>
              <%= form_with url: join_routine_club_path(@official_club), method: :post, class: "w-full sm:w-auto" do |f| %>
                <%= hidden_field_tag :depositor_name, "베타테스터" %>
                <%= hidden_field_tag :contact_info, "베타테스트" %>
                <%= hidden_field_tag :beta_test, true %>
                <button type="submit" class="group relative w-full sm:w-auto px-8 lg:px-16 py-6 lg:py-8 bg-gradient-to-r from-orange-500 to-pink-500 hover:from-orange-600 hover:to-pink-600 text-white rounded-[28px] lg:rounded-[32px] font-black text-lg lg:text-xl transition-all shadow-xl hover:shadow-2xl active:scale-95 overflow-hidden flex items-center justify-center gap-3">
                  <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
                  </svg>
                  <span class="relative z-10">기존 멤버 참여</span>
                  <div class="absolute inset-0 bg-gradient-to-r from-pink-600 to-orange-600 opacity-0 group-hover:opacity-100 transition-opacity"></div>
                </button>
              <% end %>
            <% end %>
          <% end %>
          <div class="flex flex-col sm:flex-row items-center gap-2 lg:gap-3 text-slate-500">
            <% if @official_club %>
              <% 
                fee = @official_club.calculate_quarterly_fee
              %>
              <span class="text-xs lg:text-sm font-bold">분기 정액 멤버십 <span class="text-amber-400"><%= number_to_currency(fee, unit: "₩", precision: 0) %></span> (하루 100원 수준)</span>
            <% else %>
              <span class="text-xs lg:text-sm font-bold">하루 100원 수준의 투자로 누리는 모든 권한</span>
            <% end %>
            <div class="hidden sm:block w-1 h-1 bg-slate-700 rounded-full"></div>
            <span class="text-xs lg:text-sm font-bold">기수 완주(출석 70%) 시 명예 배지 부여</span>
          </div>
        </div>
      </div>
    </section>
  <% elsif current_user.pending_rufa_membership %>
    <!-- Approved but waiting for quarter start -->
    <section class="relative overflow-hidden bg-slate-900 rounded-[48px] lg:rounded-[64px] px-6 py-20 lg:p-32 text-white shadow-2xl text-center">
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_50%_0%,rgba(99,102,241,0.15),transparent_50%)]"></div>
      
      <div class="relative z-10 max-w-2xl mx-auto space-y-10">
        <div class="inline-flex items-center gap-3 bg-indigo-500/20 backdrop-blur-xl px-6 py-3 rounded-full border border-indigo-500/30">
          <span class="w-2.5 h-2.5 bg-indigo-400 rounded-full animate-pulse"></span>
          <% d_day = (current_user.pending_rufa_membership.membership_start_date - Date.current).to_i %>
          <span class="text-sm font-black uppercase tracking-widest text-indigo-100 italic">D-<%= d_day %> 활동 시작까지</span>
        </div>

        <h2 class="text-4xl lg:text-6xl font-black leading-tight break-keep">
          승인이 완료되었습니다!<br/>
          <span class="text-indigo-400">명예로운 <%= current_user.pending_rufa_membership.routine_club.generation_number(current_user.pending_rufa_membership.membership_start_date) %>기</span> 활동을 기다려주세요.
        </h2>
        
        <p class="text-lg lg:text-xl text-slate-400 font-medium leading-relaxed break-keep">
          루파 클럽의 모든 프리미엄 기능은 기수가 시작되는 <br class="hidden lg:block"/>
          <span class="text-white font-bold"><%= current_user.pending_rufa_membership.membership_start_date.strftime("%Y년 %m월 %d일") %></span>부터 자동으로 활성화됩니다.
        </p>

        <div class="bg-white/5 border border-white/10 rounded-3xl p-8 space-y-4">
          <p class="text-sm font-bold text-slate-500 uppercase tracking-widest">활동 준비 방법</p>
          <ul class="text-left space-y-3 max-w-md mx-auto">
            <li class="flex items-center gap-3 text-slate-300 font-bold">
              <svg class="w-5 h-5 text-indigo-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"/></svg>
              <span>나만의 성취 루틴 미리 설계하기</span>
            </li>
            <li class="flex items-center gap-3 text-slate-300 font-bold">
              <svg class="w-5 h-5 text-indigo-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"/></svg>
              <span>루파 클럽 공지사항 및 가이드 숙지하기</span>
            </li>
          </ul>
        </div>
      </div>
    </section>

  <% elsif (@my_membership.present? && @my_membership.status_active?) || current_user.admin? %>
    <!-- START: Premium Member Dashboard (Prototype Style) -->
    <div class="space-y-8" id="rufa-premium-board">
      
      <!-- 1. Identity & Wallet Section (Top) -->
      <section class="grid grid-cols-1 md:grid-cols-12 gap-6">
        <!-- Growth Identity Card -->
        <div class="md:col-span-8 relative overflow-hidden bg-gradient-to-br from-indigo-700 via-indigo-900 to-black rounded-[40px] p-8 lg:p-10 text-white shadow-2xl group cursor-pointer" onclick="toggleVipCard()">
          <div class="absolute top-0 right-0 w-64 h-64 bg-white/10 rounded-full -translate-y-1/2 translate-x-1/2 blur-3xl group-hover:bg-white/20 transition-all"></div>
          <div class="relative z-10 flex flex-col md:flex-row justify-between h-full gap-8">
            <div class="space-y-6">
              <div class="inline-flex items-center gap-3 px-4 py-1.5 bg-white/10 backdrop-blur-md rounded-full border border-white/20">
                <span class="w-2 h-2 bg-amber-400 rounded-full animate-pulse"></span>
                <span class="text-[10px] font-black uppercase tracking-[0.2em] text-indigo-100">Official Membership</span>
              </div>
              <div>
                <h2 class="text-3xl md:text-5xl font-black tracking-tight mb-2">
                  <%= current_user.nickname %><span class="text-indigo-400">님,</span>
                </h2>
                <p class="text-lg md:text-xl font-bold text-white/60">당신의 정체성은 <span class="text-white">'<%= @my_official_membership&.identity_title || "루파" %>'</span> 입니다.</p>
              </div>
            </div>
            <div class="flex flex-col items-end justify-between text-right">
              <div class="text-5xl opacity-80 group-hover:scale-110 transition-transform duration-500">🔱</div>
              <div class="space-y-1">
                <p class="text-[10px] font-black text-indigo-300 uppercase tracking-widest">Mastering Routine</p>
                <div class="flex items-center gap-2 justify-end">
                  <span class="text-2xl font-black">Lv.<%= (@member_days / 7) + 1 %></span>
                  <div class="w-24 h-1.5 bg-white/20 rounded-full overflow-hidden">
                    <div class="h-full bg-indigo-400" style="width: <%= (@member_days % 7) * 14 %>%"></div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Quick Stats / Wallet -->
        <div class="md:col-span-4 grid grid-rows-2 gap-4">
          <div class="bg-slate-900 rounded-[32px] p-6 border border-white/5 flex items-center justify-between group hover:border-indigo-500/50 transition-colors">
            <div class="space-y-1">
              <p class="text-[10px] font-black text-slate-500 uppercase tracking-widest">Growth Points</p>
              <h4 class="text-3xl font-black text-white tabular-nums"><%= @my_official_membership&.growth_points || 0 %>P</h4>
            </div>
            <div class="w-12 h-12 bg-indigo-500/10 rounded-2xl flex items-center justify-center text-2xl">⚡</div>
          </div>
          <div class="bg-slate-900 rounded-[32px] p-6 border border-white/5 flex items-center justify-between group hover:border-emerald-500/50 transition-colors">
            <div class="space-y-1">
              <p class="text-[10px] font-black text-slate-500 uppercase tracking-widest">Relax Passes</p>
              <h4 class="text-3xl font-black text-white tabular-nums"><%= @my_membership.remaining_passes %>개</h4>
            </div>
            <div class="w-12 h-12 bg-emerald-500/10 rounded-2xl flex items-center justify-center text-2xl">🎟️</div>
          </div>
        </div>
      </section>

      <!-- 2. Dashboard Tabs (Prototype Style Nav) -->
      <div class="flex flex-col lg:flex-row gap-8">
        <!-- Sidebar Navigation -->
        <aside class="w-full lg:w-64 shrink-0 space-y-4">
          <nav class="flex flex-row lg:flex-col gap-2 overflow-x-auto no-scrollbar pb-2">
            <% [
              ["club_dashboard", "🏠", "대시보드"],
              ["dashboard", "⚡", "나의 루틴"],
              ["synergy", "✨", "시너지"],
              ["members", "👥", "멤버 루틴"],
              ["ranking", "🏆", "실시간 랭킹"],
              ["architecture", "📐", "목표 설계"]
            ].each do |id, icon, label| %>
              <button type="button" onclick="switchRufaTab('<%= id %>')" id="nav-<%= id %>" 
                      class="rufa-nav-btn whitespace-nowrap lg:w-full flex items-center gap-3 px-6 py-4 rounded-2xl font-black text-xs transition-all group <%= id == 'club_dashboard' ? 'bg-indigo-600 text-white shadow-xl shadow-indigo-900/20' : 'bg-slate-900 text-slate-500 border border-white/5 hover:bg-white/5' %>">
                <span class="text-lg"><%= icon %></span>
                <span><%= label %></span>
              </button>
            <% end %>
          </nav>
          
          <div class="hidden lg:block p-6 bg-indigo-500/5 border border-indigo-500/10 rounded-3xl space-y-4">
            <p class="text-[10px] font-black text-indigo-400 uppercase tracking-widest">Quick Actions</p>
            <%= link_to achievement_report_path, class: "flex items-center justify-between text-xs font-bold text-white/50 hover:text-white transition-colors" do %>
              <span>성과 통합 리포트</span>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M13 7l5 5m0 0l-5 5m5-5H6"/></svg>
            <% end %>
            <%= link_to routine_club_reports_path, class: "flex items-center justify-between text-xs font-bold text-white/50 hover:text-white transition-colors" do %>
              <span>주간 성실함 리포트</span>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M13 7l5 5m0 0l-5 5m5-5H6"/></svg>
            <% end %>
          </div>
        </aside>

        <!-- Main Content Area -->
        <main class="flex-1 min-w-0">
          <!-- Club Dashboard -->
          <div id="tab-club_dashboard" class="rufa-content-tab space-y-6">
             <%= render 'routine_clubs/dashboard' %>
          </div>

          <!-- My Routine (Integrated with Orbit) -->
          <div id="tab-dashboard" class="rufa-content-tab hidden space-y-8">
            <!-- Orbital Visualization Holder -->
            <div class="bg-slate-950 rounded-[48px] p-6 border border-white/5 overflow-hidden relative">
              <div class="absolute inset-0 bg-gradient-to-b from-indigo-500/5 to-transparent"></div>
              <%= render partial: "prototype/orbit", locals: { 
                    progress: @current_achievement_rate.to_i, 
                    completed_count: @todays_routines.select(&:completed_today?).count, 
                    total_task_count: @todays_routines.count,
                    orbit_users: @rufa_rankings.map{ |r| r[:user] },
                    todays_routines: @todays_routines 
                  } %>
            </div>

            <div class="space-y-4">
              <div class="flex items-center justify-between px-2">
                <h3 class="text-xs font-black text-slate-500 uppercase tracking-widest">오늘의 루틴 체크인</h3>
                <span class="text-[10px] font-black text-indigo-400"><%= Date.current.strftime("%m.%d %a") %></span>
              </div>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <% @todays_routines.each do |routine| %>
                  <div class="rufa-routine-item" id="routine_<%= routine.id %>_club">
                    <%= render 'personal_routines/club_routine_card', routine: routine %>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
          
          <!-- Synergy Feed -->
          <div id="tab-synergy" class="rufa-content-tab hidden">
             <div class="bg-slate-900 rounded-[32px] border border-white/5 p-8">
                <!-- Synergy content similarly styled as synergy.html.erb -->
                <%= render 'personal_routines/club_synergy_feed' %>
             </div>
          </div>

          <!-- Members List -->
          <div id="tab-members" class="rufa-content-tab hidden">
             <%= render 'personal_routines/club_members_list' %>
          </div>

          <!-- Ranking -->
          <div id="tab-ranking" class="rufa-content-tab hidden">
             <%= render 'personal_routines/club_ranking_board' %>
          </div>

          <!-- Architecture -->
          <div id="tab-architecture" class="rufa-content-tab hidden">
             <%= render 'personal_routines/club_architecture_form' %>
          </div>
        </main>
      </div>
    </div>
  <% end %>
</div>

<!-- Digital VIP Card Modal -->
<div id="vip-card-modal" class="fixed inset-0 z-[110] hidden items-center justify-center p-6 text-white">
  <div class="absolute inset-0 bg-slate-900/98 backdrop-blur-3xl" onclick="toggleVipCard()"></div>
  <div class="relative z-20 w-full max-w-md bg-gradient-to-br from-indigo-700 via-slate-900 to-black rounded-[72px] p-16 border border-white/10 shadow-[0_0_100px_rgba(79,70,229,0.4)] space-y-16 transform scale-90 opacity-0 transition-all duration-700" id="vip-card-content">
     <div class="flex justify-between items-center">
        <div class="space-y-4">
          <div class="px-4 py-1.5 bg-amber-400 text-slate-900 rounded-full w-fit">
            <p class="text-[10px] font-black tracking-[0.4em] uppercase">루파 오피셜</p>
          </div>
          <h2 class="text-6xl font-black text-white italic tracking-tighter">루파 VIP</h2>
        </div>
        <div class="w-20 h-20 bg-white rounded-3xl flex items-center justify-center text-slate-900 text-4xl shadow-glow shadow-indigo-500/50">🔱</div>
     </div>

     <div class="space-y-12">
        <div class="flex items-center gap-10">
           <img src="<%= current_user.profile_image %>" class="w-32 h-32 rounded-[48px] border-4 border-white/20 shadow-2xl" />
           <div class="space-y-3">
             <p class="text-xs font-black text-indigo-300/50 uppercase tracking-[0.3em]">Platinum Member</p>
             <h3 class="text-4xl font-black text-white tracking-tight"><%= current_user.nickname %></h3>
           </div>
        </div>

        <div class="grid grid-cols-2 gap-12 border-t border-white/5 pt-12">
           <div class="space-y-2">
             <p class="text-[10px] font-black text-white/30 uppercase tracking-[0.2em]">Achievement Rate</p>
             <p class="text-xl font-black text-indigo-400"><%= @current_achievement_rate.to_i %>%</p>
           </div>
           <div class="space-y-2">
             <p class="text-[10px] font-black text-white/30 uppercase tracking-[0.2em]">Identity</p>
             <p class="text-lg font-black text-white"><%= @my_official_membership&.identity_title || "루파" %></p>
           </div>
           <div class="space-y-2">
             <p class="text-[10px] font-black text-white/30 uppercase tracking-[0.2em]">Rank</p>
             <p class="text-lg font-black text-emerald-400">Top <%= rand(1..5) %>%</p>
           </div>
           <div class="space-y-2">
             <p class="text-[10px] font-black text-white/30 uppercase tracking-[0.2em]">Authenticated</p>
             <p class="text-lg font-black text-amber-400"><%= @my_official_membership&.joined_at&.strftime("%Y.%m.%d") || Date.current.strftime("%Y.%m.%d") %></p>
           </div>
        </div>
     </div>

      <div class="pt-12 border-t border-white/5">
        <div class="text-[11px] font-bold text-slate-500 uppercase leading-relaxed text-left opacity-60">
          본 패스는 루틴 파인더스(Routine Finders) 본부에서 인증 및 발행되었습니다. 2026년 루파 클럽 멤버 전용 혜택 증빙용으로 사용 가능합니다.
        </div>
      </div>
  </div>
</div>

<script>
  function toggleVipCard() {
    const modal = document.getElementById('vip-card-modal');
    const content = document.getElementById('vip-card-content');
    
    if (modal.classList.contains('hidden')) {
      modal.classList.remove('hidden');
      modal.classList.add('flex');
      setTimeout(() => {
        content.classList.remove('scale-90', 'opacity-0');
        content.classList.add('scale-100', 'opacity-100');
      }, 10);
    } else {
      content.classList.remove('scale-100', 'opacity-100');
      content.classList.add('scale-90', 'opacity-0');
      setTimeout(() => {
        modal.classList.add('hidden');
        modal.classList.remove('flex');
      }, 500);
    }
  }

  function switchRufaTab(id) {
    const targetTab = document.getElementById('tab-' + id);
    if (!targetTab) return;

    // Hide all tabs
    document.querySelectorAll('.rufa-content-tab').forEach(tab => tab.classList.add('hidden'));
    // Show selected tab
    targetTab.classList.remove('hidden');
    
    // Update navigation styles (Dark theme aware)
    document.querySelectorAll('.rufa-nav-btn').forEach(btn => {
      btn.classList.remove('bg-indigo-600', 'text-white', 'shadow-xl');
      btn.classList.add('bg-slate-900', 'text-slate-500', 'border-white/5');
    });

    const activeBtn = document.getElementById('nav-' + id);
    if (activeBtn) {
      activeBtn.classList.remove('bg-slate-900', 'text-slate-500', 'border-white/5');
      activeBtn.classList.add('bg-indigo-600', 'text-white', 'shadow-xl');
      activeBtn.scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'center' });
    }

    // Update URL without full page reload
    const url = new URL(window.location);
    url.searchParams.set('sub_tab', id);
    window.history.replaceState({}, '', url);

    // Scroll to top of content area on mobile
    if (window.innerWidth < 1024) {
      const board = document.getElementById('rufa-premium-board');
      if (board) {
        window.scrollTo({ top: board.offsetTop - 20, behavior: 'smooth' });
      }
    }
  }

  function toggleMemberRoutines(userId) {
    const content = document.getElementById(`routines-${userId}`);
    const arrow = document.getElementById(`arrow-${userId}`);
    
    if (content.classList.contains('hidden')) {
      content.classList.remove('hidden');
      arrow.style.transform = 'rotate(180deg)';
    } else {
      content.classList.add('hidden');
      arrow.style.transform = 'rotate(0deg)';
    }
  }

  function switchRankingMode(mode) {
    const monthlyContainer = document.getElementById('monthly-ranking-container');
    const lifetimeContainer = document.getElementById('lifetime-ranking-container');
    const monthlyStats = document.getElementById('monthly-stats-summary');
    const lifetimeStats = document.getElementById('lifetime-stats-summary');
    const periodBadge = document.getElementById('ranking-period-badge');
    const description = document.getElementById('ranking-description');
    
    // Update button states
    document.querySelectorAll('.ranking-mode-btn').forEach(btn => {
      btn.classList.remove('bg-white', 'text-indigo-600', 'text-emerald-600', 'shadow-sm');
      btn.classList.add('text-slate-400');
    });
    
    if (mode === 'monthly') {
      document.getElementById('btn-monthly-rank').classList.remove('text-slate-400');
      document.getElementById('btn-monthly-rank').classList.add('bg-white', 'text-indigo-600', 'shadow-sm');
      
      monthlyContainer?.classList.remove('hidden');
      lifetimeContainer?.classList.add('hidden');
      monthlyStats?.classList.remove('hidden');
      lifetimeStats?.classList.add('hidden');
      
      if (periodBadge) periodBadge.innerText = '<%= Date.current.strftime("%m.%d") %>';
      if (description) description.innerText = '월간 성장 리더보드';
    } else {
      document.getElementById('btn-lifetime-rank').classList.remove('text-slate-400');
      document.getElementById('btn-lifetime-rank').classList.add('bg-white', 'text-emerald-600', 'shadow-sm');
      
      monthlyContainer?.classList.add('hidden');
      lifetimeContainer?.classList.remove('hidden');
      monthlyStats?.classList.add('hidden');
      lifetimeStats?.classList.remove('hidden');
      
      if (periodBadge) periodBadge.innerText = '누적';
      if (description) description.innerText = '누적 성취 리더보드';
    }
  }

  // Handle initial tab and history
  const initRufaTabs = () => {
    const urlParams = new URLSearchParams(window.location.search);
    const subTab = urlParams.get('sub_tab') || 'club_dashboard';
    if (document.getElementById('tab-' + subTab)) {
      switchRufaTab(subTab);
    }
  };

  document.addEventListener('turbo:load', initRufaTabs);
  window.addEventListener('popstate', initRufaTabs);
</script>
