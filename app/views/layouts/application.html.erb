<!DOCTYPE html>
<html class="scroll-smooth">
  <head>
    <title><%= content_for(:title) || "Routine Finders" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="<%= content_for(:description) || "함께 습관을 만들어가는 루틴 챌린지 플랫폼" %>">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Routine Finders">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#4F46E5">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Open Graph / SNS 공유 -->
    <meta property="og:type" content="website">
    <meta property="og:title" content="<%= content_for(:title) || "Routine Finders" %>">
    <meta property="og:description" content="<%= content_for(:description) || "함께 습관을 만들어가는 루틴 챌린지 플랫폼" %>">
    <meta property="og:image" content="<%= request.base_url %>/og-image.png">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:site_name" content="Routine Finders">

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= content_for(:title) || "Routine Finders" %>">
    <meta name="twitter:description" content="<%= content_for(:description) || "함께 습관을 만들어가는 루틴 챌린지 플랫폼" %>">
    <meta name="twitter:image" content="<%= request.base_url %>/og-image.png">

    <%= yield :head %>

    <!-- PWA -->
    <link rel="manifest" href="/manifest.json">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="루파">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png">
    <meta name="theme-color" content="#7C4DFF">

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/icon.svg">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">


    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@100..900&display=swap" rel="stylesheet">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.9.2/dist/confetti.browser.min.js"></script>
    
    <!-- Kakao SDK -->
    <script src="https://t1.kakaocdn.net/kakao_js_sdk/2.7.0/kakao.min.js" crossorigin="anonymous"></script>
    <script>
      document.addEventListener('turbo:load', function() {
        if (typeof Kakao !== 'undefined' && !Kakao.isInitialized()) {
          Kakao.init('f959f8ada6c21d791ef5be7f4257e19e');
        }
      });
    </script>

    <style>
      /* Mobile App-like Experience Globals */
      * {
        -webkit-tap-highlight-color: transparent;
        -webkit-touch-callout: none;
      }
      
      body {
        overscroll-behavior-y: none; /* Prevent rubber-band effect on body if needed, or keep for native feel */
      }

      .active-scale {
        transition: transform 0.1s ease-out;
      }
      .active-scale:active {
        transform: scale(0.95);
      }

      /* Bottom Sheet Animation */
      @keyframes slide-up {
        from { transform: translateY(100%); }
        to { transform: translateY(0); }
      }
      .animate-slide-up {
        animation: slide-up 0.4s cubic-bezier(0.16, 1, 0.3, 1);
      }

      @keyframes slide-out-down {
        from { transform: translateY(0); opacity: 1; }
        to { transform: translateY(20px); opacity: 0; }
      }
      .animate-slide-out-down {
        animation: slide-out-down 0.4s cubic-bezier(0.16, 1, 0.3, 1) forwards;
      }
    </style>
  </head>

  <body class="min-h-screen bg-[#0C0B12]" 
        data-controller="modal inapp-browser" 
        data-modal-auto-open-value="<%= alert == '로그인이 필요합니다.' %>"
        style="font-family: 'Outfit', sans-serif;">
    
    <%= render 'shared/welcome_modal' %>
    

    <%# ORIGINAL WEB NAVIGATION REMOVED FOR PROTOTYPE CONSISTENCY %>

    <!-- Mobile Bottom Navigation -->
    <%= render "shared/bottom_nav" %>


    <!-- Trendy Flash Notifications -->
    <div class="fixed top-6 left-1/2 -translate-x-1/2 z-[100] flex flex-col gap-3 w-full max-w-sm px-4">
      <% if notice %>
        <div data-controller="flash" 
             data-flash-timeout-value="3000"
             class="group relative overflow-hidden bg-white/80 backdrop-blur-xl border border-emerald-500/20 shadow-[0_8px_32px_rgba(16,185,129,0.15)] rounded-2xl p-4 flex items-center gap-4 transition-all duration-500
                    opacity-0 translate-y-[-10px] scale-95 group-[.visible]:opacity-100 group-[.visible]:translate-y-0 group-[.visible]:scale-100
                    hover:scale-[1.02] active:scale-95">
          <div class="flex-shrink-0 w-10 h-10 bg-emerald-500 rounded-xl flex items-center justify-center shadow-lg shadow-emerald-500/20">
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="m5 13 4 4L19 7"/>
            </svg>
          </div>
          <div class="flex-1">
            <p class="text-emerald-900 font-bold text-sm tracking-tight"><%= notice %></p>
            <p class="text-emerald-600/70 text-[11px] font-medium uppercase tracking-wider mt-0.5">Success</p>
          </div>
          <button data-action="click->flash#dismiss" class="text-emerald-300 hover:text-emerald-500 transition-colors">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
          </button>
          
          <!-- Progress Bar -->
          <div class="absolute bottom-0 left-0 h-1 bg-emerald-500/30 w-full">
            <div class="h-full bg-emerald-500 transition-all duration-[3000ms] ease-linear w-0 group-[.visible]:w-full"></div>
          </div>
        </div>
      <% end %>

      <% if alert %>
        <div data-controller="flash" 
             data-flash-timeout-value="4000"
             class="group relative overflow-hidden bg-white/80 backdrop-blur-xl border border-rose-500/20 shadow-[0_8px_32px_rgba(244,63,94,0.15)] rounded-2xl p-4 flex items-center gap-4 transition-all duration-500
                    opacity-0 translate-y-[-10px] scale-95 group-[.visible]:opacity-100 group-[.visible]:translate-y-0 group-[.visible]:scale-100
                    hover:scale-[1.02] active:scale-95">
          <div class="flex-shrink-0 w-10 h-10 bg-rose-500 rounded-xl flex items-center justify-center shadow-lg shadow-rose-500/20">
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
            </svg>
          </div>
          <div class="flex-1">
            <p class="text-rose-900 font-bold text-sm tracking-tight"><%= alert %></p>
            <p class="text-rose-600/70 text-[11px] font-medium uppercase tracking-wider mt-0.5">Alert</p>
          </div>
          <button data-action="click->flash#dismiss" class="text-rose-300 hover:text-rose-500 transition-colors">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
          </button>

          <!-- Progress Bar -->
          <div class="absolute bottom-0 left-0 h-1 bg-rose-500/30 w-full">
            <div class="h-full bg-rose-500 transition-all duration-[4000ms] ease-linear w-0 group-[.visible]:w-full"></div>
          </div>
        </div>
      <% end %>
    </div>

    <main class="flex-1 w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 pb-32 lg:pb-8">
      <%= yield %>
    </main>


    <!-- Auth Modal -->
    <% unless current_user %>
      <%= render "shared/auth_modal" %>
    <% end %>

    <!-- Badge Award Modal -->
    <%= render "shared/badge_award" if logged_in? %>
  </body>
</html>
