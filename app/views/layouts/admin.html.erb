<!DOCTYPE html>
<html class="dark">
  <head>
    <title>Routine Finders | Management System</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@100..900&family=Inter:wght@100..900&display=swap" rel="stylesheet">
    
    <%= stylesheet_link_tag "tailwind", "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
      :root {
        --admin-bg: #030712;
        --admin-card: rgba(17, 24, 39, 0.7);
        --admin-accent: #6366f1;
        --admin-accent-glow: rgba(99, 102, 241, 0.3);
      }

      body {
        font-family: 'Inter', sans-serif;
        background-color: var(--admin-bg);
        color: #f3f4f6;
        margin: 0;
        overflow-x: hidden;
      }

      h1, h2, h3, .font-outfit {
        font-family: 'Outfit', sans-serif;
      }

      .admin-glass {
        background: var(--admin-card);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 255, 255, 0.05);
      }

      .admin-sidebar {
        width: 280px;
        height: 100vh;
        position: fixed;
        left: 0;
        top: 0;
        z-index: 50;
        border-right: 1px solid rgba(255, 255, 255, 0.05);
      }

      .admin-content {
        margin-left: 280px;
        padding: 40px;
        min-height: 100vh;
        background: radial-gradient(circle at 50% -20%, #1e1b4b 0%, #030712 60%);
      }

      .nav-link {
        display: flex;
        items-center: center;
        gap: 12px;
        padding: 12px 20px;
        margin: 4px 16px;
        border-radius: 12px;
        color: #9ca3af;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        font-weight: 500;
        text-decoration: none;
      }

      .nav-link:hover {
        background: rgba(255, 255, 255, 0.05);
        color: white;
        transform: translateX(4px);
      }

      .nav-link.active {
        background: var(--admin-accent);
        color: white;
        box-shadow: 0 0 20px var(--admin-accent-glow);
      }

      .admin-stat-card {
        padding: 24px;
        border-radius: 24px;
        transition: all 0.4s ease;
        position: relative;
        overflow: hidden;
      }

      .admin-stat-card:hover {
        transform: translateY(-8px);
        border-color: rgba(99, 102, 241, 0.3);
      }

      .accent-glow {
        position: absolute;
        width: 100px;
        height: 100px;
        background: var(--admin-accent);
        filter: blur(80px);
        opacity: 0.15;
        top: -40px;
        right: -40px;
      }

      /* Custom Scrollbar */
      ::-webkit-scrollbar { width: 8px; }
      ::-webkit-scrollbar-track { background: transparent; }
      ::-webkit-scrollbar-thumb { 
        background: rgba(255, 255, 255, 0.1); 
        border-radius: 4px;
      }
      ::-webkit-scrollbar-thumb:hover { background: rgba(255, 255, 255, 0.2); }

      @media (max-width: 1024px) {
        .admin-sidebar { width: 80px; }
        .admin-content { margin-left: 80px; }
        .nav-text { display: none; }
      }
    </style>
  </head>

  <body>
    <!-- Sidebar -->
    <aside class="admin-sidebar admin-glass flex flex-col">
      <div class="p-8 pb-10">
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 bg-indigo-600 rounded-xl flex items-center justify-center shadow-lg shadow-indigo-600/30">
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
          </div>
          <div>
            <span class="font-outfit text-xl font-black tracking-tight nav-text block">RF System</span>
            <span class="text-[8px] font-black text-indigo-400 uppercase tracking-[0.2em] nav-text">V2 Intelligence</span>
          </div>
        </div>
      </div>

      <nav class="flex-1 space-y-1">
        <div class="px-6 mb-2">
          <p class="text-[9px] font-black text-slate-600 uppercase tracking-widest pl-4">Main Monitor</p>
        </div>
        <a href="/admin" class="nav-link <%= 'active' if controller_name == 'dashboard' %>">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/></svg>
          <span class="nav-text">인텔리전스 대시보드</span>
        </a>

        <div class="px-6 mt-6 mb-2">
          <p class="text-[9px] font-black text-slate-600 uppercase tracking-widest pl-4">Resource Control</p>
        </div>
        <a href="/admin/users" class="nav-link <%= 'active' if controller_name == 'users' %>">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 01-9-3.5m9.908-3.908A3.333 3.333 0 0118 13.333M18 10a3.333 3.333 0 00-3.333-3.333"/></svg>
          <span class="nav-text">사용자 인텔리전스</span>
        </a>
        <a href="/admin/challenges" class="nav-link <%= 'active' if controller_name == 'challenges' %>">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A3.323 3.323 0 0010.65 15c-1.846 0-3.415-1.29-3.762-3.132M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          <span class="nav-text">챌린지 퍼포먼스</span>
        </a>
        <a href="/admin/routine_clubs" class="nav-link <%= 'active' if controller_name == 'routine_clubs' %>">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/></svg>
          <span class="nav-text">루파 클럽 네트워크</span>
        </a>

        <div class="px-6 mt-6 mb-2">
          <p class="text-[9px] font-black text-slate-600 uppercase tracking-widest pl-4">Marketing & System</p>
        </div>
        <a href="/admin/banners" class="nav-link <%= 'active' if controller_name == 'banners' %>">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>
          <span class="nav-text">프로모션 에셋</span>
        </a>
        <button class="nav-link w-[calc(100%-32px)] text-left opacity-40 cursor-not-allowed">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A3.323 3.323 0 0010.65 15c-1.846 0-3.415-1.29-3.762-3.132M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          <span class="nav-text">성장/배지 엔진</span>
        </button>
      </nav>

      <div class="p-4 mt-auto">
        <div class="p-4 rounded-[24px] bg-white/5 border border-white/10 relative overflow-hidden group">
          <div class="absolute inset-0 bg-indigo-500/5 translate-y-full group-hover:translate-y-0 transition-transform"></div>
          <p class="text-[9px] uppercase font-black text-slate-500 tracking-widest mb-1 relative z-10">Operator</p>
          <p class="text-xs font-black text-indigo-400 relative z-10"><%= current_user.nickname %></p>
          <%= link_to root_path, class: "mt-4 block py-3 bg-white/5 hover:bg-white/10 rounded-xl text-xs font-bold transition-all text-center relative z-10" do %>
            Exit to Workspace
          <% end %>
        </div>
      </div>
    </aside>

    <!-- Content -->
    <main class="admin-content">
      <%= yield %>
    </main>
  </body>
</html>
