<div class="max-w-2xl mx-auto">
  <div class="flex items-center gap-4 mb-10">
    <a href="<%= admin_user_path(@user) %>" class="p-3 bg-white/5 hover:bg-white/10 rounded-2xl text-slate-400 hover:text-white transition-all">
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M10 19l-7-7m0 0l7-7m-7 7h18"/></svg>
    </a>
    <h1 class="text-3xl font-black tracking-tight">Edit Profiles</h1>
  </div>

  <%= form_with model: [:admin, @user], class: "space-y-6" do |f| %>
    <div class="admin-glass rounded-[40px] p-10 space-y-8">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div class="space-y-2">
          <%= f.label :nickname, "Nickname", class: "text-[10px] font-black text-slate-500 uppercase tracking-widest ml-1" %>
          <%= f.text_field :nickname, class: "w-full px-6 py-4 bg-white/5 border border-white/10 rounded-2xl text-white font-bold focus:border-indigo-500 focus:ring-0 transition-all" %>
        </div>
        <div class="space-y-2">
          <%= f.label :email, "Email Address", class: "text-[10px] font-black text-slate-500 uppercase tracking-widest ml-1" %>
          <%= f.email_field :email, class: "w-full px-6 py-4 bg-white/5 border border-white/10 rounded-2xl text-white font-bold focus:border-indigo-500 focus:ring-0 transition-all" %>
        </div>
      </div>

      <div class="space-y-2">
        <%= f.label :role, "System Role", class: "text-[10px] font-black text-slate-500 uppercase tracking-widest ml-1" %>
        <%= f.select :role, [["Standard User", "user"], ["Club Administrator", "club_admin"], ["Super Administrator", "super_admin"]], {}, 
            class: "w-full px-6 py-4 bg-white/5 border border-white/10 rounded-2xl text-indigo-400 font-black focus:border-indigo-500 focus:ring-0 transition-all appearance-none" %>
      </div>

      <div class="grid grid-cols-2 gap-6">
        <div class="space-y-2">
          <%= f.label :level, "User Level", class: "text-[10px] font-black text-slate-500 uppercase tracking-widest ml-1" %>
          <%= f.number_field :level, class: "w-full px-6 py-4 bg-white/5 border border-white/10 rounded-2xl text-white font-bold focus:border-indigo-500 focus:ring-0 transition-all" %>
        </div>
        <div class="space-y-2">
          <%= f.label :wallet_balance, "Wallet Balance (₩)", class: "text-[10px] font-black text-slate-500 uppercase tracking-widest ml-1" %>
          <%= f.number_field :wallet_balance, class: "w-full px-6 py-4 bg-white/5 border border-white/10 rounded-2xl text-emerald-400 font-bold focus:border-indigo-500 focus:ring-0 transition-all" %>
        </div>
      </div>

      <div class="pt-6 border-t border-white/5 flex gap-4">
        <%= f.submit "Save Changes", class: "flex-1 py-5 bg-indigo-600 hover:bg-indigo-500 text-white rounded-3xl font-black transition-all shadow-xl shadow-indigo-600/20 cursor-pointer" %>
        <a href="<%= admin_user_path(@user) %>" class="px-10 py-5 bg-white/5 hover:bg-white/10 text-slate-400 rounded-3xl font-black transition-all">Cancel</a>
      </div>
    </div>
  <% end %>

  <div class="mt-8 p-8 rounded-[32px] bg-amber-500/5 border border-amber-500/10">
    <div class="flex gap-4">
      <div class="w-10 h-10 rounded-xl bg-amber-500/20 flex items-center justify-center text-amber-500 flex-shrink-0">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/></svg>
      </div>
      <div>
        <p class="text-sm font-black text-amber-500 mb-1">Warning: Privilege Escalation</p>
        <p class="text-[11px] text-amber-500/60 leading-relaxed font-medium">
          사용자의 역할을 'Super Administrator'로 변경하면 시스템 전체에 대한 모든 권한이 부여됩니다. 이 작업은 신뢰할 수 있는 사용자에게만 수행하십시오.
        </p>
      </div>
    </div>
  </div>
</div>
