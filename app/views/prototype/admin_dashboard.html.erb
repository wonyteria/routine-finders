<div class="min-h-screen bg-[#0F0F13] text-slate-200" 
     data-controller="system-admin tabs"
     data-tabs-active-class="bg-indigo-600 text-white shadow-lg shadow-indigo-600/20"
     data-tabs-inactive-class="text-slate-500 hover:text-slate-300">

  <!-- 1. Top GNB -->
  <header class="sticky top-0 z-50 bg-[#0F0F13]/80 backdrop-blur-xl border-b border-white/5 px-6 py-4">
    <div class="flex items-center justify-between gap-4">
      <div class="flex items-center gap-3">
        <%= link_to prototype_home_path, class: "w-10 h-10 rounded-2xl bg-white/5 flex items-center justify-center text-slate-400 active:scale-95 transition-all" do %>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M15 19l-7-7 7-7"/></svg>
        <% end %>
        <div>
          <h1 class="text-lg font-black text-white leading-none">시스템 명령 센터</h1>
          <p class="text-[9px] font-bold text-slate-500 uppercase tracking-widest mt-1">System Administration v2.1</p>
        </div>
      </div>

      <div class="flex items-center gap-2">
        <div class="px-3 py-1.5 rounded-xl bg-emerald-500/10 border border-emerald-500/20 flex items-center gap-2">
          <div class="w-1.5 h-1.5 rounded-full bg-emerald-500 animate-pulse"></div>
          <span class="text-[10px] font-black text-emerald-400 uppercase tracking-widest">운영 중</span>
        </div>
      </div>
    </div>

    <!-- Main Navigation Tabs -->
    <nav class="flex gap-1 mt-6 p-1 bg-black/40 rounded-2xl border border-white/5">
      <button data-action="tabs#change" data-tabs-target="tab" data-id="overview" class="flex-1 py-3 rounded-xl text-[10px] font-black transition-all">개요</button>
      <button data-action="tabs#change" data-tabs-target="tab" data-id="users" class="flex-1 py-3 rounded-xl text-[10px] font-black transition-all">유저 관리</button>
      <button data-action="tabs#change" data-tabs-target="tab" data-id="content" class="flex-1 py-3 rounded-xl text-[10px] font-black transition-all">콘텐츠 관리</button>
      <button data-action="tabs#change" data-tabs-target="tab" data-id="push" class="flex-1 py-3 rounded-xl text-[10px] font-black transition-all">푸시 설정</button>
      <button data-action="tabs#change" data-tabs-target="tab" data-id="system" class="flex-1 py-3 rounded-xl text-[10px] font-black transition-all">시스템 로그</button>
    </nav>
  </header>

  <main class="py-8 pb-32">
    
    <!-- SECTION 1: OVERVIEW -->
    <div data-tabs-target="panel" data-id="overview" class="space-y-8 animate-in fade-in slide-in-from-bottom-4 duration-500">
      <section class="px-6 grid grid-cols-2 gap-4">
        <div class="app-glass-card p-6 border-white/5 space-y-4">
          <div class="flex items-center justify-between">
            <div class="w-10 h-10 rounded-2xl bg-indigo-500/10 flex items-center justify-center text-indigo-400">
               <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"/></svg>
            </div>
            <span class="text-[10px] font-black text-emerald-500">+<%= rand(5..15) %>%</span>
          </div>
          <div>
            <p class="text-[10px] font-black text-slate-500 uppercase tracking-widest mb-1">전체 사용자</p>
            <h3 class="text-3xl font-black text-white"><%= number_with_delimiter(@total_users) %></h3>
          </div>
        </div>

        <div class="app-glass-card p-6 border-white/5 space-y-4">
          <div class="flex items-center justify-between">
            <div class="w-10 h-10 rounded-2xl bg-amber-500/10 flex items-center justify-center text-amber-400">
               <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
            </div>
          </div>
          <div>
            <p class="text-[10px] font-black text-slate-500 uppercase tracking-widest mb-1">시스템 활성율</p>
            <h3 class="text-3xl font-black text-white"><%= @system_pulse %>%</h3>
          </div>
        </div>
      </section>

      <section class="px-6 space-y-4">
        <h2 class="text-xs font-black text-slate-500 uppercase tracking-widest pl-1">빠른 실행</h2>
        <div class="grid grid-cols-2 gap-3">
          <% if current_user.super_admin? %>
            <button data-action="system-admin#broadcast" class="p-5 rounded-2xl bg-rose-500/5 border border-rose-500/10 flex flex-col items-center gap-2 group active:scale-95 transition-all">
              <span class="text-2xl group-hover:scale-110 transition-transform">🚨</span>
              <span class="text-[10px] font-black text-rose-500 uppercase tracking-widest text-center">전체 공지 발송</span>
            </button>
          <% end %>
          
          <%= link_to prototype_admin_clubs_path, class: "p-5 rounded-2xl bg-indigo-500/5 border border-indigo-500/10 flex flex-col items-center gap-2 group active:scale-95 transition-all text-center #{current_user.super_admin? ? '' : 'col-span-2'}" do %>
            <span class="text-2xl group-hover:scale-110 transition-transform">🔱</span>
            <span class="text-[10px] font-black text-indigo-500 uppercase tracking-widest">루파 클럽 관리</span>
          <% end %>

          <!-- DANGER ZONE: Reset Users -->
          <% if current_user.super_admin? %>
            <%= button_to prototype_reset_users_path, 
                          method: :post, 
                          data: { turbo_confirm: "jorden00@naver.com 계정을 제외한 모든 회원이 삭제됩니다. 정말 진행할까요?" },
                          class: "p-5 rounded-2xl bg-red-600/10 border border-red-600 flex flex-col items-center gap-2 group active:scale-95 transition-all text-center w-full col-span-2" do %>
              <span class="text-2xl group-hover:scale-110 transition-transform">💀</span>
              <span class="text-[10px] font-black text-red-500 uppercase tracking-widest">전체 유저 초기화</span>
            <% end %>
          <% end %>
        </div>
      </section>
    </div>

    <!-- SECTION 2: USERS -->
    <div data-tabs-target="panel" data-id="users" class="hidden px-6 space-y-6 animate-in fade-in slide-in-from-bottom-4 duration-500">
      <div class="space-y-4">
        <!-- Search Bar -->
        <div class="app-glass-card p-4 border-white/5">
          <%= form_with url: prototype_admin_dashboard_path, method: :get, class: "flex gap-2", data: { turbo_frame: "users_table" } do |f| %>
            <%= hidden_field_tag :tab, "users" %>
            <%= hidden_field_tag :member_filter, @member_filter %>
            <div class="flex-1 relative">
              <svg class="absolute left-4 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/></svg>
              <%= f.text_field :user_search, 
                               placeholder: "유저 닉네임 또는 이메일 검색...", 
                               class: "w-full pl-11 pr-4 py-3.5 bg-black/40 border border-white/5 rounded-2xl text-sm font-bold text-white focus:border-indigo-500 outline-none transition-all" %>
            </div>
            <button type="submit" class="px-6 rounded-2xl bg-indigo-600 text-white font-black text-[10px] uppercase tracking-widest active:scale-95 transition-all">검색</button>
          <% end %>
        </div>

        <!-- Filter Chips -->
        <div class="flex gap-2 overflow-x-auto pb-2 hide-scrollbar">
          <% [["all", "전체 사용자"], ["club", "루파 클럽 멤버"], ["general", "일반 사용자"]].each do |val, label| %>
            <%= link_to prototype_admin_dashboard_path(tab: "users", member_filter: val, user_search: params[:user_search]), 
                        data: { turbo_frame: "users_table" },
                        class: "px-4 py-2 rounded-full text-[10px] font-black whitespace-nowrap transition-all border #{ @member_filter == val ? 'bg-indigo-600 border-indigo-500 text-white shadow-lg shadow-indigo-600/20' : 'bg-white/5 border-white/5 text-slate-500 hover:text-slate-300' }" do %>
              <%= label %>
            <% end %>
          <% end %>
        </div>
      </div>

      <turbo-frame id="users_table">
        <div class="app-glass-card border-white/5 overflow-x-auto custom-scrollbar">
          <table class="w-full text-left min-w-[600px]">
            <thead>
              <tr class="bg-white/[0.02] border-b border-white/5">
                <th class="px-4 py-4 text-[9px] font-black text-slate-500 uppercase tracking-widest">사용자 정보</th>
                <th class="px-4 py-4 text-[9px] font-black text-slate-500 uppercase tracking-widest text-center">권한 설정</th>
                <th class="px-4 py-4 text-[9px] font-black text-slate-500 uppercase tracking-widest text-right">상태 관리</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-white/5">
              <% if @users.any? %>
                <% @users.each do |user| %>
                  <tr class="hover:bg-white/[0.02] transition-colors group">
                    <td class="px-4 py-4">
                      <div class="flex items-center gap-3">
                        <div class="relative shrink-0">
                          <div class="w-10 h-10 rounded-xl bg-white/10 overflow-hidden">
                            <img src="<%= user.profile_image %>" class="w-full h-full object-cover">
                          </div>
                          <div class="absolute -bottom-1 -right-1 px-1 bg-indigo-500 rounded text-[7px] font-black text-white border border-[#0F0F13]">Lv.<%= user.level %></div>
                        </div>
                        <div class="min-w-0">
                          <div class="flex items-center gap-2">
                          <p class="text-xs font-black text-white truncate"><%= user.nickname %></p>
                          <% if user.is_rufa_club_member? %>
                             <span class="px-1 py-0.5 bg-indigo-500/10 text-indigo-400 text-[7px] font-black rounded border border-indigo-500/20">루파 클럽 멤버</span>
                          <% else %>
                             <span class="px-1 py-0.5 bg-white/5 text-slate-500 text-[7px] font-black rounded border border-white/5">일반 사용자</span>
                          <% end %>
                        </div>
                          <p class="text-[9px] font-bold text-slate-500 truncate mt-0.5"><%= user.email %></p>
                          <p class="text-[8px] font-medium text-slate-600 mt-1">가입: <%= user.created_at.strftime('%Y.%m.%d') %></p>
                        </div>
                      </div>
                    </td>
                    <td class="px-4 py-4 text-center">
                      <% if current_user.super_admin? %>
                        <select data-action="change->system-admin#updateUserRoleSelect"
                                data-user-id="<%= user.id %>"
                                data-nickname="<%= user.nickname %>"
                                data-initial-value="<%= user.role %>"
                                class="w-24 bg-black/40 border border-white/10 rounded-xl px-2 py-2 text-[10px] font-black text-white focus:border-indigo-500 outline-none cursor-pointer appearance-none text-center">
                          <% [["user", "일반 사용자"], ["club_admin", "루파 클럽 관리자"], ["super_admin", "마스터 권한"]].each do |role, label| %>
                            <option value="<%= role %>" <%= 'selected' if user.role == role %> class="bg-[#1A1921]"><%= label %></option>
                          <% end %>
                        </select>
                      <% else %>
                        <span class="text-[10px] font-medium text-slate-400"><%= user.role %></span>
                      <% end %>
                    </td>
                    <td class="px-4 py-4 text-right">
                      <div class="flex flex-col items-end gap-1">
                        <% if current_user.super_admin? %>
                          <select data-action="change->system-admin#updateUserStatusSelect"
                                  data-user-id="<%= user.id %>"
                                  data-nickname="<%= user.nickname %>"
                                  data-initial-value="<%= user.active_status %>"
                                  class="w-24 bg-black/40 border border-white/10 rounded-xl px-2 py-2 text-[10px] font-black text-white focus:border-indigo-500 outline-none cursor-pointer appearance-none text-center">
                            <% [["active", "이용 중"], ["suspended", "정지"], ["withdrawn", "탈퇴"]].each do |status, label| %>
                              <option value="<%= status %>" <%= 'selected' if user.active_status == status %> class="bg-[#1A1921]"><%= label %></option>
                            <% end %>
                          </select>
                        <% else %>
                          <span class="px-2 py-1 bg-white/5 rounded-lg text-[10px] text-slate-400"><%= user.active_status %></span>
                        <% end %>
                      </div>
                    </td>
                  </tr>
                <% end %>
              <% else %>
                <tr>
                  <td colspan="3" class="px-4 py-20 text-center">
                    <p class="text-[11px] font-black text-slate-700 uppercase tracking-widest italic">조건에 맞는 사용자가 없습니다.</p>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </turbo-frame>
    </div>

    <!-- SECTION 3: CONTENT -->
    <div data-tabs-target="panel" data-id="content" class="hidden px-6 space-y-6 animate-in fade-in slide-in-from-bottom-4 duration-500" 
         data-controller="sub-tabs"
         data-sub-tabs-active-class="border-indigo-500 text-white"
         data-sub-tabs-inactive-class="border-transparent text-slate-500 hover:text-slate-300">
      <!-- Content Sub-tabs -->
      <div class="flex gap-4 border-b border-white/5 pb-px overflow-x-auto hide-scrollbar">
        <button data-sub-tabs-target="tab" data-id="active_challenges" data-action="click->sub-tabs#change" class="pb-4 text-[10px] font-black uppercase tracking-widest transition-all border-b-2 border-indigo-500 text-white">진행 중 챌린지</button>
        <button data-sub-tabs-target="tab" data-id="active_gatherings" data-action="click->sub-tabs#change" class="pb-4 text-[10px] font-black uppercase tracking-widest transition-all border-b-2 border-transparent text-slate-500 hover:text-slate-300">진행 중 모임</button>
        <button data-sub-tabs-target="tab" data-id="pending_review" data-action="click->sub-tabs#change" class="pb-4 text-[10px] font-black uppercase tracking-widest transition-all border-b-2 border-transparent text-slate-500 hover:text-slate-300">신규 검토</button>
      </div>

      <!-- Active Challenges Panel -->
      <div data-sub-tabs-target="panel" data-id="active_challenges" class="space-y-4">
        <% if @active_challenges.any? %>
          <% @active_challenges.each do |c| %>
            <div class="app-glass-card p-5 border-white/5 flex items-center justify-between group">
              <div class="flex items-center gap-4">
                <div class="w-12 h-12 rounded-xl bg-white/5 overflow-hidden">
                  <img src="<%= c.thumbnail %>" class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all">
                </div>
                <div>
                  <h4 class="text-sm font-black text-white"><%= c.title %></h4>
                  <div class="flex flex-col mt-0.5 space-y-1">
                    <p class="text-[10px] font-bold text-slate-400">호스트: <%= c.host_name %> (<%= c.host.email %>)</p>
                    <div class="flex items-center gap-2">
                       <span class="text-[9px] font-medium text-slate-500">연락처: <%= c.host_phone.presence || '미등록' %></span>
                       <% if c.chat_link.present? %>
                        <a href="<%= c.chat_link %>" target="_blank" class="text-[8px] font-black text-indigo-400 bg-indigo-400/10 px-1.5 py-0.5 rounded border border-indigo-400/20 hover:bg-indigo-400/20 transition-all">오픈채팅방 ↗</a>
                      <% end %>
                    </div>
                    <p class="text-[9px] font-medium text-indigo-500/50">참여 <%= c.current_participants %>명</p>
                  </div>
                </div>
              </div>
              <div class="flex gap-2">
                <button data-action="system-admin#notifyHost" data-id="<%= c.id %>" data-nickname="<%= c.host_name %>" data-title="<%= c.title %>" class="w-9 h-9 rounded-xl border border-white/5 flex items-center justify-center text-slate-500 hover:text-indigo-400 hover:bg-indigo-400/10 transition-all" title="호스트공지">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"/></svg>
                </button>
                <button data-action="system-admin#deleteContent" data-id="<%= c.id %>" data-title="<%= c.title %>" class="w-9 h-9 rounded-xl border border-white/5 flex items-center justify-center text-slate-500 hover:text-rose-400 hover:bg-rose-400/10 transition-all" title="삭제">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/></svg>
                </button>
              </div>
            </div>
          <% end %>
        <% else %>
          <div class="py-20 text-center opacity-30 italic text-[11px] font-black uppercase tracking-widest">진행 중인 챌린지가 없습니다.</div>
        <% end %>
      </div>

      <!-- Active Gatherings Panel -->
      <div data-sub-tabs-target="panel" data-id="active_gatherings" class="hidden space-y-4 text-white">
        <% if @active_gatherings.any? %>
          <% @active_gatherings.each do |c| %>
            <div class="app-glass-card p-5 border-white/5 flex items-center justify-between group">
              <div class="flex items-center gap-4">
                <div class="w-12 h-12 rounded-xl bg-white/5 overflow-hidden">
                  <img src="<%= c.thumbnail %>" class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all">
                </div>
                <div>
                  <h4 class="text-sm font-black text-white"><%= c.title %></h4>
                  <div class="flex flex-col mt-0.5 space-y-1">
                    <p class="text-[10px] font-bold text-slate-400">호스트: <%= c.host_name %> (<%= c.host.email %>)</p>
                    <div class="flex items-center gap-2">
                       <span class="text-[9px] font-medium text-slate-500">연락처: <%= c.host_phone.presence || '미등록' %></span>
                       <% if c.chat_link.present? %>
                        <a href="<%= c.chat_link %>" target="_blank" class="text-[8px] font-black text-indigo-400 bg-indigo-400/10 px-1.5 py-0.5 rounded border border-indigo-400/20 hover:bg-indigo-400/20 transition-all">오픈채팅방 ↗</a>
                      <% end %>
                    </div>
                    <p class="text-[9px] font-medium text-slate-600">장소: <%= c.offline? ? c.meeting_info&.place_name : '온라인' %></p>
                  </div>
                </div>
              </div>
              <div class="flex gap-2">
                <button data-action="system-admin#notifyHost" data-id="<%= c.id %>" data-nickname="<%= c.host_name %>" data-title="<%= c.title %>" class="w-9 h-9 rounded-xl border border-white/5 flex items-center justify-center text-slate-500 hover:text-indigo-400 hover:bg-indigo-400/10 transition-all">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"/></svg>
                </button>
                <button data-action="system-admin#deleteContent" data-id="<%= c.id %>" data-title="<%= c.title %>" class="w-9 h-9 rounded-xl border border-white/5 flex items-center justify-center text-slate-500 hover:text-rose-400 hover:bg-rose-400/10 transition-all">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/></svg>
                </button>
              </div>
            </div>
          <% end %>
        <% else %>
          <div class="py-20 text-center opacity-30 italic text-[11px] font-black uppercase tracking-widest">진행 중인 모임이 없습니다.</div>
        <% end %>
      </div>

      <!-- Pending Review Panel -->
      <div data-sub-tabs-target="panel" data-id="pending_review" class="hidden space-y-4">
        <p class="text-[9px] font-bold text-slate-500 px-1 italic">* 이 탭의 콘텐츠는 승인 절차 없이도 사용자에게 노출됩니다. 관리 점검용으로 활용하세요.</p>
        <% if @pending_challenges.any? %>
          <% @pending_challenges.each do |c| %>
            <div class="app-glass-card p-5 border-rose-500/10 bg-rose-500/[0.02] flex items-center justify-between">
              <div class="flex items-center gap-4">
                <div class="w-10 h-10 rounded-xl bg-white/5 overflow-hidden ring-1 ring-rose-500/20">
                  <img src="<%= c.thumbnail %>" class="w-full h-full object-cover">
                </div>
                <div>
                  <h4 class="text-sm font-black text-white"><%= c.title %></h4>
                  <div class="flex flex-col space-y-1">
                    <p class="text-[10px] font-bold text-rose-300 mt-0.5">호스트: <%= c.host_name %> (<%= c.host.email %>)</p>
                    <div class="flex items-center gap-2">
                       <span class="text-[9px] font-medium text-slate-500">연락처: <%= c.host_phone.presence || '미등록' %></span>
                       <% if c.chat_link.present? %>
                        <a href="<%= c.chat_link %>" target="_blank" class="text-[8px] font-black text-indigo-400 bg-indigo-400/10 px-1.5 py-0.5 rounded border border-indigo-400/20 hover:bg-indigo-400/20 transition-all">오픈채팅방 ↗</a>
                      <% end %>
                    </div>
                    <p class="text-[9px] font-bold text-slate-600"><%= time_ago_in_words(c.created_at) %> 전 생성</p>
                  </div>
                </div>
              </div>
              <div class="flex gap-2">
                <button data-action="system-admin#deleteContent" data-id="<%= c.id %>" data-title="<%= c.title %>" class="px-3 py-2 rounded-xl bg-rose-500 text-white text-[10px] font-black uppercase active:scale-95 transition-all">삭제</button>
              </div>
            </div>
          <% end %>
        <% else %>
          <div class="py-20 text-center opacity-30 italic text-[11px] font-black uppercase tracking-widest">새롭게 검토할 콘텐츠가 없습니다.</div>
        <% end %>
      </div>
    </div>

    <!-- SECTION 4: SYSTEM -->
    <div data-tabs-target="panel" data-id="system" class="hidden px-6 space-y-8 animate-in fade-in slide-in-from-bottom-4 duration-500">
      <section class="space-y-4">
        <label class="text-[10px] font-black text-rose-500 uppercase tracking-widest pl-1">실시간 스트림</label>
        <div class="app-glass-card border-rose-500/10 bg-black/40 p-3 space-y-2 font-mono max-h-96 overflow-y-auto custom-scrollbar">
            <% combined_logs = (@stream_memberships + @stream_joins + @stream_completions + @stream_activities).sort_by(&:created_at).reverse %>
            <% combined_logs.first(50).each do |log| %>
              <div class="text-[9px] leading-relaxed">
                <span class="text-slate-600">[<%= log.created_at.strftime("%H:%M:%S") %>]</span>
                <span class="text-indigo-400 font-bold"><%= log.class.name %>:</span> 
                <span class="text-slate-300"><%= log.respond_to?(:user) ? log.user.nickname : 'System' %></span>
              </div>
            <% end %>
        </div>
      </section>

      <section class="space-y-3">
        <% if current_user.super_admin? %>
          <button data-action="system-admin#purgeCache" class="w-full p-4 bg-white/5 border border-white/10 rounded-2xl flex items-center justify-between group active:scale-[0.98] transition-all">
            <div>
              <p class="text-xs font-black text-rose-500">시스템 캐시 초기화</p>
              <p class="text-[9px] font-bold text-slate-600 italic">서버의 모든 캐시 데이터를 삭제합니다.</p>
            </div>
            <svg class="w-5 h-5 text-rose-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/></svg>
          </button>
        <% else %>
          <div class="p-4 bg-white/5 border border-white/10 rounded-2xl flex items-center justify-between opacity-50 cursor-not-allowed">
            <div>
              <p class="text-xs font-black text-slate-500">시스템 캐시 초기화</p>
              <p class="text-[9px] font-bold text-slate-600 italic">마스터 권한이 필요합니다.</p>
            </div>
            <svg class="w-5 h-5 text-slate-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/></svg>
          </div>
        <% end %>
      </section>
    </div>

    <!-- SECTION 5: PUSH SETTINGS -->
    <div data-tabs-target="panel" data-id="push" class="hidden px-6 space-y-6 animate-in fade-in slide-in-from-bottom-4 duration-500">
      <section class="space-y-4">
        <h3 class="text-xs font-black text-slate-500 uppercase tracking-widest pl-1">정기 자동 알림 설정</h3>
        
        <div class="grid grid-cols-1 gap-6">
          <% (@push_configs || []).each do |config| %>
            <div class="app-glass-card p-6 border-white/5 space-y-6">
              <div class="flex items-center justify-between">
                <div class="flex items-center gap-3">
                  <div class="w-10 h-10 rounded-2xl bg-indigo-500/10 flex items-center justify-center text-xl">
                    <%= config.config_type == 'morning_reminder' ? '☀️' : '🌙' %>
                  </div>
                  <div>
                    <h4 class="text-sm font-black text-white"><%= config.config_type == 'morning_reminder' ? '아침 리마인더' : '저녁 기록 체크' %></h4>
                    <p class="text-[9px] font-bold text-slate-500 uppercase tracking-widest mt-0.5"><%= config.config_type %></p>
                  </div>
                </div>
                <div class="flex items-center gap-2">
                  <span class="text-[10px] font-black <%= config.enabled ? 'text-emerald-500' : 'text-slate-500' %> uppercase tracking-widest"><%= config.enabled ? '활성화' : '비활성화' %></span>
                </div>
              </div>

              <%= form_with model: config, url: prototype_update_push_config_path(config_type: config.config_type), method: :patch, class: "space-y-4" do |f| %>
                <div class="grid grid-cols-2 gap-4">
                  <div class="col-span-2">
                    <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest pl-1 mb-1.5 block">알림 제목</label>
                    <%= f.text_field :title, class: "w-full bg-black/40 border border-white/10 rounded-xl px-4 py-3 text-sm font-bold text-white focus:border-indigo-500 outline-none transition-all" %>
                  </div>
                  <div class="col-span-2">
                    <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest pl-1 mb-1.5 block">알림 내용</label>
                    <%= f.text_area :content, rows: 2, class: "w-full bg-black/40 border border-white/10 rounded-xl px-4 py-3 text-sm font-bold text-white focus:border-indigo-500 outline-none transition-all resize-none" %>
                  </div>
                  <div class="col-span-2">
                    <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest pl-1 mb-1.5 block">이동 경로 (Local Path)</label>
                    <%= f.text_field :link_url, placeholder: "/?tab=club", class: "w-full bg-black/40 border border-white/10 rounded-xl px-4 py-3 text-sm font-bold text-white focus:border-indigo-500 outline-none transition-all" %>
                  </div>
                  <div>
                    <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest pl-1 mb-1.5 block">발송 시간 (HH:MM)</label>
                    <%= f.text_field :schedule_time, placeholder: "09:00", class: "w-full bg-black/40 border border-white/10 rounded-xl px-4 py-3 text-sm font-bold text-white focus:border-indigo-500 outline-none transition-all text-center" %>
                  </div>
                  <div>
                    <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest pl-1 mb-1.5 block">활성화 여부</label>
                    <div class="flex items-center gap-3 bg-black/40 border border-white/10 rounded-xl px-4 py-2.5">
                      <label class="flex items-center gap-2 cursor-pointer group">
                        <%= f.check_box :enabled, class: "w-4 h-4 rounded border-white/20 bg-transparent text-indigo-500 focus:ring-offset-0 focus:ring-0" %>
                        <span class="text-xs font-bold text-slate-400 group-hover:text-slate-200 transition-colors">푸시 발송 활성화</span>
                      </label>
                    </div>
                  </div>
                </div>

                <div class="flex justify-end pt-2">
                  <%= f.submit "설정 저장하기", class: "px-6 py-3 rounded-xl bg-indigo-600 text-white font-black text-[10px] uppercase tracking-widest active:scale-95 transition-all shadow-lg shadow-indigo-600/20" %>
                </div>
              <% end %>
            </div>
          <% end %>
        </div>

        <div class="p-4 rounded-2xl bg-amber-500/5 border border-amber-500/10 flex items-start gap-3">
          <span class="text-lg">ℹ️</span>
          <div class="text-[10px] font-bold text-amber-500/80 leading-relaxed">
            <p>발송 시간은 1시간 단위로 체크됩니다. (예: 09:00 설정 시 매일 오전 9시 정각 발송)</p>
            <p class="mt-1 pb-1 border-b border-amber-500/20">사용 가능한 변수: <code class="bg-amber-500/20 px-1 rounded text-amber-400 font-black">{{nickname}}</code> (유저 닉네임으로 자동 치환)</p>
            <p class="mt-1">구독 중인 모든 PWA 유저들에게 발송되며, 활성화 상태에서만 자동 발송됩니다.</p>
          </div>
        </div>
      </section>
    </div>
  </main>

  <style>
    .custom-scrollbar::-webkit-scrollbar { width: 4px; }
    .custom-scrollbar::-webkit-scrollbar-track { background: transparent; }
    .custom-scrollbar::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.08); border-radius: 10px; }
    
    @keyframes badge-pop {
      0% { transform: scale(0.95); opacity: 0; }
      100% { transform: scale(1); opacity: 1; }
    }
    .animate-badge-pop { animation: badge-pop 0.3s cubic-bezier(0.34, 1.56, 0.64, 1) forwards; }
  </style>
</div>
