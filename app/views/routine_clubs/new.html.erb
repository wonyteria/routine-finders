<div class="min-h-screen bg-gradient-to-br from-indigo-50 via-white to-purple-50/30">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    
    <!-- Header -->
    <header class="mb-12 text-center">
      <div class="inline-flex items-center gap-2 bg-indigo-100 px-4 py-2 rounded-full mb-4">
        <svg class="w-4 h-4 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
        </svg>
        <span class="text-sm font-black text-indigo-600">HOST</span>
      </div>
      <h1 class="text-4xl md:text-5xl font-black text-slate-900 mb-4">
        루틴 클럽 개설하기
      </h1>
      <p class="text-lg text-slate-600 font-medium">
        함께 성장할 멤버들을 모집하고, 체계적인 환경에서 루틴을 이끌어가세요
      </p>
    </header>

    <!-- Form -->
    <%= form_with model: @routine_club, class: "space-y-8" do |f| %>
      
      <!-- Basic Info -->
      <section class="bg-white rounded-[40px] p-8 border border-slate-200 shadow-sm">
        <h2 class="text-2xl font-black text-slate-900 mb-6">기본 정보</h2>
        
        <div class="space-y-6">
          <div>
            <label class="block text-sm font-black text-slate-700 mb-2">클럽 이름 *</label>
            <%= f.text_field :title, placeholder: "예: 새벽 5시 기상 챌린지", required: true, class: "w-full px-5 py-4 bg-slate-50 border-2 border-slate-200 rounded-2xl text-slate-900 placeholder-slate-400 font-bold focus:ring-2 ring-indigo-500 focus:border-indigo-500 transition-all" %>
          </div>
          
          <div>
            <label class="block text-sm font-black text-slate-700 mb-2">카테고리 *</label>
            <%= f.select :category, ["건강·운동", "학습·자기계발", "재테크·부업", "취미·여가", "생활습관"], {}, class: "w-full px-5 py-4 bg-slate-50 border-2 border-slate-200 rounded-2xl text-slate-900 font-bold focus:ring-2 ring-indigo-500 focus:border-indigo-500 transition-all" %>
          </div>
          
          <div>
            <label class="block text-sm font-black text-slate-700 mb-2">클럽 소개 *</label>
            <%= f.text_area :description, rows: 4, placeholder: "이 클럽에서 어떤 루틴을 함께 만들어갈지 설명해주세요", required: true, class: "w-full px-5 py-4 bg-slate-50 border-2 border-slate-200 rounded-2xl text-slate-900 placeholder-slate-400 font-bold focus:ring-2 ring-indigo-500 focus:border-indigo-500 transition-all resize-none" %>
          </div>
        </div>
      </section>

      <!-- Schedule -->
      <section class="bg-white rounded-[40px] p-8 border border-slate-200 shadow-sm">
        <h2 class="text-2xl font-black text-slate-900 mb-6">일정 설정</h2>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div>
            <label class="block text-sm font-black text-slate-700 mb-2">시작일 *</label>
            <%= f.date_field :start_date, min: Date.current, required: true, class: "w-full px-5 py-4 bg-slate-50 border-2 border-slate-200 rounded-2xl text-slate-900 font-bold focus:ring-2 ring-indigo-500 focus:border-indigo-500 transition-all" %>
          </div>
          
          <div>
            <label class="block text-sm font-black text-slate-700 mb-2">종료일 *</label>
            <%= f.date_field :end_date, min: Date.current + 3.months, required: true, class: "w-full px-5 py-4 bg-slate-50 border-2 border-slate-200 rounded-2xl text-slate-900 font-bold focus:ring-2 ring-indigo-500 focus:border-indigo-500 transition-all" %>
            <p class="text-xs text-slate-500 font-medium mt-2">최소 3개월 이상 설정해주세요</p>
          </div>
        </div>
      </section>

      <!-- Pricing -->
      <section class="bg-white rounded-[40px] p-8 border border-slate-200 shadow-sm">
        <h2 class="text-2xl font-black text-slate-900 mb-6">요금 설정</h2>
        
        <div class="space-y-6">
          <div>
            <label class="block text-sm font-black text-slate-700 mb-2">월 요금 *</label>
            <div class="relative">
              <%= f.number_field :monthly_fee, min: 0, step: 1000, placeholder: "30000", required: true, class: "w-full px-5 py-4 bg-slate-50 border-2 border-slate-200 rounded-2xl text-slate-900 placeholder-slate-400 font-bold focus:ring-2 ring-indigo-500 focus:border-indigo-500 transition-all" %>
              <span class="absolute right-5 top-1/2 -translate-y-1/2 text-slate-400 font-bold">원</span>
            </div>
            <p class="text-xs text-slate-500 font-medium mt-2">멤버들이 매월 납부할 금액입니다</p>
          </div>
          
          <div class="bg-indigo-50 border border-indigo-100 rounded-2xl p-6">
            <div class="flex items-start gap-3">
              <svg class="w-6 h-6 text-indigo-600 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
              <div>
                <p class="text-sm font-black text-indigo-900 mb-2">입금 계좌 안내</p>
                <p class="text-sm text-indigo-700 font-medium">
                  모든 입금은 <span class="font-black">루틴 파인더스 계좌</span>로 진행됩니다.<br/>
                  신한은행 110-123-456789 (예금주: 루틴파인더스)
                </p>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Capacity -->
      <section class="bg-white rounded-[40px] p-8 border border-slate-200 shadow-sm">
        <h2 class="text-2xl font-black text-slate-900 mb-6">정원 설정</h2>
        
        <div>
          <label class="block text-sm font-black text-slate-700 mb-2">최대 인원 *</label>
          <%= f.number_field :max_members, min: 5, max: 50, value: 30, required: true, class: "w-full px-5 py-4 bg-slate-50 border-2 border-slate-200 rounded-2xl text-slate-900 font-bold focus:ring-2 ring-indigo-500 focus:border-indigo-500 transition-all" %>
          <p class="text-xs text-slate-500 font-medium mt-2">5명 ~ 50명 사이로 설정할 수 있습니다</p>
        </div>
      </section>

      <!-- Rules -->
      <section class="bg-white rounded-[40px] p-8 border border-slate-200 shadow-sm">
        <div class="flex items-center justify-between mb-6">
          <div>
            <h2 class="text-2xl font-black text-slate-900">클럽 규칙</h2>
            <p class="text-sm text-slate-500 font-medium mt-1">멤버들이 지켜야 할 규칙을 설정하세요</p>
          </div>
          <button type="button" onclick="addRule()" class="px-4 py-2 bg-indigo-600 text-white rounded-xl font-black text-sm hover:bg-indigo-700 transition-colors">
            + 규칙 추가
          </button>
        </div>
        
        <div id="rules-container" class="space-y-4">
          <%= f.fields_for :rules do |rule_form| %>
            <div class="rule-item bg-slate-50 rounded-3xl p-6 border-2 border-slate-200">
              <div class="space-y-4">
                <div>
                  <%= rule_form.text_field :title, placeholder: "규칙 제목", class: "w-full px-4 py-3 bg-white border-2 border-slate-200 rounded-xl text-slate-900 placeholder-slate-400 font-bold focus:ring-2 ring-indigo-500 focus:border-indigo-500" %>
                </div>
                <div>
                  <%= rule_form.text_area :description, rows: 2, placeholder: "규칙 설명 (선택)", class: "w-full px-4 py-3 bg-white border-2 border-slate-200 rounded-xl text-slate-900 placeholder-slate-400 font-medium focus:ring-2 ring-indigo-500 focus:border-indigo-500 resize-none" %>
                </div>
                
                <div class="flex items-center gap-4">
                  <label class="flex items-center gap-2 cursor-pointer">
                    <%= rule_form.check_box :has_penalty, class: "w-5 h-5 rounded border-2 border-slate-300 text-indigo-600 focus:ring-2 ring-indigo-500" %>
                    <span class="text-sm font-black text-slate-700">패널티 설정</span>
                  </label>
                </div>
                
                <div class="penalty-fields hidden">
                  <div class="bg-red-50 border border-red-100 rounded-2xl p-4 space-y-3">
                    <%= rule_form.text_field :penalty_description, placeholder: "패널티 내용", class: "w-full px-4 py-2 bg-white border border-red-200 rounded-xl text-sm text-slate-900 placeholder-slate-400 font-medium focus:ring-2 ring-red-500 focus:border-red-500" %>
                    <div class="grid grid-cols-2 gap-3">
                      <%= rule_form.number_field :penalty_points, min: 0, placeholder: "경고 횟수", class: "px-4 py-2 bg-white border border-red-200 rounded-xl text-sm text-slate-900 placeholder-slate-400 font-medium focus:ring-2 ring-red-500 focus:border-red-500" %>
                      <%= rule_form.number_field :auto_kick_threshold, min: 0, placeholder: "자동 강퇴 기준", class: "px-4 py-2 bg-white border border-red-200 rounded-xl text-sm text-slate-900 placeholder-slate-400 font-medium focus:ring-2 ring-red-500 focus:border-red-500" %>
                    </div>
                  </div>
                </div>
                
                <%= rule_form.hidden_field :position %>
              </div>
            </div>
          <% end %>
        </div>
        
        <div class="mt-6 bg-amber-50 border border-amber-100 rounded-2xl p-6">
          <div class="flex items-start gap-3">
            <svg class="w-6 h-6 text-amber-600 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
            </svg>
            <div>
              <p class="text-sm font-black text-amber-900 mb-2">규칙 설정 팁</p>
              <ul class="text-sm text-amber-700 font-medium space-y-1">
                <li>• 명확하고 측정 가능한 규칙을 만드세요</li>
                <li>• 패널티는 공정하고 일관되게 적용하세요</li>
                <li>• 너무 많은 규칙보다 핵심 규칙에 집중하세요</li>
              </ul>
            </div>
          </div>
        </div>
      </section>

      <!-- Submit -->
      <div class="flex gap-4">
        <%= link_to "취소", routine_clubs_path, class: "flex-1 py-4 bg-slate-100 text-slate-600 rounded-2xl font-black text-center hover:bg-slate-200 transition-colors" %>
        <%= f.submit "클럽 개설하기", class: "flex-1 py-4 bg-gradient-to-r from-indigo-600 to-purple-600 text-white rounded-2xl font-black text-lg hover:shadow-2xl hover:shadow-indigo-200 transition-all cursor-pointer" %>
      </div>
      
    <% end %>

  </div>
</div>

<script>
  // Toggle penalty fields
  document.addEventListener('DOMContentLoaded', () => {
    document.querySelectorAll('input[type="checkbox"][id*="has_penalty"]').forEach(checkbox => {
      checkbox.addEventListener('change', function() {
        const penaltyFields = this.closest('.rule-item').querySelector('.penalty-fields');
        if (this.checked) {
          penaltyFields.classList.remove('hidden');
        } else {
          penaltyFields.classList.add('hidden');
        }
      });
      
      // Initial state
      if (checkbox.checked) {
        const penaltyFields = checkbox.closest('.rule-item').querySelector('.penalty-fields');
        penaltyFields.classList.remove('hidden');
      }
    });
  });
  
  // Add new rule (simplified - you may want to use Stimulus for better implementation)
  function addRule() {
    alert('규칙 추가 기능은 Stimulus controller로 구현하는 것을 권장합니다.');
  }
</script>
