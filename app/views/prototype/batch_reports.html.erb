<%
  # 텍스트 리포트 생성 로직
  period_str = "#{@target_start.strftime('%Y.%m.%d')} - #{@target_end.strftime('%Y.%m.%d')}"
  
  # 1. 단톡방용 요약 문구
  summary_text = "[루틴 파인더스 #{@report_type == 'weekly' ? '주간' : '월간'} 정산]\n"
  summary_text += "평가 기간: #{period_str}\n\n"
  
  # 1-1. 100% Perfect (Legend)
  legends = @reports.select { |r| r.achievement_rate >= 100.0 }
  if legends.any?
    summary_text += "💯 100% Perfect (Legend)\n"
    summary_text += legends.map { |r| "• #{r.user.nickname} (100%)" }.join("\n") + "\n\n"
  end

  # 1-2. 90% 이상 (Super Achiever)
  supers = @reports.select { |r| r.achievement_rate >= 90.0 && r.achievement_rate < 100.0 }
  if supers.any?
    summary_text += "🔥 90% 이상 (Super Achiever)\n"
    summary_text += supers.map { |r| "• #{r.user.nickname} (#{r.achievement_rate.to_i}%)" }.join("\n") + "\n\n"
  end

  # 1-3. 70% ~ 89% (Safe Zone)
  safes = @reports.select { |r| r.achievement_rate >= 70.0 && r.achievement_rate < 90.0 }
  if safes.any?
    summary_text += "✅ 70% ~ 89% (Safe Zone)\n"
    summary_text += safes.map { |r| "• #{r.user.nickname} (#{r.achievement_rate.to_i}%)" }.join("\n") + "\n\n"
  end

  # 1-4. 70% 미만 (Manager's Care)
  cares = @reports.select { |r| r.achievement_rate < 70.0 }
  if cares.any?
    summary_text += "⚠️ 70% 미만 (Manager's Care)\n"
    summary_text += cares.map { |r| 
      status_label = if r.actual_penalty_issued
        " / 경고부여"
      elsif r.exclusion_reason.present?
        " / 평가제외(#{r.exclusion_reason})"
      else
        " / 평가제외"
      end
      "• #{r.user.nickname} (#{r.achievement_rate.to_i}%#{status_label})" 
    }.join("\n") + "\n\n"
  end
  
  summary_text += "*시너지 포인트 순으로 정렬되었습니다.*\n"
  summary_text += "꾸준함이 비범함을 만듭니다. 오늘도 파이팅! 🚀"
  
  # 2. 상세형 리포트 전체 복사
  detailed_text = "[상세 성취 리포트 - #{@report_type == 'weekly' ? '주간' : '월간'}]\n"
  detailed_text += "기간: #{period_str}\n\n"
  
  @reports.each_with_index do |r, i|
    detailed_text += "#{i+1}. #{r.user.nickname} (#{r.achievement_rate.to_i}%)\n"
    detailed_text += "- 등급: #{r.identity_title}\n"
    detailed_text += "- 패스사용: R:#{r.relax_pass_count.to_i}, S:#{r.save_pass_count.to_i}\n"
    detailed_text += "- 요약: #{r.summary}\n\n"
  end
%>

<div class="min-h-screen bg-[#0C0B12] pb-32" 
     data-controller="batch-sharing"
     data-batch-sharing-summary-value="<%= summary_text %>"
     data-batch-sharing-detailed-value="<%= detailed_text %>">
  
  <!-- Header -->
  <header class="sticky top-0 z-50 bg-[#0C0B12]/80 backdrop-blur-md border-b border-white/5 px-6 py-4 flex items-center justify-between">
    <div class="flex items-center gap-3">
      <%= link_to prototype_admin_clubs_path, class: "w-10 h-10 rounded-xl bg-white/5 flex items-center justify-center text-slate-400 hover:text-white transition-colors" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/></svg>
      <% end %>
      <div>
        <h1 class="text-lg font-black text-white">멤버 성취 리포트</h1>
        <p class="text-[10px] font-bold text-slate-500 uppercase tracking-widest"><%= @report_type.capitalize %> Performance Feed</p>
      </div>
    </div>
    <div class="flex gap-1.5">
      <%= link_to prototype_batch_reports_path(type: "weekly"), class: "px-3 py-1.5 rounded-lg text-[10px] font-black transition-all #{@report_type == 'weekly' ? 'bg-indigo-500 text-white shadow-lg shadow-indigo-500/20' : 'bg-white/5 text-slate-500 hover:text-slate-300'}" do %>
        주간
      <% end %>
      <%= link_to prototype_batch_reports_path(type: "monthly"), class: "px-3 py-1.5 rounded-lg text-[10px] font-black transition-all #{@report_type == 'monthly' ? 'bg-indigo-500 text-white shadow-lg shadow-indigo-500/20' : 'bg-white/5 text-slate-500 hover:text-slate-300'}" do %>
        월간
      <% end %>
    </div>
  </header>

  <main class="px-6 py-8 space-y-8">
    <!-- Batch Sharing UI (Retrieved from capturing image) -->
    <section class="text-center space-y-6 py-4">
      <div class="space-y-2">
        <div class="inline-flex px-3 py-1 rounded-full bg-emerald-500/10 border border-emerald-500/20">
          <span class="text-[9px] font-black text-emerald-500 uppercase tracking-widest">Batch Sharing Mode</span>
        </div>
        <h2 class="text-2xl font-black text-white leading-tight">전체 리포트 통합 요약</h2>
        <p class="text-xs text-slate-500 font-medium px-4 leading-relaxed">
          최근 생성된 모든 멤버의 리포트를 한 화면에서 확인하고<br/>
          단톡방 공유를 위해 텍스트로 빠르게 복사합니다.
        </p>
      </div>

      <div class="space-y-3 px-2">
        <button data-action="click->batch-sharing#copySummary" 
                class="w-full py-4 rounded-2xl bg-indigo-600 text-white font-black text-sm shadow-xl shadow-indigo-600/20 active:scale-[0.98] transition-all flex items-center justify-center gap-3">
          <span class="text-xl">📊</span>
          단톡방용 요약 문구 복사
        </button>
        <button data-action="click->batch-sharing#copyDetailed" 
                class="w-full py-4 rounded-2xl bg-white/5 text-slate-300 font-black text-sm border border-white/5 active:scale-[0.98] transition-all hover:bg-white/10">
          상세형 리포트 전체 복사
        </button>
      </div>
    </section>

    <div class="h-px bg-white/5 mx-2"></div>

    <!-- Period Info -->
    <div class="p-6 rounded-3xl bg-indigo-500/5 border border-indigo-500/20 flex items-center justify-between">
      <div>
        <p class="text-[10px] font-black text-indigo-400 uppercase tracking-widest mb-1 italic">Evaluation Period</p>
        <h2 class="text-sm font-black text-white">
          <%= period_str %>
        </h2>
      </div>
      <div class="text-right">
        <p class="text-[10px] font-black text-slate-500 uppercase tracking-widest mb-1 italic">Total Members</p>
        <p class="text-sm font-black text-white"><%= @reports.count %>명</p>
      </div>
    </div>

    <!-- List Header -->
    <div class="flex items-center justify-between">
      <h3 class="text-xs font-black text-slate-400 uppercase tracking-[0.2em] flex items-center gap-2">
        <span class="w-1 h-1 rounded-full bg-indigo-500 shadow-[0_0_8px_rgba(99,102,241,0.8)]"></span>
        <%= @report_type.upcase %> RANKING
      </h3>
    </div>

    <div class="space-y-3" id="reports-container">
      <% if @reports.any? %>
        <% @reports.each_with_index do |report, index| %>
          <div class="app-glass-card p-5 border-white/5 bg-white/[0.02] flex items-center justify-between">
            <div class="flex items-center gap-4">
               <span class="text-xs font-black <%= index < 3 ? 'text-amber-500' : 'text-slate-600' %> w-4 font-mono">#<%= (index + 1).to_s.rjust(2, '0') %></span>
               <div class="w-10 h-10 rounded-xl bg-gradient-to-br from-indigo-500/20 to-purple-500/20 flex items-center justify-center text-white font-black text-xs border border-white/5">
                 <%= report.user.nickname[0].upcase %>
               </div>
               <div>
                 <h4 class="text-sm font-black text-white"><%= report.user.nickname %></h4>
                 <div class="flex items-center gap-2 mt-0.5">
                   <p class="text-[9px] font-bold text-slate-500"><%= report.identity_title %></p>
                   <div class="flex items-center gap-1 opacity-60">
                     <% if report.score.to_i > 0 %>
                       <span class="text-[8px] font-bold px-1 rounded bg-amber-500/10 text-amber-500/80 border border-amber-500/10"><%= report.score.to_i %> pts</span>
                     <% end %>
                     <% if report.relax_pass_count.to_i > 0 %>
                       <span class="text-[8px] font-bold px-1 rounded bg-indigo-500/20 text-indigo-400 border border-indigo-500/20">R:<%= report.relax_pass_count %></span>
                     <% end %>
                     <% if report.save_pass_count.to_i > 0 %>
                       <span class="text-[8px] font-bold px-1 rounded bg-emerald-500/20 text-emerald-400 border border-emerald-500/20">S:<%= report.save_pass_count %></span>
                     <% end %>
                   </div>
                 </div>
               </div>
            </div>

            <div class="text-right">
              <p class="text-lg font-black <%= report.achievement_rate >= 80 ? 'text-emerald-400' : (report.achievement_rate < 70 ? 'text-rose-500' : 'text-white') %> tabular-nums italic">
                <%= report.achievement_rate.to_i %><small class="text-xs font-bold opacity-50 ml-0.5">%</small>
              </p>
              
              <% if report.has_low_rate %>
                <div class="mt-1">
                  <% if report.is_eligible %>
                    <span class="px-1.5 py-0.5 rounded bg-rose-500/20 text-[8px] font-black text-rose-500 uppercase tracking-tighter border border-rose-500/30">경고 부여됨</span>
                  <% else %>
                    <% if !report.is_eligible_date %>
                      <span class="px-1.5 py-0.5 rounded bg-slate-500/20 text-[8px] font-black text-slate-400 uppercase tracking-tighter border border-white/5">신규 유예 기간</span>
                    <% else %>
                      <span class="px-1.5 py-0.5 rounded bg-slate-500/10 text-[8px] font-black text-slate-500 uppercase tracking-tighter border border-white/5">평가 제외 대상</span>
                    <% end %>
                  <% end %>
                </div>
              <% end %>
            </div>
          </div>
        <% end %>
      <% else %>
        <div class="py-20 text-center border border-dashed border-white/10 rounded-3xl">
          <p class="text-slate-500 font-bold">리포트가 존재하지 않습니다.</p>
        </div>
      <% end %>
    </div>
  </main>
</div>
