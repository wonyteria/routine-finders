<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-slate-50 to-indigo-50 p-4">
  <div class="max-w-md w-full bg-white rounded-[48px] shadow-2xl p-8 md:p-12">
    <!-- Icon -->
    <div class="w-20 h-20 mx-auto mb-6 rounded-[24px] bg-amber-50 flex items-center justify-center">
      <svg class="w-10 h-10 text-amber-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
      </svg>
    </div>

    <!-- Title -->
    <h1 class="text-2xl md:text-3xl font-black text-slate-900 text-center mb-3">
      탈퇴한 계정 발견
    </h1>
    
    <p class="text-slate-600 text-center mb-8">
      이전에 탈퇴한 계정이 있습니다.<br>
      계정을 복구하시겠습니까?
    </p>

    <!-- User Info -->
    <% if @user %>
      <div class="bg-slate-50 rounded-[24px] p-6 mb-8">
        <div class="flex items-center gap-4">
          <img src="<%= @user.profile_image %>" class="w-16 h-16 rounded-[20px] object-cover border-4 border-white shadow-lg">
          <div>
            <p class="font-black text-slate-900"><%= @user.nickname %></p>
            <p class="text-sm text-slate-500"><%= @user.email %></p>
            <p class="text-xs text-slate-400 mt-1">
              탈퇴일: <%= @user.deleted_at&.strftime("%Y년 %m월 %d일") %>
            </p>
          </div>
        </div>
      </div>
    <% end %>

    <!-- Actions -->
    <div class="space-y-3">
      <%= button_to "계정 복구하기", confirm_restore_account_path, method: :post, class: "w-full py-4 bg-indigo-600 text-white rounded-[20px] font-black text-sm shadow-xl shadow-indigo-100 hover:bg-indigo-700 transition-all" %>
      
      <%= link_to "새 계정으로 가입", root_path, class: "block w-full py-4 bg-slate-100 text-slate-600 rounded-[20px] font-black text-sm text-center hover:bg-slate-200 transition-all" %>
    </div>

    <p class="text-xs text-slate-400 text-center mt-6">
      계정을 복구하면 이전 데이터가 모두 복원됩니다.<br>
      새 계정으로 가입하면 완전히 새로운 계정이 생성됩니다.
    </p>
  </div>
</div>
