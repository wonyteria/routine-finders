<div class="max-w-4xl mx-auto px-6 py-20 space-y-12">
  <header class="space-y-4">
    <div class="inline-flex items-center gap-2 bg-indigo-50 px-4 py-2 rounded-full border border-indigo-100">
      <span class="text-[10px] font-black uppercase tracking-[.2em] text-indigo-600">Archive</span>
    </div>
    <h1 class="text-4xl md:text-5xl font-black text-slate-900 tracking-tight">루파 리포트 보관함</h1>
    <p class="text-lg text-slate-500 font-medium">당신이 걸어온 성장의 발자취입니다.</p>
  </header>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
    <% if @reports.any? %>
      <% @reports.each do |report| %>
        <%= link_to routine_club_report_path(report), class: "group bg-white rounded-[40px] p-10 border border-slate-100 shadow-sm hover:shadow-2xl hover:-translate-y-2 transition-all relative overflow-hidden" do %>
          <div class="absolute -right-4 -top-4 w-24 h-24 bg-indigo-50 rounded-full blur-2xl group-hover:bg-indigo-100 transition-colors"></div>
          
          <div class="relative z-10 space-y-6">
            <div class="flex justify-between items-center">
              <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest leading-none">
                <%= report.start_date.strftime("%Y.%m") %>
              </p>
              <span class="px-3 py-1 bg-indigo-600 text-white rounded-full text-[9px] font-black uppercase tracking-wider">Monthly</span>
            </div>
            
            <div class="space-y-2">
              <h3 class="text-2xl font-black text-slate-900 group-hover:text-indigo-600 transition-colors"><%= report.start_date.strftime("%B") %> Report</h3>
              <p class="text-sm font-bold text-slate-400 line-clamp-1"><%= report.identity_title %></p>
            </div>

            <div class="pt-6 border-t border-slate-50 flex items-center justify-between">
              <div class="flex gap-4">
                <div class="flex flex-col">
                  <span class="text-[9px] font-black text-slate-300 uppercase leading-none mb-1">Log</span>
                  <span class="text-sm font-black text-slate-900"><%= report.log_rate.to_i %>%</span>
                </div>
                <div class="flex flex-col">
                  <span class="text-[9px] font-black text-slate-300 uppercase leading-none mb-1">Achieve</span>
                  <span class="text-sm font-black text-slate-900"><%= report.achievement_rate.to_i %>%</span>
                </div>
              </div>
              <div class="w-10 h-10 bg-indigo-50 text-indigo-600 rounded-xl flex items-center justify-center transition-all group-hover:bg-indigo-600 group-hover:text-white">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M14 5l7 7m0 0l-7 7m7-7H3"/></svg>
              </div>
            </div>
          </div>
        <% end %>
      <% end %>
    <% else %>
      <div class="col-span-full py-32 text-center bg-slate-50 rounded-[48px] border-2 border-dashed border-slate-200 space-y-6">
        <div class="w-20 h-20 bg-white rounded-3xl shadow-sm flex items-center justify-center text-4xl mx-auto">📂</div>
        <div class="space-y-2">
          <p class="text-xl font-black text-slate-900">아직 생성된 리포트가 없습니다.</p>
          <p class="text-sm font-bold text-slate-400">루파 클럽에서 성실함을 증명하고 첫 리포트를 받아보세요.</p>
        </div>
        <div class="pt-4">
          <%= button_to generate_current_routine_club_reports_path, method: :post, class: "inline-flex px-8 py-4 bg-indigo-600 text-white rounded-2xl font-black text-sm hover:bg-indigo-700 transition-all shadow-xl shadow-indigo-100 ring-4 ring-white" do %>
            테스트용 리포트 생성하기
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>
