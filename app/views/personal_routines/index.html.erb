<div class="min-h-screen bg-slate-50">
  <div class="max-w-[1600px] mx-auto px-4 sm:px-6 lg:px-10 py-12">
    
    <!-- Main Banner (Threads Aesthetic) -->
    <section class="mb-12 relative overflow-hidden rounded-threads-lg bg-threads-black text-white shadow-2xl shadow-indigo-200/20">
      <div class="absolute inset-0 bg-[url('https://www.transparenttextures.com/patterns/cubes.png')] opacity-10" loading="eager"></div>
      
      <!-- Abstract Gradients -->
      <div class="absolute top-0 right-0 w-96 h-96 bg-indigo-500/10 rounded-full blur-3xl -translate-y-1/2 translate-x-1/2"></div>
      <div class="absolute bottom-0 left-0 w-64 h-64 bg-slate-500/10 rounded-full blur-3xl translate-y-1/2 -translate-x-1/2"></div>

      <div class="relative z-10 p-6 md:p-16 flex flex-col md:flex-row items-center justify-between gap-8 md:gap-10">
        <div class="space-y-4 md:space-y-6 max-w-xl text-center md:text-left">
          <div class="space-y-2">
            <span class="inline-block px-4 py-1.5 rounded-full bg-white/10 backdrop-blur-md text-[10px] font-black uppercase tracking-[0.2em] border border-white/20">Daily Discipline</span>
            <h1 class="text-4xl md:text-6xl font-black leading-tight tracking-tighter">
              매일의 성취,<br/><span class="text-slate-400">당신의 루틴.</span>
            </h1>
            <p class="text-sm md:text-lg font-bold text-slate-400 leading-relaxed">자신만의 속도로 차근차근 나아가세요.<br/>기록된 오늘이 내일의 당신을 만듭니다.</p>
          </div>
        </div>

        <div class="grid grid-cols-2 md:grid-cols-2 gap-3 md:gap-4 w-full md:w-auto shrink-0">
          <div class="bg-white/5 backdrop-blur-xl border border-white/10 p-4 md:p-8 rounded-[24px] md:rounded-threads-lg text-center shadow-inner">
            <p class="text-[8px] md:text-[10px] font-black text-slate-500 uppercase tracking-widest mb-2 md:mb-3">Today Status</p>
            <% current_wday = Date.current.wday.to_s %>
            <% total_routines_today = (@personal_routines || []).select { |r| (r.days || []).include?(current_wday) }.count %>
            <% completed_routines_today = (@personal_routines || []).select { |r| r.completed_today? && (r.days || []).include?(current_wday) }.count %>
            <div class="flex items-baseline justify-center gap-1">
              <p class="text-2xl md:text-4xl font-black text-white"><%= completed_routines_today %></p>
              <p class="text-xs md:text-lg font-black text-slate-600">/ <%= total_routines_today %></p>
            </div>
            <div class="mt-2 md:mt-4 w-full bg-white/10 h-1 rounded-full overflow-hidden">
              <% percentage = total_routines_today > 0 ? (completed_routines_today.to_f / total_routines_today * 100) : 0 %>
              <div class="bg-white h-full transition-all duration-1000" style="width: <%= percentage %>%"></div>
            </div>
          </div>
          
          <div class="bg-white p-4 md:p-8 rounded-[24px] md:rounded-threads-lg text-center shadow-2xl">
            <p class="text-[8px] md:text-[10px] font-black text-slate-400 uppercase tracking-widest mb-2 md:mb-3">Best Streak</p>
            <p class="text-2xl md:text-4xl font-black text-threads-black">
              <%= (@personal_routines || []).map(&:current_streak).max || 0 %><span class="text-sm md:text-xl ml-0.5">🔥</span>
            </p>
            <p class="text-[8px] md:text-[10px] font-bold text-slate-400 mt-1 md:mt-2">Days in a row</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Tabs Only (Sidebar is inside partials to avoid duplication) -->
    <div class="flex justify-center md:justify-start mb-8">
      <div class="bg-indigo-50/50 p-1.5 rounded-[24px] flex w-full md:w-auto md:inline-flex gap-1.5 border border-indigo-100/50">
        <%= link_to "무료 루틴", personal_routines_path(tab: 'free'), 
            class: "flex-1 md:flex-none px-8 py-4 rounded-[20px] font-black text-sm transition-all duration-300 #{ (params[:tab].blank? || params[:tab] == 'free') ? 'bg-indigo-600 text-white shadow-xl shadow-indigo-100 scale-105 z-10' : 'text-slate-400 hover:text-slate-600' }" %>
        
        <%= link_to "루파 클럽", personal_routines_path(tab: 'club'), 
            class: "flex-1 md:flex-none px-8 py-4 rounded-[20px] font-black text-sm transition-all duration-300 #{ params[:tab] == 'club' ? 'bg-indigo-600 text-white shadow-xl shadow-indigo-100 scale-105 z-10' : 'text-slate-400 hover:text-slate-600' }" %>
      </div>
    </div>

    <% if params[:tab].blank? || params[:tab] == 'free' %>
      <div id="panel-free">
        <%= render 'free_routines' %>
      </div>
    <% end %>

    <% if params[:tab] == 'club' %>
      <div id="panel-club">
        <%= render 'club_routines' %>
      </div>
    <% end %>

    <!-- Script removed as tabs are now handled by server-side navigation -->
  </div>
</div>
