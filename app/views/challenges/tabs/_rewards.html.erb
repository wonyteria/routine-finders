<div class="space-y-10">
  <% 
    reward_policy_data = challenge.reward_policy
    if reward_policy_data.is_a?(String)
      begin
        reward_policy_data = JSON.parse(reward_policy_data)
        # Handle potential double-encoding
        reward_policy_data = JSON.parse(reward_policy_data) if reward_policy_data.is_a?(String)
      rescue
        reward_policy_data = []
      end
    end
    reward_policy_data = [] unless reward_policy_data.is_a?(Array)
  %>

  <!-- Winner Rewards -->
  <% if reward_policy_data.any? %>
    <div class="space-y-6">
      <div class="flex items-center gap-4">
        <div class="w-12 h-12 rounded-2xl bg-amber-50 flex items-center justify-center text-amber-600">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2M17 16v2a2 2 0 01-2 2H9a2 2 0 01-2-2v-2m3-5v3m4-3v3m-2-6V5a2 2 0 114 0v2"/></svg>
        </div>
        <div>
          <h2 class="text-2xl font-black text-slate-900">🏆 우승 혜택</h2>
          <p class="text-sm font-medium text-slate-400">챌린지를 성공적으로 완료한 상위 참여자에게 주어지는 특별 보상</p>
        </div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <% reward_policy_data.each_with_index do |reward, index| %>
          <div class="relative overflow-hidden group">
            <div class="absolute -right-8 -top-8 w-32 h-32 bg-gradient-to-br from-amber-400/10 to-orange-400/10 rounded-full blur-2xl group-hover:scale-150 transition-transform duration-500"></div>
            <div class="relative p-8 bg-gradient-to-br from-amber-50 to-orange-50 rounded-[32px] border border-amber-100 space-y-4 hover:shadow-xl hover:shadow-amber-500/10 transition-all">
              <div class="flex items-center gap-3">
                <div class="w-12 h-12 rounded-2xl bg-gradient-to-br from-amber-500 to-orange-500 text-white flex items-center justify-center text-2xl shadow-lg shadow-amber-500/30">
                  <% if reward["rank"].to_s.include?("1") || reward["rank"].to_s.downcase.include?("first") %>
                    🥇
                  <% elsif reward["rank"].to_s.include?("2") || reward["rank"].to_s.downcase.include?("second") %>
                    🥈
                  <% elsif reward["rank"].to_s.include?("3") || reward["rank"].to_s.downcase.include?("third") %>
                    🥉
                  <% else %>
                    🎁
                  <% end %>
                </div>
                <div>
                  <p class="text-xs font-bold text-amber-600 uppercase tracking-wider">순위</p>
                  <p class="text-lg font-black text-slate-900"><%= reward['rank'] %></p>
                </div>
              </div>
              
              <div class="pt-4 border-t border-amber-200/50">
                <p class="text-xs font-bold text-amber-700 mb-2">혜택</p>
                <p class="text-2xl font-black text-amber-600 mb-3"><%= reward['prize'] %></p>
              </div>
            </div>
          </div>
        <% end %>
      </div>

      <!-- Reward Info -->
      <div class="p-8 bg-gradient-to-br from-indigo-50 to-purple-50 rounded-[32px] border border-indigo-100">
        <div class="flex items-start gap-4">
          <div class="w-10 h-10 rounded-xl bg-white shadow-sm flex items-center justify-center text-indigo-600 shrink-0">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          </div>
          <div class="space-y-3">
            <p class="text-sm font-black text-indigo-900">혜택 지급 안내</p>
            <ul class="space-y-2 text-sm font-medium text-slate-600">
              <li class="flex items-start gap-2">
                <span class="text-indigo-500 mt-0.5">•</span>
                <span>우승 혜택은 챌린지 종료 후 호스트가 직접 지급합니다.</span>
              </li>
              <li class="flex items-start gap-2">
                <span class="text-indigo-500 mt-0.5">•</span>
                <span>순위는 달성률, 연속 달성일 등을 종합하여 결정됩니다.</span>
              </li>
              <li class="flex items-start gap-2">
                <span class="text-indigo-500 mt-0.5">•</span>
                <span>혜택 지급과 관련된 모든 책임은 호스트에게 있으며, Routine Finders는 개입하지 않습니다.</span>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  <% else %>
    <!-- No Winner Rewards -->
    <div class="text-center py-12">
      <div class="w-20 h-20 rounded-full bg-slate-100 flex items-center justify-center mx-auto mb-6">
        <svg class="w-10 h-10 text-slate-300" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2M17 16v2a2 2 0 01-2 2H9a2 2 0 01-2-2v-2m3-5v3m4-3v3m-2-6V5a2 2 0 114 0v2"/></svg>
      </div>
      <h3 class="text-xl font-black text-slate-900 mb-2">설정된 우승 혜택이 없습니다</h3>
      <p class="text-sm font-medium text-slate-400">이 챌린지는 별도의 우승 혜택이 제공되지 않습니다.</p>
    </div>
  <% end %>

  <!-- Deposit Refund Criteria (for deposit-type challenges) -->
  <% unless challenge.cost_type_free? %>
    <div class="space-y-6">
      <div class="flex items-center gap-4">
        <div class="w-12 h-12 rounded-2xl bg-emerald-50 flex items-center justify-center text-emerald-600">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
        </div>
        <div>
          <h2 class="text-2xl font-black text-slate-900">💰 <%= challenge.cost_type_deposit? ? '보증금 환급 기준' : '참가비 혜택' %></h2>
          <p class="text-sm font-medium text-slate-400">목표 달성 시 받을 수 있는 기본 혜택</p>
        </div>
      </div>

      <% if challenge.cost_type_deposit? && challenge.full_refund_threshold.present? %>
        <!-- Deposit Refund -->
        <div class="p-8 bg-gradient-to-br from-emerald-50 to-teal-50 rounded-[32px] border-2 border-emerald-100">
          <div class="flex items-center justify-between mb-6">
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 rounded-2xl bg-emerald-500 text-white flex items-center justify-center shadow-lg shadow-emerald-500/30">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
              </div>
              <div>
                <p class="text-sm font-black text-emerald-700 uppercase tracking-wider">전액 환급 기준</p>
                <p class="text-[10px] font-bold text-emerald-600/60">달성률이 이 기준 이상이면 보증금 100% 환급</p>
              </div>
            </div>
            <div class="flex items-center gap-2">
              <span class="text-6xl font-black text-emerald-600"><%= (challenge.full_refund_threshold * 100).to_i %></span>
              <span class="text-3xl font-black text-emerald-600">%</span>
            </div>
          </div>
          
          <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
            <div class="p-6 bg-white/60 rounded-2xl border border-emerald-100/50 text-center">
              <p class="text-xs text-emerald-600 font-bold mb-2">100% 달성 시</p>
              <p class="text-lg font-black text-emerald-900">전액 환급</p>
            </div>
            <div class="p-6 bg-white/60 rounded-2xl border border-emerald-100/50 text-center">
              <p class="text-xs text-emerald-600 font-bold mb-2"><%= (challenge.full_refund_threshold * 100).to_i %>% 이상 달성 시</p>
              <p class="text-lg font-black text-emerald-900">전액 환급</p>
            </div>
            <div class="p-6 bg-white/60 rounded-2xl border border-emerald-100/50 text-center">
              <p class="text-xs text-emerald-600 font-bold mb-2"><%= (challenge.full_refund_threshold * 100).to_i %>% 미만 달성 시</p>
              <p class="text-lg font-black text-emerald-900">달성률만큼 환급</p>
            </div>
          </div>

          <div class="p-4 bg-white/60 rounded-2xl border border-emerald-100/50">
            <p class="text-[11px] font-bold text-slate-600 leading-relaxed">
              💡 <span class="text-emerald-700">달성 기준:</span> 챌린지 기간 중 <span class="text-emerald-700 font-black"><%= (challenge.full_refund_threshold * 100).to_i %>%</span> 이상 달성한 참여자는 보증금을 전액 돌려받습니다.
            </p>
          </div>
        </div>

        <!-- Refund Timeline -->
        <div class="p-6 bg-slate-50 rounded-2xl border border-slate-100">
          <p class="text-xs text-slate-500 font-medium leading-relaxed">
            <% if challenge.refund_date.present? %>
              📅 <span class="font-bold text-slate-700">환급 예정일:</span> <%= challenge.refund_date.strftime('%Y년 %m월 %d일') %>에 보증금 환급이 완료됩니다.
            <% else %>
              📅 <span class="font-bold text-slate-700">환급 일정:</span> 챌린지 종료 후 영업일 기준 3~5일 이내에 환급이 처리됩니다.
            <% end %>
          </p>
        </div>
      <% else %>
        <!-- Fee Type -->
        <div class="p-8 bg-gradient-to-br from-indigo-50 to-purple-50 rounded-[32px] border border-indigo-100">
          <div class="text-center space-y-4">
            <div class="w-16 h-16 rounded-2xl bg-indigo-500 text-white flex items-center justify-center mx-auto shadow-lg shadow-indigo-500/30">
              <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2"/></svg>
            </div>
            <div>
              <p class="text-3xl font-black text-indigo-600 mb-2">참가비형 챌린지</p>
              <p class="text-sm font-medium text-slate-600">
                이 챌린지는 참가비를 지불하고 참여하는 방식입니다.<br>
                별도의 환급 정책은 없으며, 우승 혜택만 제공됩니다.
              </p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
