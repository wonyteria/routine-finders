<%# Always use prototype experience %>
<% content_for :hide_nav, true %>
<style>
  .glass-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(20px);
    border-radius: 2.5rem;
  }
  .glass-btn {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 1.25rem;
    transition: all 0.3s cubic-bezier(0.2, 1, 0.2, 1);
  }
  .glass-btn:active { transform: scale(0.95); background: rgba(255, 255, 255, 0.1); }
  .glow-indigo { box-shadow: 0 0 30px rgba(99, 102, 241, 0.15); }
  .tab-active { color: white; position: relative; }
  .tab-active::after { content: ''; position: absolute; bottom: -8px; left: 50%; transform: translateX(-50%); width: 4px; height: 4px; background: #818cf8; border-radius: 50%; box-shadow: 0 0 10px #818cf8; }
  
  @keyframes pulse-glow {
    0%, 100% { opacity: 0.5; transform: scale(1); }
    50% { opacity: 1; transform: scale(1.05); }
  }
  .pulse-glow { animation: pulse-glow 2s infinite; }

  .hide-scrollbar::-webkit-scrollbar { display: none; }

  /* Theme Overrides for Partials */
  .tab-content-env .bg-white, .tab-content-env .bg-slate-50, .tab-content-env .bg-indigo-50, .tab-content-env .bg-orange-50, .tab-content-env .bg-emerald-50, .tab-content-env .bg-amber-50 { 
    background: rgba(255, 255, 255, 0.03) !important; 
    border: 1px solid rgba(255, 255, 255, 0.08) !important;
  }
  .tab-content-env .text-slate-900, .tab-content-env .text-orange-900, .tab-content-env .text-emerald-900, .tab-content-env .text-amber-900 { color: #ffffff !important; }
  .tab-content-env .text-slate-600, .tab-content-env .text-slate-700, .tab-content-env .text-emerald-600, .tab-content-env .text-amber-600 { color: #a1a1aa !important; }
  .tab-content-env .text-slate-500 { color: #71717a !important; }

  @keyframes slide-up {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .animate-slide-up {
    animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }
</style>

<div class="flex flex-col bg-[#0C0B12] text-white min-h-screen relative -mx-4 pb-32 selection:bg-indigo-500/30">
  <!-- Top Nav (Floating Glass) -->
  <header class="sticky top-0 z-[100] px-6 py-8 bg-[#0C0B12]/80 backdrop-blur-3xl border-b border-white/5 flex items-center justify-between">
    <%= link_to (params[:source] == 'prototype' ? prototype_explore_path : :back), class: "w-11 h-11 rounded-2xl bg-white/[0.03] border border-white/10 flex items-center justify-center text-white/60 active:scale-90 transition-all shadow-inner" do %>
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M15 19l-7-7 7-7"/></svg>
    <% end %>
    <div class="flex-1 text-center px-4 min-w-0">
      <h1 class="text-[15px] font-black truncate tracking-tight text-white"><%= @challenge.title %></h1>
      <div class="flex items-center justify-center gap-1.5 mt-1">
        <span class="w-1 h-1 rounded-full bg-indigo-500 shadow-[0_0_5px_rgba(99,102,241,1)]"></span>
        <p class="text-[10px] font-black text-indigo-400 uppercase tracking-widest"><%= @d_day %></p>
      </div>
    </div>
    <div class="flex items-center gap-2">
      <% if current_user&.admin? %>
        <%= link_to challenge_path(@challenge), 
                    method: :delete,
                    data: { 
                      turbo_method: :delete,
                      turbo_confirm: "'#{@challenge.title}' 챌린지를 삭제하시겠습니까?" 
                    },
                    class: "w-11 h-11 rounded-2xl bg-rose-500/10 border border-rose-500/20 flex items-center justify-center text-rose-500 active:scale-90 transition-all shadow-lg shadow-rose-500/5" do %>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/></svg>
        <% end %>
      <% end %>
      <button 
        data-controller="share"
        data-share-title-value="<%= @challenge.title %>"
        data-share-text-value="루틴 파인더에서 '<%= @challenge.title %>' 도전에 함께해요! 🌿"
        data-share-url-value="<%= challenge_url(@challenge) %>"
        data-action="click->share#share"
        class="w-11 h-11 rounded-2xl bg-white/[0.03] border border-white/10 flex items-center justify-center text-white/60 active:scale-90 transition-all">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z"/></svg>
      </button>
    </div>
  </header>

  <div class="px-6 space-y-10 pt-6">
    <!-- 1. Hero: Strategic Mission Center -->
    <section class="relative">
      <% if @challenge.offline? %>
        <!-- GATHERING MODE (Premium Event Card) -->
        <div class="absolute -top-10 -left-10 w-48 h-48 bg-emerald-500/10 blur-[80px] pointer-events-none"></div>
        <div class="app-glass-card-premium p-8 relative overflow-hidden group border-emerald-500/20 bg-emerald-500/5">
          <div class="absolute top-0 right-[-10%] p-6 opacity-5 group-hover:scale-110 transition-transform duration-1000 grayscale">
             <span class="text-9xl">📍</span>
          </div>

          <div class="space-y-8 relative z-10">
            <div class="flex items-center gap-2">
              <span class="w-1.5 h-1.5 rounded-full bg-emerald-500 shadow-[0_0_8px_rgba(16,185,129,0.8)]"></span>
              <span class="text-[11px] font-black text-emerald-400 uppercase tracking-[0.2em] leading-none">Upcoming Event</span>
            </div>

            <div class="space-y-3">
              <p class="text-[11px] font-black text-white/40 uppercase tracking-widest leading-none">Next Meeting</p>
              <h2 class="text-3xl font-black leading-none text-white tracking-tighter">
                <%= @challenge.start_date.strftime('%m.%d') %> 
                <span class="text-emerald-400 ml-1"><%= @challenge.meeting_info&.meeting_time %></span>
              </h2>
              <div class="flex items-center gap-2.5 text-white/60">
                <svg class="w-4 h-4 text-emerald-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
                <p class="text-[15px] font-bold"><%= @challenge.meeting_info&.place_name || "장소 조율 중" %></p>
              </div>
            </div>
            
            <div class="flex gap-3">
              <% if @challenge.meeting_info&.place_url.present? || @challenge.meeting_info&.address.present? %>
                <% 
                  map_url = @challenge.meeting_info.place_url.presence || 
                  "https://www.google.com/maps/search/?api=1&query=#{CGI.escape(@challenge.meeting_info.address)}"
                %>
                <%= link_to map_url, target: "_blank", class: "flex-1 py-4.5 bg-white/5 hover:bg-white/10 rounded-2xl text-[13px] font-black text-white transition-all border border-white/5 flex items-center justify-center gap-2 active:scale-95 shadow-xl" do %>
                   📍 가이드 맵
                <% end %>
              <% end %>
              
              <% if @challenge.chat_link.present? %>
                <%= link_to @challenge.chat_link, target: "_blank", class: "flex-1 py-4.5 bg-emerald-500 rounded-2xl text-[13px] font-black text-white transition-all shadow-lg shadow-emerald-500/20 flex items-center justify-center gap-2 active:scale-95" do %>
                   💬 커뮤니티 입방
                <% end %>
              <% end %>
            </div>
          </div>
        </div>

      <% else %>
        <!-- CHALLENGE MODE (Strategic Mission Dashboard) -->
        <div class="absolute -top-10 -left-10 w-48 h-48 bg-indigo-500/10 blur-[80px] pointer-events-none"></div>
        <div class="app-glass-card-premium p-10 relative overflow-hidden group min-h-[220px] flex flex-col justify-center">
          <!-- Background Decoration -->
          <div class="absolute top-0 right-[-5%] p-6 opacity-5 group-hover:scale-110 transition-transform duration-1000 grayscale pointer-events-none">
             <span class="text-[120px]">🔥</span>
          </div>

          <div class="space-y-8 relative z-10">
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-2">
                <span class="w-1.5 h-1.5 rounded-full bg-indigo-500 shadow-[0_0_8px_rgba(99,102,241,1)]"></span>
                <span class="text-[11px] font-black text-indigo-400 uppercase tracking-[0.2em] leading-none">Victory Objective</span>
              </div>
              <% unless @today_verified %>
                <span class="px-2 py-0.5 rounded bg-rose-500/10 text-rose-500 text-[9px] font-black tracking-tighter animate-pulse border border-rose-500/20">AWAITING ACTION</span>
              <% end %>
            </div>

            <div class="space-y-3">
              <h2 class="text-2xl font-black leading-tight text-white tracking-tight">
                <%= @challenge.summary.presence || "오늘의 성취를 향해 나아가세요" %>
              </h2>
              <p class="text-xs font-bold text-white/30 italic leading-relaxed">
                "위대한 여정도 오늘 하나의 루틴에서 시작됩니다."
              </p>
            </div>

            <% if @today_verified %>
              <div class="pt-4 flex items-center gap-5">
                <div class="w-14 h-14 rounded-[22px] bg-emerald-500/10 flex items-center justify-center text-emerald-400 border border-emerald-500/20 shadow-lg shadow-emerald-500/5">
                  <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"/></svg>
                </div>
                <div>
                  <p class="text-base font-black text-white leading-none mb-2">오늘의 미션 완료</p>
                  <p class="text-[11px] font-bold text-indigo-400/60">현재 연속 <span class="text-white"><%= @participant.current_streak %>일째</span> 달성 중!</p>
                </div>
              </div>
            <% else %>
              <div class="pt-2">
                <%= link_to new_challenge_verification_log_path(@challenge), 
                            class: "block w-full py-5 app-fab-gradient text-white text-center rounded-3xl font-black text-[15px] shadow-xl shadow-indigo-500/30 active:scale-[0.98] transition-all border border-white/10" do %>
                   인증 기록 남기기 ⚡
                <% end %>
              </div>
            <% end %>
          </div>
        </div>
      <% end %>
    </section>

    <!-- 2. Status Board -->
    <!-- 2. Status Board (Challenge Only) -->
    <% unless @challenge.offline? %>
      <section class="grid grid-cols-2 gap-4">
        <!-- Progress Bar -->
        <div class="glass-card p-6 flex flex-col justify-between gap-6">
          <div class="relative w-20 h-20 mx-auto">
            <svg class="w-full h-full transform -rotate-90">
              <circle cx="40" cy="40" r="36" stroke="currentColor" stroke-width="8" fill="transparent" class="text-white/5" />
              <circle cx="40" cy="40" r="36" stroke="currentColor" stroke-width="8" fill="transparent" 
                      class="text-indigo-500" stroke-linecap="round"
                      stroke-dasharray="226.19"
                      stroke-dashoffset="<%= 226.19 * (1 - (@participant.completion_rate / 100.0)) %>" />
            </svg>
            <div class="absolute inset-0 flex items-center justify-center">
              <span class="text-lg font-black italic"><%= @participant.completion_rate.to_i %>%</span>
            </div>
          </div>
          <div class="text-center">
            <p class="text-[10px] font-black text-slate-500 uppercase tracking-widest">Progress</p>
            <p class="text-sm font-black mt-1">총 <%= @completed_days %>회 달성</p>
          </div>
        </div>

        <!-- streak Card -->
        <div class="glass-card p-6 flex flex-col justify-between gap-4">
          <div class="flex-1 flex flex-col items-center justify-center py-2">
            <div class="relative">
              <span class="text-4xl">🔥</span>
              <div class="absolute -top-1 -right-1 w-3 h-3 bg-rose-500 rounded-full animate-ping"></div>
            </div>
            <p class="text-3xl font-black italic mt-3"><%= @participant.current_streak %><span class="text-sm ml-1">Days</span></p>
          </div>
          <div class="text-center">
            <p class="text-[10px] font-black text-slate-500 uppercase tracking-widest">Active Streak</p>
            <p class="text-xs font-bold text-indigo-400 mt-1">최고 <%= @participant.max_streak %>일 연속</p>
          </div>
        </div>
      </section>
    <% end %>

    <!-- 3. Tabs Navigation (Premium Floating Glass) -->
    <turbo-frame id="dashboard_tabs" target="_top">
      <section class="space-y-10">
        <div class="flex items-center gap-2 overflow-x-auto no-scrollbar py-2">
          <% 
            if @challenge.offline?
              p_tabs = [
                { id: "info", label: "안내", icon: "✨" },
                { id: "dashboard", label: "공지", icon: "📢" }
              ]
            else
              p_tabs = [
                { id: "records", label: "기록", icon: "📅" },
                { id: "dashboard", label: "피드", icon: "🔥" },
                { id: "ranking", label: "랭킹", icon: "🏆" },
                { id: "info", label: "정보", icon: "✨" }
              ]
            end
            p_tabs.insert(1, { id: "management", label: "관리", icon: "⚙️" }) if @is_host
          %>
          <% p_tabs.each do |t| %>
            <% 
              is_active = (@tab == t[:id])
              active_class = if @challenge.offline?
                "bg-emerald-500 text-white border-emerald-400/50 shadow-[0_5px_15px_rgba(16,185,129,0.3)]"
              else
                "bg-indigo-500 text-white border-indigo-400/50 shadow-[0_5px_15px_rgba(99,102,241,0.3)]"
              end
              inactive_class = "text-white/40 border-white/5 bg-white/[0.02] hover:bg-white/5"
            %>
            <%= link_to challenge_path(@challenge, tab: t[:id], source: params[:source]), 
                        data: { turbo_frame: "dashboard_tabs" },
                        class: "shrink-0 px-6 py-3.5 rounded-2xl font-black text-[13px] transition-all flex items-center gap-2 border #{ is_active ? active_class : inactive_class }" do %>
              <span class="text-sm"><%= t[:icon] %></span>
              <%= t[:label] %>
            <% end %>
          <% end %>
        </div>

        <!-- Tab Content -->
        <div class="tab-content-env min-h-[400px]">
          <% case @tab %>
        <% when "records" %>
           <!-- Simplified Calendar-like dots -->
           <div class="space-y-8">
              <div class="glass-card p-6" data-controller="calendar-scroll">
                <div class="flex items-center justify-between mb-6">
                  <h3 class="text-xs font-black flex items-center gap-2">
                    <span class="w-1 h-3 bg-indigo-500 rounded-full"></span>
                    ACTIVITY CALENDAR
                  </h3>
                  <div class="flex items-center gap-1.5 opacity-50">
                     <span class="text-[9px] font-bold text-slate-500 uppercase tracking-widest">Swipe</span>
                     <svg class="w-3 h-3 text-slate-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
                  </div>
                </div>

                <div class="flex gap-4 items-start">
                  <!-- Day Labels -->
                  <div class="flex flex-col gap-2 pt-6 shrink-0">
                    <% %w(S M T W T F S).each do |day| %>
                      <span class="text-[8px] font-black text-slate-700 w-3 h-3 flex items-center justify-center"><%= day %></span>
                    <% end %>
                  </div>

                  <!-- Scrollable Area -->
                  <div class="flex-1 overflow-x-auto no-scrollbar scroll-smooth" data-calendar-scroll-target="container">
                    <div class="flex gap-2 pb-2">
                      <% 
                        # Ensure we start from Sunday to match S M T W T F S rows
                        start_date = @challenge.start_date.beginning_of_week(:sunday)
                        end_date = @challenge.end_date.end_of_week(:sunday)
                        calendar_days = (start_date..end_date).to_a
                        weeks = calendar_days.in_groups_of(7)
                        current_month = nil
                      %>
                      <% weeks.each do |week| %>
                        <div class="flex flex-col gap-2 shrink-0">
                          <!-- Month Header -->
                          <div class="h-4 text-[9px] font-black text-slate-500">
                            <% if week.compact.first&.month != current_month %>
                              <%= week.compact.first&.strftime('%b') %>
                              <% current_month = week.compact.first&.month %>
                            <% end %>
                          </div>

                          <% week.each do |date| %>
                            <% if date.nil? || date < @challenge.start_date || date > @challenge.end_date %>
                              <div class="w-6 h-6 rounded-md bg-white/[0.02]"></div>
                            <% else %>
                              <div id="calendar-day-<%= date %>" 
                                   <%= 'data-calendar-scroll-target=today' if date == Date.current %>
                                   class="w-6 h-6 rounded-md flex items-center justify-center text-[8px] font-bold transition-all
                                          <%= if @daily_status_map[date]
                                                'bg-indigo-500 text-white shadow-lg shadow-indigo-500/20 scale-110 z-10'
                                              elsif date == Date.current
                                                'bg-indigo-500/20 text-indigo-400 border border-indigo-500/30'
                                              elsif date > Date.current
                                                'bg-white/5 text-slate-700'
                                              else
                                                'bg-white/5 text-slate-500'
                                              end %>">
                                <%= date.day %>
                              </div>
                            <% end %>
                          <% end %>
                        </div>
                      <% end %>
                    </div>
                  </div>
                </div>
              </div>

             <div class="space-y-4">
               <h3 class="text-xs font-black flex items-center gap-2 px-2">
                 <span class="w-1 h-3 bg-indigo-500 rounded-full"></span>
                 RECENT HISTORY
               </h3>
               <% my_logs = @participant.verification_logs.where(status: [:approved, :rejected, :pending]).order(created_at: :desc).limit(10) %>
               <% if my_logs.any? %>
                 <div class="space-y-3">
                   <% my_logs.each do |log| %>
                     <div class="glass-card p-5 flex items-center justify-between gap-4">
                       <div class="flex items-center gap-4">
                         <div class="w-10 h-10 rounded-xl bg-white/5 flex items-center justify-center text-lg">
                           <%= log.approved? ? "✅" : (log.pending? ? "⏳" : "❌") %>
                         </div>
                         <div>
                           <p class="text-sm font-black text-white"><%= log.created_at.strftime('%m월 %d일') %></p>
                           <p class="text-[10px] font-bold text-slate-500"><%= log.created_at.strftime('%H:%M') %> • <%= log.status.upcase %></p>
                         </div>
                       </div>
                       <div class="text-right">
                         <p class="text-xs font-black text-indigo-400"><%= log.value.presence || "Completed" %></p>
                       </div>
                     </div>
                   <% end %>
                 </div>
               <% else %>
                 <div class="py-12 text-center glass-card">
                   <p class="text-xs font-bold text-slate-500 italic">"첫 기록을 남겨보세요!"</p>
                 </div>
               <% end %>
             </div>
           </div>

        <% when "dashboard" %>
          <% if @challenge.offline? %>
            <!-- GATHERING: Announcements Feed -->
            <% if @challenge.announcements.any? %>
              <div class="space-y-4">
                <% @challenge.announcements.order(created_at: :desc).each do |notice| %>
                  <div class="glass-card p-6 space-y-3 border-emerald-500/20 bg-emerald-500/5">
                    <div class="flex items-center justify-between">
                      <span class="px-2 py-1 rounded-md bg-emerald-500/20 text-emerald-400 text-[10px] font-black uppercase tracking-widest">NOTICE</span>
                      <span class="text-[10px] text-slate-500 font-bold"><%= time_ago_in_words(notice.created_at) %> 전</span>
                    </div>
                    <div>
                      <h3 class="text-sm font-black text-white mb-2"><%= notice.title %></h3>
                      <p class="text-xs text-slate-300 leading-relaxed"><%= notice.content %></p>
                    </div>
                  </div>
                <% end %>
              </div>
            <% else %>
              <div class="py-20 text-center glass-card">
                <span class="text-3xl block mb-4">📢</span>
                <p class="text-xs font-bold text-slate-500 italic">"등록된 공지사항이 없습니다"</p>
              </div>
            <% end %>
          <% else %>
            <!-- CHALLENGE: Verification Feed -->
            <% if @challenge.verification_logs.any? %>
              <div class="space-y-6">
                <% @challenge.verification_logs.includes(participant: :user).recent.limit(20).each do |log| %>
                  <div class="glass-card p-6 space-y-4">
                    <div class="flex items-center justify-between">
                      <div class="flex items-center gap-3">
                        <img src="<%= log.participant.user.profile_image %>" class="w-10 h-10 rounded-xl object-cover bg-white/10" />
                        <div>
                          <p class="text-sm font-black text-white"><%= log.participant.nickname %></p>
                          <p class="text-[10px] font-bold text-slate-500"><%= time_ago_in_words(log.created_at) %> 전</p>
                        </div>
                      </div>
                      <% if log.approved? %>
                        <span class="p-2 rounded-lg bg-emerald-500/10 text-emerald-400 text-[10px]">SUCCESS</span>
                      <% end %>
                    </div>
                    <% if log.image_url.present? %>
                      <div class="rounded-2xl overflow-hidden aspect-video bg-white/5">
                        <img src="<%= log.image_url %>" class="w-full h-full object-cover" />
                      </div>
                    <% end %>
                    <p class="text-sm font-medium text-slate-300 leading-relaxed"><%= log.value %></p>
                  </div>
                <% end %>
              </div>
            <% else %>
              <div class="py-20 text-center glass-card">
                <span class="text-3xl block mb-4">🌱</span>
                <p class="text-xs font-bold text-slate-500 italic">"아직 등록된 인증 피드가 없습니다.<br>첫 번째 주인공이 되어보세요!"</p>
              </div>
            <% end %>
          <% end %>
        <% when "ranking" %>
          <div class="glass-card overflow-hidden">
             <div class="p-6 border-b border-white/5 flex items-center justify-between bg-white/[0.02]">
               <p class="text-[10px] font-black text-slate-500 uppercase tracking-widest">Rank</p>
               <p class="text-[10px] font-black text-slate-500 uppercase tracking-widest">Achievement</p>
             </div>
             <div class="divide-y divide-white/5">
               <% @rankings.each_with_index do |p, idx| %>
                  <div class="p-6 flex items-center justify-between">
                    <div class="flex items-center gap-4">
                      <span class="w-6 text-center text-sm font-black <%= idx < 3 ? 'text-indigo-400' : 'text-slate-600' %>">
                        <%= idx + 1 %>
                      </span>
                      <%= link_to user_profile_path(p.user), class: "flex items-center gap-4 group/rank" do %>
                        <img src="<%= p.user.profile_image %>" class="w-9 h-9 rounded-xl object-cover bg-white/10 group-hover/rank:scale-110 transition-transform" />
                        <span class="text-sm font-black text-white group-hover/rank:text-indigo-400 transition-colors"><%= p.nickname %></span>
                      <% end %>
                    </div>
                    <div class="text-right">
                      <p class="text-sm font-black text-white italic"><%= p.completion_rate.to_i %>%</p>
                      <p class="text-[10px] font-bold text-indigo-500/60 mt-0.5">🔥 <%= p.current_streak %>일</p>
                    </div>
                  </div>
               <% end %>
             </div>
          </div>

        <% when "info", "intro" %>
          <div class="space-y-8">
            <!-- Summary -->
            <% if @challenge.summary.present? %>
              <div class="glass-card p-8 bg-indigo-500/5">
                <p class="text-sm font-bold text-slate-300 italic">"<%= @challenge.summary %>"</p>
              </div>
            <% end %>

            <!-- Description -->
            <div class="glass-card p-8 space-y-6">
              <h3 class="text-sm font-black flex items-center gap-2">
                <span class="w-1.5 h-1.5 rounded-full bg-indigo-500"></span>
                <%= @challenge.offline? ? "GATHERING GUIDE" : "CHALLENGE GUIDE" %>
              </h3>
              <div class="text-sm font-medium text-slate-400 leading-relaxed whitespace-pre-wrap"><%= @challenge.description %></div>
            </div>

            <!-- Preparation -->
            <% if @challenge.preparation_items.present? %>
              <div class="glass-card p-8 border-amber-500/20 bg-amber-500/5">
                 <h3 class="text-xs font-black text-amber-500 mb-4 uppercase tracking-widest flex items-center gap-2">
                   <span class="text-base">📋</span> PREPARATION & NOTICE
                 </h3>
                 <p class="text-sm font-medium text-slate-300 leading-relaxed whitespace-pre-wrap"><%= @challenge.preparation_items %></p>
              </div>
            <% end %>

            <!-- Rules Partial -->
            <div class="glass-card p-8 space-y-6">
              <h3 class="text-sm font-black flex items-center gap-2">
                <span class="w-1.5 h-1.5 rounded-full bg-indigo-500"></span>
                RULES & SCHEDULE
              </h3>
              <div class="text-white/80">
                <%= render "challenges/tabs/rules", challenge: @challenge %>
              </div>
            </div>

            <!-- Extra Stats -->
            <div class="grid grid-cols-2 gap-4">
              <div class="glass-card p-6 text-center">
                <p class="text-[9px] font-black text-slate-500 uppercase tracking-widest mb-1"><%= @challenge.offline? ? "Date" : "Frequency" %></p>
                <p class="text-sm font-black text-white italic"><%= @challenge.offline? ? @challenge.start_date.strftime('%m.%d') : @challenge.mission_frequency %></p>
              </div>
              <div class="glass-card p-6 text-center">
                <p class="text-[9px] font-black text-slate-500 uppercase tracking-widest mb-1"><%= @challenge.offline? ? "Entry Fee" : "Deposit" %></p>
                <p class="text-sm font-black text-white italic">
                  <% if @challenge.cost_type_free? %>
                    FREE
                  <% else %>
                    <%= number_with_delimiter(@challenge.amount || 0) %>원
                  <% end %>
                </p>
              </div>
            </div>
          </div>

        <% when "management" %>
          <div class="space-y-8">
            <div class="glass-card p-8 bg-indigo-500/10 border-indigo-500/20 flex items-center justify-between">
              <div>
                <h3 class="text-lg font-black italic mb-2">Host Dashboard</h3>
                <p class="text-xs text-indigo-400/80 font-medium">참여자 현황을 실시간으로 분석합니다.</p>
              </div>
              <%= link_to hosted_challenge_path(@challenge), class: "px-4 py-2 bg-indigo-500 text-white rounded-xl text-[10px] font-black uppercase tracking-widest shadow-lg shadow-indigo-500/20 active:scale-95 transition-all" do %>
                ADMIN SUITE
              <% end %>
            </div>

            <div class="grid grid-cols-2 gap-4">
              <div class="glass-card p-6">
                 <p class="text-[9px] font-black text-slate-500 mb-1 uppercase">Total Members</p>
                 <p class="text-2xl font-black italic"><%= @challenge.current_participants %></p>
              </div>
              <div class="glass-card p-6">
                 <p class="text-[9px] font-black text-slate-500 mb-1 uppercase"><%= @challenge.offline? ? "Capacity" : "Today Rate" %></p>
                 <% if @challenge.offline? %>
                   <p class="text-2xl font-black italic text-indigo-400"><%= @challenge.current_participants %> / <%= @challenge.meeting_info&.max_attendees || '∞' %></p>
                 <% else %>
                   <p class="text-2xl font-black italic text-indigo-400"><%= (@today_verified_count.to_f / [@challenge.current_participants, 1].max * 100).to_i %>%</p>
                 <% end %>
              </div>
            </div>

            <!-- Pending Applications -->
            <% if @pending_applications&.any? %>
              <div class="space-y-4">
                <h4 class="text-xs font-black px-2 flex items-center justify-between">
                  <span>PENDING APPROVALS</span>
                  <span class="text-indigo-400"><%= @pending_applications.count %></span>
                </h4>
                <div class="space-y-3">
                  <% @pending_applications.limit(5).each do |app| %>
                    <div class="glass-card p-6 flex items-center justify-between gap-4">
                      <div class="flex items-center gap-3">
                        <img src="<%= app.user.profile_image %>" class="w-10 h-10 rounded-xl object-cover bg-white/10" />
                        <div>
                          <p class="text-sm font-black"><%= app.user.nickname %></p>
                          <p class="text-[10px] font-bold text-slate-500"><%= time_ago_in_words(app.applied_at) %> 전 신청</p>
                        </div>
                      </div>
                      <div class="flex gap-2">
                         <%= button_to approve_challenge_application_path(@challenge, app), method: :post, class: "px-4 py-2 bg-indigo-500 rounded-xl text-[10px] font-black" do %>APPROVE<% end %>
                      </div>
                    </div>
                  <% end %>
                </div>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    </turbo-frame>
  </div>
</div>
