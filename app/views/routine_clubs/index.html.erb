<div class="min-h-screen bg-gradient-to-br from-orange-50 via-white to-pink-50/30">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    
    <!-- Header -->
    <header class="mb-12">
      <div class="flex flex-col md:flex-row md:items-end justify-between gap-6">
        <div>
          <div class="inline-flex items-center gap-2 bg-orange-100 px-4 py-2 rounded-full mb-4">
            <span class="w-2 h-2 bg-orange-500 rounded-full animate-pulse"></span>
            <span class="text-sm font-black text-orange-600">PREMIUM</span>
          </div>
          <h1 class="text-4xl md:text-6xl font-black text-slate-900 tracking-tight mb-4">
            루틴 클럽
          </h1>
          <p class="text-xl text-slate-600 font-bold max-w-2xl">
            규칙 · 가시성 · 책임감이 만드는 강력한 환경.<br/>
            최소 3개월, 함께하는 사람들과 진짜 변화를 경험하세요.
          </p>
        </div>
        
        <%= link_to new_routine_club_path, class: "inline-flex items-center gap-2 px-8 py-4 bg-gradient-to-r from-orange-500 to-pink-500 text-white rounded-2xl font-black text-lg hover:shadow-2xl hover:shadow-orange-200 transition-all" do %>
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"/>
          </svg>
          클럽 개설하기
        <% end %>
      </div>
    </header>

    <!-- Filters -->
    <div class="mb-8 flex flex-wrap gap-3">
      <%= link_to routine_clubs_path, class: "px-6 py-3 rounded-2xl font-black text-sm transition-all #{params[:category].blank? ? 'bg-slate-900 text-white' : 'bg-white text-slate-400 hover:text-slate-600 border border-slate-200'}" do %>
        전체
      <% end %>
      
      <% ["건강·운동", "학습·자기계발", "재테크·부업", "취미·여가", "생활습관"].each do |cat| %>
        <%= link_to routine_clubs_path(category: cat), class: "px-6 py-3 rounded-2xl font-black text-sm transition-all #{params[:category] == cat ? 'bg-slate-900 text-white' : 'bg-white text-slate-400 hover:text-slate-600 border border-slate-200'}" do %>
          <%= cat %>
        <% end %>
      <% end %>
    </div>

    <!-- Stats Bar -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-12">
      <div class="bg-white border border-slate-100 rounded-3xl p-6">
        <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-2">모집 중</p>
        <p class="text-3xl font-black text-orange-500"><%= @routine_clubs.count %>개</p>
      </div>
      
      <div class="bg-white border border-slate-100 rounded-3xl p-6">
        <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-2">총 멤버</p>
        <p class="text-3xl font-black text-indigo-500"><%= @routine_clubs.sum(:current_members) %>명</p>
      </div>
      
      <div class="bg-white border border-slate-100 rounded-3xl p-6">
        <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-2">평균 출석률</p>
        <p class="text-3xl font-black text-emerald-500">
          <%= @routine_clubs.where.not(average_attendance_rate: 0).average(:average_attendance_rate)&.round || 0 %>%
        </p>
      </div>
      
      <div class="bg-white border border-slate-100 rounded-3xl p-6">
        <p class="text-xs font-black text-slate-400 uppercase tracking-widest mb-2">최소 기간</p>
        <p class="text-3xl font-black text-purple-500">3개월</p>
      </div>
    </div>

    <!-- Clubs Grid -->
    <% if @routine_clubs.any? %>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        <% @routine_clubs.each do |club| %>
          <%= link_to routine_club_path(club), class: "group" do %>
            <div class="bg-white border-2 border-slate-100 rounded-[32px] overflow-hidden hover:shadow-2xl hover:border-orange-300 transition-all duration-300">
              
              <!-- Thumbnail -->
              <div class="aspect-video bg-gradient-to-br from-orange-400 via-pink-500 to-purple-500 relative overflow-hidden">
                <div class="absolute inset-0 flex items-center justify-center">
                  <div class="text-7xl opacity-40 group-hover:scale-110 transition-transform duration-500">
                    <%= ["🎯", "🔥", "💪", "📚", "🌟", "⚡"].sample %>
                  </div>
                </div>
                
                <!-- Category Badge -->
                <div class="absolute top-4 left-4">
                  <span class="bg-white/90 backdrop-blur-sm px-4 py-2 rounded-full text-xs font-black text-orange-600">
                    <%= club.category %>
                  </span>
                </div>
                
                <!-- Members Badge -->
                <div class="absolute bottom-4 right-4">
                  <div class="bg-black/50 backdrop-blur-sm px-4 py-2 rounded-full flex items-center gap-2">
                    <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/>
                    </svg>
                    <span class="text-xs font-black text-white">
                      <%= club.current_members %> / <%= club.max_members %>
                    </span>
                  </div>
                </div>
                
                <!-- Status Badge -->
                <% if club.is_full? %>
                  <div class="absolute top-4 right-4">
                    <span class="bg-red-500 px-4 py-2 rounded-full text-xs font-black text-white">
                      마감
                    </span>
                  </div>
                <% elsif club.current_members >= club.max_members * 0.8 %>
                  <div class="absolute top-4 right-4">
                    <span class="bg-amber-500 px-4 py-2 rounded-full text-xs font-black text-white">
                      마감임박
                    </span>
                  </div>
                <% end %>
              </div>
              
              <!-- Content -->
              <div class="p-6 space-y-4">
                <!-- Host Info -->
                <div class="flex items-center gap-3">
                  <div class="w-10 h-10 rounded-full bg-gradient-to-br from-orange-400 to-pink-500 flex items-center justify-center text-white font-black">
                    <%= club.host.nickname[0].upcase %>
                  </div>
                  <div>
                    <p class="text-xs font-black text-slate-400 uppercase tracking-widest">Host</p>
                    <p class="text-sm font-black text-slate-900"><%= club.host.nickname %></p>
                  </div>
                </div>
                
                <!-- Title & Description -->
                <div>
                  <h3 class="text-xl font-black text-slate-900 group-hover:text-orange-600 transition-colors mb-2 line-clamp-2">
                    <%= club.title %>
                  </h3>
                  <p class="text-sm text-slate-600 font-medium line-clamp-2">
                    <%= club.description %>
                  </p>
                </div>
                
                <!-- Stats -->
                <div class="grid grid-cols-3 gap-4 pt-4 border-t border-slate-100">
                  <div>
                    <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-1">월 요금</p>
                    <p class="text-lg font-black text-slate-900">
                      <%= number_to_currency(club.monthly_fee, unit: "₩", precision: 0, format: "%n%u") %>
                    </p>
                  </div>
                  <div>
                    <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-1">기간</p>
                    <p class="text-lg font-black text-slate-900">
                      <%= club.duration_in_months %>개월
                    </p>
                  </div>
                  <div>
                    <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-1">시작</p>
                    <p class="text-lg font-black text-slate-900">
                      <%= club.start_date.strftime("%m/%d") %>
                    </p>
                  </div>
                </div>
                
                <!-- CTA Button -->
                <div class="pt-2">
                  <div class="w-full py-3 bg-gradient-to-r from-orange-500 to-pink-500 text-white rounded-2xl font-black text-sm text-center group-hover:shadow-lg group-hover:shadow-orange-200 transition-all">
                    자세히 보기
                  </div>
                </div>
              </div>
            </div>
          <% end %>
        <% end %>
      </div>
    <% else %>
      <!-- Empty State -->
      <div class="bg-white border-2 border-dashed border-slate-200 rounded-[48px] p-20 text-center">
        <div class="text-8xl mb-6 opacity-20">🎯</div>
        <h3 class="text-2xl font-black text-slate-400 mb-3">
          <%= params[:category].present? ? "#{params[:category]} 카테고리에" : "" %>
          모집 중인 클럽이 없습니다
        </h3>
        <p class="text-slate-400 font-medium mb-8">
          곧 새로운 클럽이 열릴 예정입니다
        </p>
        <%= link_to "전체 클럽 보기", routine_clubs_path, class: "inline-block px-8 py-4 bg-slate-900 text-white rounded-2xl font-black hover:bg-slate-700 transition-colors" %>
      </div>
    <% end %>

    <!-- Bottom CTA -->
    <div class="mt-16 relative overflow-hidden bg-gradient-to-br from-indigo-600 to-purple-700 rounded-[48px] p-12 text-white shadow-2xl">
      <div class="absolute top-0 right-0 w-64 h-64 bg-white/10 rounded-full blur-3xl"></div>
      <div class="absolute bottom-0 left-0 w-96 h-96 bg-purple-500/20 rounded-full blur-3xl"></div>
      
      <div class="relative z-10 max-w-3xl mx-auto text-center">
        <div class="text-6xl mb-6">🚀</div>
        <h2 class="text-3xl md:text-4xl font-black mb-4">
          나만의 루틴 클럽을 만들어보세요
        </h2>
        <p class="text-xl text-white/90 font-bold mb-8">
          호스트가 되어 함께 성장할 멤버들을 모집하고,<br/>
          체계적인 환경에서 루틴을 이끌어가세요.
        </p>
        <%= link_to new_routine_club_path, class: "inline-flex items-center gap-2 px-8 py-4 bg-white text-indigo-600 rounded-2xl font-black text-lg hover:bg-yellow-300 hover:text-indigo-700 transition-all shadow-2xl" do %>
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"/>
          </svg>
          클럽 개설하기
        <% end %>
      </div>
    </div>

  </div>
</div>
