<!-- Stats Summary Banner -->
<section class="grid grid-cols-2 lg:grid-cols-4 gap-3 md:gap-4 mb-8">
  <% 
    total_completed = @monthly_completions&.count || 0
    avg_rate = @personal_routines.any? ? (total_completed.to_f / (@personal_routines.count * [Date.current.day, 1].max) * 100).clamp(0, 100).round : 0
    max_streak = @personal_routines.map(&:current_streak).max || 0
    top_category = @personal_routines.group_by(&:category).max_by { |_, v| v.count }&.first || "None"
  %>
  
  <div class="bg-white p-5 md:p-6 rounded-[24px] md:rounded-[32px] border border-slate-100 shadow-sm hover:shadow-md transition-all group relative overflow-hidden">
    <div class="absolute -right-4 -top-4 w-16 h-16 bg-indigo-50 rounded-full blur-2xl group-hover:bg-indigo-100 transition-colors"></div>
    <div class="relative z-10 space-y-1 md:space-y-2">
      <p class="text-[9px] md:text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Monthly Done</p>
      <div class="flex items-baseline gap-1">
        <h4 class="text-2xl md:text-3xl font-black text-slate-900"><%= total_completed %></h4>
        <span class="text-[10px] md:text-xs font-bold text-slate-400">회</span>
      </div>
    </div>
  </div>

  <div class="bg-white p-5 md:p-6 rounded-[24px] md:rounded-[32px] border border-slate-100 shadow-sm hover:shadow-md transition-all group relative overflow-hidden">
    <div class="absolute -right-4 -top-4 w-16 h-16 bg-emerald-50 rounded-full blur-2xl group-hover:bg-emerald-100 transition-colors"></div>
    <div class="relative z-10 space-y-1 md:space-y-2">
      <p class="text-[9px] md:text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Avg. Success</p>
      <div class="flex items-baseline gap-1">
        <h4 class="text-2xl md:text-3xl font-black text-emerald-600"><%= avg_rate %></h4>
        <span class="text-[10px] md:text-xs font-bold text-emerald-400">%</span>
      </div>
    </div>
  </div>

  <div class="bg-white p-5 md:p-6 rounded-[24px] md:rounded-[32px] border border-slate-100 shadow-sm hover:shadow-md transition-all group relative overflow-hidden">
    <div class="absolute -right-4 -top-4 w-16 h-16 bg-orange-50 rounded-full blur-2xl group-hover:bg-orange-100 transition-colors"></div>
    <div class="relative z-10 space-y-1 md:space-y-2">
      <p class="text-[9px] md:text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Max Streak</p>
      <div class="flex items-baseline gap-1">
        <h4 class="text-2xl md:text-3xl font-black text-orange-500"><%= max_streak %></h4>
        <span class="text-[10px] md:text-xs font-bold text-orange-400">일</span>
      </div>
    </div>
  </div>

  <div class="bg-white p-5 md:p-6 rounded-[24px] md:rounded-[32px] border border-slate-100 shadow-sm hover:shadow-md transition-all group relative overflow-hidden">
    <div class="absolute -right-4 -top-4 w-16 h-16 bg-purple-50 rounded-full blur-2xl group-hover:bg-purple-100 transition-colors"></div>
    <div class="relative z-10 space-y-1 md:space-y-2">
      <p class="text-[9px] md:text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Main Focus</p>
      <div class="flex items-baseline gap-1">
        <h4 class="text-lg md:text-xl font-black text-purple-600 truncate"><%= top_category %></h4>
      </div>
    </div>
  </div>
</section>

<div class="grid grid-cols-1 lg:grid-cols-12 gap-8 md:gap-12">
  <!-- Recommended Routines (Full Width) -->
  <section class="lg:col-span-12 mb-4">
    <div class="flex items-center justify-between mb-4 px-1">
      <div class="space-y-1">
        <h3 class="text-lg font-black text-slate-900">인기 루틴 추천</h3>
        <p class="text-xs font-bold text-slate-400">다른 사용자들이 많이 실천하는 루틴이에요</p>
      </div>
      <div class="flex items-center gap-2 text-xs font-bold text-slate-400">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"/></svg>
        인기순
      </div>
    </div>
    
    <div class="flex gap-4 overflow-x-auto pb-6 no-scrollbar -mx-1 px-1">
      <% @recommended_routines.take(8).each do |routine| %>
        <div class="min-w-[220px] md:min-w-[240px] bg-white p-5 rounded-[28px] border border-slate-100 hover:border-indigo-200 hover:shadow-lg transition-all group cursor-pointer">
          <div class="flex items-start justify-between mb-4">
            <div class="w-12 h-12 md:w-14 md:h-14 bg-gradient-to-br from-indigo-50 to-purple-50 rounded-2xl flex items-center justify-center text-xl md:text-2xl group-hover:scale-110 transition-transform">
              <%= routine[:icon] %>
            </div>
            <%= button_to personal_routines_path(personal_routine: { title: routine[:title], category: routine[:category], icon: routine[:icon], days: [1,2,3,4,5,6,0] }), method: :post, data: { turbo: false }, class: "w-9 h-9 bg-slate-50 text-slate-400 rounded-xl flex items-center justify-center hover:bg-indigo-600 hover:text-white transition-all shadow-sm hover:scale-110" do %>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M12 6v12m6-6H6"/></svg>
            <% end %>
          </div>
          <div class="space-y-2">
            <h4 class="text-sm font-black text-slate-900 group-hover:text-indigo-600 transition-colors line-clamp-2"><%= routine[:title] %></h4>
            <div class="flex items-center justify-between">
              <span class="text-[9px] font-bold text-slate-400 uppercase tracking-widest"><%= routine[:category] %></span>
              <div class="flex items-center gap-1 text-[11px] font-bold text-emerald-600">
                <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 20 20"><path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/></svg>
                <%= routine[:users_count] %>명
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </section>

  <!-- Left Column: My Routines -->
  <div class="lg:col-span-8 space-y-8">
    <!-- Day Filter Bar (Styled to match dashboard) -->
    <div class="flex flex-col gap-6">
      <div class="flex items-center justify-between px-1">
        <div class="space-y-1">
          <h2 class="text-2xl font-black text-slate-900 tracking-tight">마이 루틴 피드</h2>
          <p class="text-xs font-bold text-slate-400">당신의 오늘의 정상을 향한 기록입니다.</p>
        </div>
        <span class="px-3 py-1 bg-indigo-50 text-indigo-600 rounded-full text-[10px] font-black uppercase"><%= @personal_routines.count %> Active</span>
      </div>
      
      <div class="flex bg-slate-100/80 backdrop-blur-md p-1 rounded-2xl gap-1 overflow-x-auto no-scrollbar border border-slate-200/50 shadow-inner">
        <% [["전체", "all"], ["월", "1"], ["화", "2"], ["수", "3"], ["목", "4"], ["금", "5"], ["토", "6"], ["일", "0"]].each do |label, value| %>
          <button type="button" 
                  onclick="filterRoutines('<%= value %>')"
                  id="btn-<%= value %>"
                  class="routine-filter-btn whitespace-nowrap px-5 py-2.5 rounded-[14px] text-xs font-black transition-all duration-300 <%= value == Date.current.wday.to_s ? 'bg-white text-indigo-600 shadow-sm' : 'text-slate-400 hover:text-slate-600' %>">
            <%= label %>
          </button>
        <% end %>
      </div>
    </div>

    <!-- Dashboard Container (Same as Home) -->
    <div class="bg-indigo-600 rounded-[40px] p-6 md:p-10 shadow-xl shadow-indigo-100 flex flex-col gap-8 relative overflow-hidden">
      <!-- Decor (Same as Home) -->
      <div class="absolute top-0 right-0 w-64 h-64 bg-white/5 rounded-full -mr-32 -mt-32 blur-3xl"></div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4 relative z-10" id="routine-container">
        <% if @personal_routines.any? %>
          <% @personal_routines.each do |routine| %>
            <%= render 'routine_card', routine: routine %>
          <% end %>
        <% else %>
          <div class="col-span-full py-20 text-center space-y-4">
            <div class="text-4xl">✨</div>
            <p class="text-white/50 font-bold">아직 생성된 루틴이 없습니다.</p>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <!-- Right Column: Sidebar -->
  <aside class="lg:col-span-4 space-y-8">
    <!-- Achievement Matrix (Calendar Styles) -->
    <section class="bg-white rounded-[32px] md:rounded-[40px] p-6 md:p-8 border border-slate-100 shadow-sm space-y-6 md:space-y-8 relative overflow-hidden group">
      <div class="absolute -right-4 -top-4 w-24 h-24 bg-indigo-50/50 rounded-full blur-2xl group-hover:scale-110 transition-transform"></div>
      
      <div class="relative z-10 flex items-center justify-between">
        <h3 class="text-xl font-black text-slate-900 tracking-tight">성취 매트릭스</h3>
        <span class="text-[10px] font-black text-slate-400"><%= Date.current.strftime("%B") %></span>
      </div>

      <div class="relative z-10 space-y-4">
        <div class="grid grid-cols-7 gap-1 md:gap-1.5">
          <% ["S", "M", "T", "W", "T", "F", "S"].each do |day| %>
            <div class="text-[9px] font-black text-slate-300 text-center py-1 md:py-2"><%= day %></div>
          <% end %>

          <% 
            start_of_month = Date.current.beginning_of_month
            grid_start = start_of_month - start_of_month.wday
            end_of_month = Date.current.end_of_month
            grid_end = end_of_month + (6 - end_of_month.wday)
          %>
          
          <% (grid_start..grid_end).each do |date| %>
            <% 
              is_current_month = date.month == Date.current.month
              count = (@personal_routines || []).select { |r| r.completed_on?(date) }.count 
              color_class = case
              when !is_current_month then "bg-transparent opacity-0"
              when count == 0 then "bg-slate-50"
              when count <= 2 then "bg-indigo-100"
              when count <= 4 then "bg-indigo-300"
              else "bg-indigo-600"
              end
              
              is_today = date == Date.current
            %>
            <div class="aspect-square rounded-lg md:rounded-xl <%= color_class %> transition-all relative group/day <%= 'ring-2 ring-indigo-500 ring-offset-2 scale-90' if is_today && is_current_month %>" 
                 title="<%= date %>: <%= count %> routines">
              <% if is_today && is_current_month %>
                <span class="absolute -top-1 -right-1 w-1.5 h-1.5 bg-indigo-500 rounded-full"></span>
              <% end %>
              <div class="absolute inset-0 flex items-center justify-center opacity-0 group-hover/day:opacity-100 transition-opacity pointer-events-none">
                 <span class="text-[8px] font-black <%= count > 2 ? 'text-white' : 'text-indigo-600' %>"><%= date.day %></span>
              </div>
            </div>
          <% end %>
        </div>

        <div class="flex justify-between items-center pt-4 border-t border-slate-50">
          <div class="flex items-center gap-1.5">
            <div class="w-2.5 h-2.5 rounded-full bg-indigo-600"></div>
            <span class="text-[9px] font-bold text-slate-500">Perfect Day</span>
          </div>
          <p class="text-[9px] font-black text-slate-400 uppercase tracking-widest text-right">
            <%= @monthly_completions&.count || 0 %> DONE THIS MONTH
          </p>
        </div>
      </div>
    </section>

    <!-- Custom Routine Form -->
    <section class="bg-indigo-600 rounded-[32px] md:rounded-[40px] p-6 md:p-8 text-white space-y-6 shadow-xl shadow-indigo-100 relative overflow-hidden group">
      <div class="absolute -right-10 -bottom-10 w-40 h-40 bg-white/5 rounded-full blur-3xl group-hover:scale-110 transition-transform"></div>
      
      <div class="relative z-10 space-y-1">
        <h3 class="text-xl font-black">새 루틴 추가</h3>
        <p class="text-indigo-100/70 text-sm font-bold tracking-tight">오늘부터 시작할 작은 습관</p>
      </div>

      <%= form_with model: PersonalRoutine.new, url: personal_routines_path, method: :post, data: { turbo: false }, class: "relative z-10 space-y-5" do |f| %>
        <div class="space-y-4">
          <%= f.text_field :title, placeholder: "어떤 루틴인가요?", required: true, class: "w-full px-5 py-4 bg-white/10 border-white/10 rounded-2xl text-white placeholder-white/30 font-black text-sm focus:ring-4 ring-white/10 border-none transition-all" %>
          
          <div class="grid grid-cols-2 gap-3">
             <div class="space-y-1.5">
               <label class="text-[9px] font-black text-indigo-300 uppercase tracking-widest pl-1">Icon</label>
                <%= f.select :icon, ["🔥", "⭐", "💪", "📖", "🥦", "🏃", "🎸", "🧘", "💊", "💧", "👀", "📝", "💰", "☀️", "🌙", "⏰", "🎯", "✨", "🌱", "🧠", "❤️", "🎨", "💻", "📱", "🎮", "🍎", "🥗", "🚴", "🏋️", "🧗", "🏊", "⚽", "🎵", "📚", "✍️", "💡"], {}, class: "w-full px-4 py-3 bg-white text-slate-900 border border-slate-200 rounded-2xl font-black text-sm cursor-pointer focus:ring-4 ring-indigo-100" %>
             </div>
             <div class="space-y-1.5">
               <label class="text-[9px] font-black text-indigo-300 uppercase tracking-widest pl-1">Category</label>
               <%= f.select :category, ["HEALTH", "LIFE", "MIND", "HOBBY", "STUDY", "MONEY"], {}, class: "w-full px-4 py-3 bg-white text-slate-900 border border-slate-200 rounded-2xl font-black text-sm cursor-pointer focus:ring-4 ring-indigo-100" %>
             </div>
          </div>

          <div class="space-y-2">
            <label class="text-[9px] font-black text-indigo-300 uppercase tracking-widest pl-1">Repeat Days</label>
            <div class="flex justify-between gap-1">
              <% [["월", 1], ["화", 2], ["수", 3], ["목", 4], ["금", 5], ["토", 6], ["일", 0]].each do |label, value| %>
                <label class="flex-1">
                  <%= check_box_tag "personal_routine[days][]", value, false, class: "peer hidden" %>
                  <div class="w-full py-2.5 text-center rounded-xl bg-white/20 text-white/70 font-black text-[9px] cursor-pointer border-2 border-transparent peer-checked:bg-white peer-checked:text-indigo-600 peer-checked:border-white peer-checked:shadow-lg transition-all hover:bg-white/30 hover:text-white">
                    <%= label %>
                  </div>
                </label>
              <% end %>
            </div>
          </div>
        </div>

        <%= f.submit "루틴 생성하기", class: "w-full py-4.5 bg-white text-indigo-600 rounded-2xl font-black text-sm hover:scale-[1.02] active:scale-95 transition-all shadow-xl shadow-indigo-900/20 cursor-pointer mt-2" %>
      <% end %>
    </section>
  </aside>
</div>

<script>
  function filterRoutines(day) {
    document.querySelectorAll('.routine-card').forEach(card => {
      if (day === 'all' || (card.dataset.days && card.dataset.days.split(',').includes(day))) {
        card.style.display = 'flex';
      } else {
        card.style.display = 'none';
      }
    });

    document.querySelectorAll('.routine-filter-btn').forEach(btn => {
      btn.classList.remove('bg-white', 'text-indigo-600', 'shadow-sm');
      btn.classList.add('text-slate-400');
    });
    const activeBtn = document.getElementById('btn-' + day);
    if (activeBtn) {
      activeBtn.classList.remove('text-slate-400');
      activeBtn.classList.add('bg-white', 'text-indigo-600', 'shadow-sm');
    }
  }
  
  function toggleDropdown(id) {
    document.querySelectorAll('[id^="dropdown-"]').forEach(dropdown => {
      if (dropdown.id !== `dropdown-${id}`) dropdown.classList.add('hidden');
    });
    const dropdown = document.getElementById(`dropdown-${id}`);
    if (dropdown) dropdown.classList.toggle('hidden');
  }
  
  document.addEventListener('click', (e) => {
    if (!e.target.closest('[onclick^="toggleDropdown"]') && !e.target.closest('[id^="dropdown-"]')) {
      document.querySelectorAll('[id^="dropdown-"]').forEach(d => d.classList.add('hidden'));
    }
  });
  
  document.addEventListener('DOMContentLoaded', () => {
    filterRoutines('<%= Date.current.wday %>');
  });
</script>
