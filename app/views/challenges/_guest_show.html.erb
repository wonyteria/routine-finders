<% content_for :hide_nav, true %>

<style>
  /* Premium Dark Mode Overrides */
  .detail-content-env h2, .detail-content-env h3 { color: #ffffff !important; font-weight: 900 !important; }
  .detail-content-env p, .detail-content-env li, .detail-content-env span { color: #a1a1aa !important; }
  .detail-content-env .bg-white, .detail-content-env .bg-slate-50, .detail-content-env .bg-indigo-50, .detail-content-env .bg-orange-50, .detail-content-env .bg-emerald-50, .detail-content-env .bg-amber-50 { 
    background-color: rgba(255, 255, 255, 0.03) !important; 
    border: 1px solid rgba(255, 255, 255, 0.08) !important;
    border-radius: 24px !important;
  }
  .detail-content-env .text-slate-900, .detail-content-env .text-orange-900, .detail-content-env .text-emerald-900, .detail-content-env .text-amber-900 { color: #ffffff !important; }
  .detail-content-env .text-slate-600, .detail-content-env .text-slate-700, .detail-content-env .text-emerald-600, .detail-content-env .text-amber-600 { color: #a1a1aa !important; }

  /* Hide Scrollbar */
  .no-scrollbar::-webkit-scrollbar { display: none; }
  .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
</style>

<div class="relative -mx-4 min-h-screen bg-[#0C0B12]">
  <!-- 1. Immersive Hero (Fixed Aspect) -->
  <section class="relative h-[380px] w-full overflow-hidden">
    <% if @challenge.thumbnail_image.attached? %>
      <%= image_tag @challenge.thumbnail_image, class: "w-full h-full object-cover" %>
    <% else %>
      <img src="<%= @challenge.thumbnail %>" class="w-full h-full object-cover" alt="">
    <% end %>
    
    <div class="absolute inset-0 bg-gradient-to-b from-black/40 via-transparent to-[#0C0B12]"></div>
    
    <!-- Top Nav -->
    <div class="absolute top-10 left-6 right-6 flex justify-between items-center z-50">
      <button onclick="history.back()" class="w-10 h-10 rounded-full bg-black/40 backdrop-blur-xl border border-white/10 flex items-center justify-center text-white app-tap-active">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M15 19l-7-7 7-7"/></svg>
      </button>
      <button 
        data-controller="share"
        data-share-title-value="<%= @challenge.title %>"
        data-share-text-value="루틴 파인더에서 '<%= @challenge.title %>' 도전에 함께해요! 🌿"
        data-share-url-value="<%= challenge_url(@challenge) %>"
        data-action="click->share#share"
        class="w-10 h-10 rounded-full bg-black/40 backdrop-blur-xl border border-white/10 flex items-center justify-center text-white app-tap-active">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z"/></svg>
      </button>
    </div>

    <!-- Title Info -->
    <div class="absolute bottom-6 left-6 right-6 z-20 space-y-2">
      <div class="flex gap-2">
        <span class="px-2 py-0.5 rounded bg-indigo-500 text-[10px] font-black text-white uppercase tracking-tighter shadow-lg shadow-indigo-500/30">
          <%= @challenge.offline? ? 'Gathering' : 'Challenge' %>
        </span>
      </div>
      <h1 class="text-3xl font-black text-white leading-tight drop-shadow-xl"><%= @challenge.title %></h1>
    </div>
  </section>

  <!-- 2. Smart Category Switcher (Sticky Control) -->
  <div class="sticky top-0 z-40 bg-[#0C0B12]/80 backdrop-blur-3xl border-b border-white/5 py-4">
    <div class="flex gap-1 overflow-x-auto no-scrollbar px-6">
      <% 
        main_tabs = [
          { id: "intro", label: "상세설명", icon: "✨" },
          { id: "rules", label: "진행방법", icon: "⚖️" },
          { id: "rewards", label: "리워드", icon: "🎁" },
          { id: "community", label: "커뮤니티", icon: "💬" }
        ]
      %>
      <% main_tabs.each do |t| %>
        <%= link_to challenge_path(@challenge, tab: t[:id], source: 'prototype'), 
                    class: "shrink-0 px-5 py-2.5 rounded-full font-black text-xs transition-all flex items-center gap-1.5 border #{@tab == t[:id] ? 'bg-white text-[#0C0B12] border-white shadow-xl shadow-white/10' : 'text-slate-500 border-white/10 bg-white/[0.02]'}" do %>
          <span class="text-xs"><%= t[:icon] %></span>
          <%= t[:label] %>
        <% end %>
      <% end %>
    </div>
  </div>

  <!-- 3. Dynamic Content Area -->
  <section class="px-6 py-8 pb-44 detail-content-env">
    <% case @tab %>
    <% when "intro" %>
      <div class="space-y-10 animate-fade-in">
        <!-- Host Preview Card -->
        <div class="app-glass-card p-6 border-white/10 bg-white/[0.02] flex items-center gap-4">
          <div class="relative">
            <img src="<%= @challenge.host.profile_image %>" class="w-14 h-14 rounded-2xl border-2 border-indigo-500/30">
            <div class="absolute -right-1 -bottom-1 w-5 h-5 bg-indigo-500 rounded-full border-2 border-[#0C0B12] flex items-center justify-center">
               <svg class="w-3 h-3 text-white" fill="currentColor" viewBox="0 0 20 20"><path d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z"/></svg>
            </div>
          </div>
          <div>
            <p class="text-[10px] font-bold text-slate-500 uppercase">Challenge Host</p>
            <h3 class="text-base font-black text-white"><%= @challenge.host_name %></h3>
            <p class="text-[11px] font-medium text-slate-400 mt-0.5"><%= @challenge.host.bio.present? ? @challenge.host.bio.truncate(40) : "이 도전을 이끄는 열정적인 루퍼입니다." %></p>
          </div>
        </div>
        <%= render "challenges/tabs/intro", challenge: @challenge %>
      </div>

    <% when "rules", "records" %>
      <div class="space-y-8 animate-fade-in">
        <div class="p-6 bg-indigo-500/10 border border-indigo-500/20 rounded-3xl">
           <p class="text-indigo-300 text-xs font-bold leading-relaxed">
             💡 <%= @challenge.offline? ? "모임 시간과 장소를 사전에 숙지하여 노쇼가 발생하지 않도록 주의해주세요." : "매일 정해진 가이드에 맞춰 인증샷을 남겨주시면 성실함 지수가 올라갑니다." %>
           </p>
        </div>
        <%= render "challenges/tabs/rules", challenge: @challenge %>
        <% if @challenge.offline? && @challenge.meeting_info %>
          <div class="space-y-4">
             <h3 class="text-xl font-black text-white px-2">장소 정보</h3>
             <div class="w-full h-48 bg-slate-800 rounded-3xl flex items-center justify-center text-slate-500 font-bold border border-white/5">
                지도 데이터를 로딩 중...
             </div>
             <p class="text-xs font-bold text-white px-2"><%= @challenge.meeting_info.address %></p>
          </div>
        <% end %>
      </div>

    <% when "rewards" %>
      <div class="animate-fade-in">
        <%= render "challenges/tabs/rewards", challenge: @challenge %>
      </div>

    <% when "community" %>
      <div class="space-y-12 animate-fade-in">
         <div class="space-y-4">
            <h3 class="text-xl font-black text-white px-2">참여 멤버</h3>
            <%= render "challenges/tabs/participants", challenge: @challenge, participants: @participants %>
         </div>
         <div class="space-y-4">
            <h3 class="text-xl font-black text-white px-2">참여 후기 <span class="text-sm font-bold text-indigo-400 ml-1"><%= @reviews.count %></span></h3>
            <%= render "challenges/tabs/reviews", challenge: @challenge, reviews: @reviews, can_write_review: @can_write_review %>
         </div>
      </div>
    <% end %>
  </section>

  <!-- 4. Fixed Final Action Bar -->
  <div class="fixed bottom-8 left-1/2 -translate-x-1/2 w-[92%] max-w-sm z-[100]">
    <div class="app-glass-card p-4 !rounded-[32px] border-white/10 bg-[#16151D]/90 backdrop-blur-3xl shadow-[0_30px_60px_rgba(0,0,0,0.8)] flex items-center gap-4">
      <div class="flex-1 pl-4">
        <p class="text-[10px] font-bold text-slate-500 uppercase tracking-widest mb-1 leading-none">Total Fee</p>
        <div class="flex items-baseline gap-1">
          <span class="text-xl font-black text-white"><%= @challenge.cost_type_free? ? '0' : number_with_delimiter(@challenge.total_payment_amount) %></span>
          <span class="text-[10px] font-bold text-slate-500">원</span>
        </div>
      </div>
      
      <% if @is_host %>
         <%= link_to hosted_challenge_path(@challenge, source: params[:source]), class: "px-10 py-4 app-fab-gradient rounded-full text-sm font-black text-white active:scale-95 transition-all" do %>
           관리하기
         <% end %>
      <% elsif @is_joined %>
         <%= link_to challenge_path(@challenge, tab: 'records', source: 'prototype'), class: "px-10 py-4 bg-emerald-600 rounded-full text-sm font-black text-white active:scale-95 transition-all" do %>
           대시보드
         <% end %>
      <% else %>
        <button data-action="click->modal#open" class="px-10 py-4 app-fab-gradient rounded-full text-sm font-black text-white active:scale-95 transition-all">
           지금 참여
        </button>
      <% end %>
    </div>
  </div>

  <!-- Redesigned Modal -->
  <div data-modal-target="container" 
       data-action="click->modal#closeOnBackground" 
       class="fixed inset-0 z-[300] hidden bg-black/80 backdrop-blur-xl flex items-center justify-center p-6">
    <div class="app-glass-card w-full max-w-md bg-[#1B1A24] border-white/10 overflow-hidden animate-slide-up">
      <div class="p-8 text-center space-y-6">
        <div class="w-20 h-20 rounded-[32px] bg-indigo-500/10 flex items-center justify-center text-4xl mx-auto border border-white/5">
          <%= @challenge.category == 'HEALTH' ? '💪' : '🌱' %>
        </div>
        <h2 class="text-2xl font-black text-white tracking-tight"><%= @challenge.title %></h2>
        <p class="text-sm font-bold text-slate-500">함께 성취를 만들어볼까요?</p>

        <div class="pt-4 space-y-3">
          <% if @challenge.cost_type_free? %>
            <%= button_to join_challenge_path(@challenge, source: 'prototype'), method: :post, class: "w-full py-4.5 app-fab-gradient text-white rounded-2xl font-black text-sm active:scale-95 transition-all" do %>
              지금 시작하기
            <% end %>
          <% else %>
            <%= link_to new_challenge_application_path(@challenge, source: 'prototype'), class: "block w-full py-4.5 app-fab-gradient text-white rounded-2xl font-black text-sm active:scale-95 transition-all text-center" do %>
              신청 정보 확인
            <% end %>
          <% end %>
          <button data-action="click->modal#close" class="w-full py-2 text-slate-600 font-bold text-[10px] tracking-widest">Close</button>
        </div>
      </div>
    </div>
  </div>
</div>
