<!-- Stats Summary Banner -->
<section class="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-8">
  <% 
    total_completed = @monthly_completions.count
    avg_rate = @personal_routines.any? ? (total_completed.to_f / (@personal_routines.count * Date.current.day) * 100).clamp(0, 100).round : 0
    max_streak = @personal_routines.map(&:current_streak).max || 0
    top_category = @personal_routines.group_by(&:category).max_by { |_, v| v.count }&.first || "None"
  %>
  
  <div class="bg-white p-6 rounded-[32px] border border-slate-100 shadow-sm hover:shadow-md transition-all group relative overflow-hidden">
    <div class="absolute -right-4 -top-4 w-16 h-16 bg-indigo-50 rounded-full blur-2xl group-hover:bg-indigo-100 transition-colors"></div>
    <div class="relative z-10 space-y-2">
      <p class="text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Monthly Done</p>
      <div class="flex items-baseline gap-1">
        <h4 class="text-3xl font-black text-slate-900"><%= total_completed %></h4>
        <span class="text-xs font-bold text-slate-400">회</span>
      </div>
    </div>
  </div>

  <div class="bg-white p-6 rounded-[32px] border border-slate-100 shadow-sm hover:shadow-md transition-all group relative overflow-hidden">
    <div class="absolute -right-4 -top-4 w-16 h-16 bg-emerald-50 rounded-full blur-2xl group-hover:bg-emerald-100 transition-colors"></div>
    <div class="relative z-10 space-y-2">
      <p class="text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Avg. Success</p>
      <div class="flex items-baseline gap-1">
        <h4 class="text-3xl font-black text-emerald-600"><%= avg_rate %></h4>
        <span class="text-xs font-bold text-emerald-400">%</span>
      </div>
    </div>
  </div>

  <div class="bg-white p-6 rounded-[32px] border border-slate-100 shadow-sm hover:shadow-md transition-all group relative overflow-hidden">
    <div class="absolute -right-4 -top-4 w-16 h-16 bg-orange-50 rounded-full blur-2xl group-hover:bg-orange-100 transition-colors"></div>
    <div class="relative z-10 space-y-2">
      <p class="text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Max Streak</p>
      <div class="flex items-baseline gap-1">
        <h4 class="text-3xl font-black text-orange-500"><%= max_streak %></h4>
        <span class="text-xs font-bold text-orange-400">일</span>
      </div>
    </div>
  </div>

  <div class="bg-white p-6 rounded-[32px] border border-slate-100 shadow-sm hover:shadow-md transition-all group relative overflow-hidden">
    <div class="absolute -right-4 -top-4 w-16 h-16 bg-purple-50 rounded-full blur-2xl group-hover:bg-purple-100 transition-colors"></div>
    <div class="relative z-10 space-y-2">
      <p class="text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Main Focus</p>
      <div class="flex items-baseline gap-1">
        <h4 class="text-xl font-black text-purple-600 truncate"><%= top_category %></h4>
      </div>
    </div>
  </div>
</section>

<div class="grid grid-cols-1 lg:grid-cols-12 gap-8">
  <!-- Left Column: My Routines -->
  <div class="lg:col-span-8 space-y-8">
    <div class="flex flex-col md:flex-row md:items-center justify-between gap-6">
      <div class="flex items-center gap-3">
        <h2 class="text-2xl font-black text-slate-900 tracking-tight">마이 루틴 피드</h2>
        <span class="px-3 py-1 bg-slate-100 text-slate-500 rounded-full text-[10px] font-black uppercase"><%= @personal_routines.count %> Active</span>
      </div>
      
      <div class="flex bg-slate-100/50 backdrop-blur-sm p-1.5 rounded-2xl gap-1 overflow-x-auto no-scrollbar border border-slate-100">
        <% [["전체", "all"], ["월", "1"], ["화", "2"], ["수", "3"], ["목", "4"], ["금", "5"], ["토", "6"], ["일", "0"]].each do |label, value| %>
          <button type="button" 
                  onclick="filterRoutines('<%= value %>')"
                  id="btn-<%= value %>"
                  class="routine-filter-btn px-5 py-2 rounded-xl text-xs font-black transition-all <%= value == Date.current.wday.to_s ? 'bg-white text-indigo-600 shadow-sm ring-1 ring-slate-100' : 'text-slate-400 hover:text-slate-600' %>">
            <%= label %>
          </button>
        <% end %>
      </div>
    </div>

    <% if @personal_routines.any? %>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6" id="routine-container">
        <% @personal_routines.each do |routine| %>
          <div class="routine-card group bg-white p-7 rounded-[40px] border border-slate-100 shadow-sm hover:shadow-xl hover:-translate-y-1 transition-all flex flex-col gap-6 relative overflow-hidden" 
               data-days="<%= (routine.days || []).join(',') %>">
            
            <% if routine.completed_today? %>
              <div class="absolute -right-8 -top-8 w-24 h-24 bg-emerald-50 rounded-full blur-2xl"></div>
            <% end %>

            <div class="flex justify-between items-start relative z-10">
              <div class="w-16 h-16 <%= routine.completed_today? ? 'bg-emerald-500 text-white shadow-lg shadow-emerald-100' : 'bg-slate-50 text-slate-900 group-hover:bg-indigo-50 group-hover:text-indigo-600' %> rounded-[24px] flex items-center justify-center text-3xl transition-all duration-500 transform group-hover:scale-110">
                <%= routine.icon %>
              </div>
              <div class="flex gap-2">
                <%= button_to toggle_personal_routine_path(routine), method: :post, class: "w-12 h-12 rounded-2xl flex items-center justify-center transition-all #{routine.completed_today? ? 'bg-emerald-500 text-white shadow-lg shadow-emerald-200 ring-4 ring-emerald-50' : 'bg-slate-50 text-slate-300 hover:bg-emerald-50 hover:text-emerald-500 hover:scale-105'}" do %>
                  <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"/></svg>
                <% end %>
                <div class="relative" data-controller="dropdown">
                  <button type="button" class="w-12 h-12 bg-slate-50 text-slate-300 rounded-2xl flex items-center justify-center hover:bg-slate-100 transition-all">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M12 5v.01M12 12v.01M12 19v.01M12 6a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2z"/></svg>
                  </button>
                </div>
              </div>
            </div>

            <div class="space-y-4 relative z-10">
              <div>
                <p class="text-[10px] font-black text-slate-300 uppercase tracking-[0.2em] mb-1"><%= routine.category %></p>
                <h4 class="text-2xl font-black text-slate-900 <%= 'text-slate-300 line-through' if routine.completed_today? %> transition-all tracking-tight"><%= routine.title %></h4>
              </div>
              
              <div class="flex items-center justify-between pt-5 border-t border-slate-50">
                <div class="flex gap-1.5">
                  <% days_map = { "1" => "월", "2" => "화", "3" => "수", "4" => "목", "5" => "금", "6" => "토", "0" => "일" } %>
                  <% ["1","2","3","4","5","6","0"].each do |d| %>
                    <span class="w-7 h-7 flex items-center justify-center text-[10px] font-black rounded-lg transition-colors <%= (routine.days || []).include?(d) ? (routine.completed_today? ? 'bg-emerald-50 text-emerald-600' : 'bg-indigo-50 text-indigo-600') : 'text-slate-200 bg-transparent' %>">
                      <%= days_map[d] %>
                    </span>
                  <% end %>
                </div>
                <div class="flex flex-col items-end">
                  <span class="text-[11px] font-black <%= routine.completed_today? ? 'text-emerald-500' : 'text-orange-500 text-opacity-70' %> flex items-center gap-1">
                    🔥 <%= routine.current_streak %>일 연속
                  </span>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="bg-indigo-50/30 border-2 border-dashed border-indigo-100 rounded-[40px] p-24 text-center space-y-6">
        <div class="w-20 h-20 bg-white rounded-3xl flex items-center justify-center text-4xl mx-auto shadow-sm">✨</div>
        <div class="space-y-2">
          <p class="text-slate-900 font-black text-xl">첫 번째 루틴을 만들어보세요!</p>
          <p class="text-slate-500 font-medium">관리할 루틴이 추가되면 기상, 독서 등 다양한 습관을 추적할 수 있습니다.</p>
        </div>
        <%= link_to "#", class: "inline-block px-8 py-4 bg-indigo-600 text-white rounded-2xl font-black text-sm shadow-xl shadow-indigo-100" do %>
          시작하기
        <% end %>
      </div>
    <% end %>
  </div>

  <!-- Right Column: Sidebar -->
  <div class="lg:col-span-4 space-y-8">
    <!-- Achievement Matrix (Calendar Styles) -->
    <section class="bg-white rounded-[40px] p-8 border border-slate-100 shadow-sm space-y-8 relative overflow-hidden group">
      <div class="absolute -right-4 -top-4 w-24 h-24 bg-indigo-50/50 rounded-full blur-2xl group-hover:scale-110 transition-transform"></div>
      
      <div class="relative z-10 flex items-center justify-between">
        <h3 class="text-xl font-black text-slate-900 tracking-tight">성취 매트릭스</h3>
        <span class="text-[10px] font-black text-slate-400"><%= Date.current.strftime("%B") %></span>
      </div>

      <div class="relative z-10 space-y-4">
        <div class="grid grid-cols-7 gap-1">
          <% ["S", "M", "T", "W", "T", "F", "S"].each do |day| %>
            <div class="text-[9px] font-black text-slate-300 text-center py-2"><%= day %></div>
          <% end %>

          <% 
            start_of_month = Date.current.beginning_of_month
            grid_start = start_of_month - start_of_month.wday
            end_of_month = Date.current.end_of_month
            grid_end = end_of_month + (6 - end_of_month.wday)
          %>
          
          <% (grid_start..grid_end).each do |date| %>
            <% 
              is_current_month = date.month == Date.current.month
              count = @personal_routines.select { |r| r.completed_on?(date) }.count 
              color_class = case
              when !is_current_month then "bg-transparent opacity-0"
              when count == 0 then "bg-slate-50"
              when count <= 2 then "bg-indigo-100"
              when count <= 4 then "bg-indigo-300"
              else "bg-indigo-600"
              end
              
              is_today = date == Date.current
            %>
            <div class="aspect-square rounded-lg <%= color_class %> transition-all relative group/day <%= 'ring-2 ring-indigo-500 ring-offset-2 scale-90' if is_today && is_current_month %>" 
                 title="<%= date %>: <%= count %> routines">
              <% if is_today && is_current_month %>
                <span class="absolute -top-1 -right-1 w-2 h-2 bg-indigo-500 rounded-full"></span>
              <% end %>
              <div class="absolute inset-0 flex items-center justify-center opacity-0 group-hover/day:opacity-100 transition-opacity pointer-events-none">
                 <span class="text-[8px] font-black <%= count > 2 ? 'text-white' : 'text-indigo-600' %>"><%= date.day %></span>
              </div>
            </div>
          <% end %>
        </div>

        <div class="flex justify-between items-center pt-4 border-t border-slate-50">
          <div class="flex items-center gap-1.5">
            <div class="w-3 h-3 rounded-full bg-indigo-600"></div>
            <span class="text-[10px] font-bold text-slate-500">Perfect Day</span>
          </div>
          <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest">
            <%= @monthly_completions.count %> DONE THIS MONTH
          </p>
        </div>
      </div>
    </section>

    <!-- Custom Routine Form -->
    <section class="bg-indigo-600 rounded-[40px] p-8 text-white space-y-6 shadow-xl shadow-indigo-100 relative overflow-hidden group">
      <div class="absolute -right-10 -bottom-10 w-40 h-40 bg-white/5 rounded-full blur-3xl group-hover:scale-110 transition-transform"></div>
      
      <div class="relative z-10 space-y-1">
        <h3 class="text-xl font-black">새 루틴 추가</h3>
        <p class="text-indigo-100/70 text-sm font-bold tracking-tight">오늘부터 시작할 작은 습관</p>
      </div>

      <%= form_with model: PersonalRoutine.new, class: "relative z-10 space-y-4" do |f| %>
        <div class="space-y-4">
          <%= f.text_field :title, placeholder: "어떤 루틴인가요?", required: true, class: "w-full px-5 py-4 bg-white/10 border-white/10 rounded-2xl text-white placeholder-white/30 font-black text-sm focus:ring-4 ring-white/10 border-none transition-all" %>
          
          <div class="grid grid-cols-2 gap-3">
             <div class="space-y-1.5">
               <label class="text-[10px] font-black text-indigo-200 uppercase tracking-widest pl-1">Icon</label>
               <%= f.select :icon, ["🔥", "⭐", "💪", "📖", "🥦", "🏃", "🎸", "🧘", "💊", "💧", "👀", "📝"], {}, class: "w-full px-4 py-3 bg-white/10 border-white/10 rounded-2xl text-white font-black text-sm appearance-none cursor-pointer border-none focus:ring-4 ring-white/10" %>
             </div>
             <div class="space-y-1.5">
               <label class="text-[10px] font-black text-indigo-200 uppercase tracking-widest pl-1">Category</label>
               <%= f.select :category, ["HEALTH", "LIFE", "MIND", "HOBBY", "STUDY"], {}, class: "w-full px-4 py-3 bg-white/10 border-white/10 rounded-2xl text-white font-black text-sm appearance-none cursor-pointer border-none focus:ring-4 ring-white/10" %>
             </div>
          </div>

          <div class="space-y-2">
            <label class="text-[10px] font-black text-indigo-200 uppercase tracking-widest pl-1">Repeat Days</label>
            <div class="flex justify-between gap-1">
              <% [["월", 1], ["화", 2], ["수", 3], ["목", 4], ["금", 5], ["토", 6], ["일", 0]].each do |label, value| %>
                <label class="flex-1">
                  <%= check_box_tag "personal_routine[days][]", value, true, class: "peer hidden" %>
                  <div class="w-full py-2.5 text-center rounded-xl bg-white/5 text-white/40 font-black text-[10px] cursor-pointer peer-checked:bg-white peer-checked:text-indigo-600 transition-all hover:bg-white/10">
                    <%= label %>
                  </div>
                </label>
              <% end %>
            </div>
          </div>
        </div>

        <%= f.submit "루틴 생성하기", class: "w-full py-4 bg-white text-indigo-600 rounded-2xl font-black text-sm hover:scale-[1.02] active:scale-95 transition-all shadow-xl shadow-indigo-900/20 cursor-pointer mt-2" %>
      <% end %>
    </section>

    <!-- Recommended Routines -->
    <section class="space-y-6">
      <div class="flex items-center justify-between px-2">
        <h4 class="text-sm font-black text-slate-400 uppercase tracking-[0.2em]">Recommended</h4>
        <div class="h-px flex-1 bg-slate-100 ml-4"></div>
      </div>
      
      <div class="space-y-3">
        <% @recommended_routines.take(5).each do |routine| %>
          <div class="bg-white p-5 rounded-[28px] border border-slate-100 flex items-center justify-between hover:border-indigo-100 hover:shadow-md transition-all group">
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 bg-slate-50 rounded-2xl flex items-center justify-center text-2xl group-hover:scale-110 transition-transform">
                <%= routine[:icon] %>
              </div>
              <div>
                <h4 class="text-sm font-black text-slate-900 group-hover:text-indigo-600 transition-colors"><%= routine[:title] %></h4>
                <p class="text-[9px] font-bold text-slate-400 uppercase tracking-widest"><%= routine[:category] %></p>
              </div>
            </div>
            <%= button_to personal_routines_path(personal_routine: { title: routine[:title], category: routine[:category], icon: routine[:icon], days: [1,2,3,4,5,6,0] }), method: :post, class: "w-10 h-10 bg-slate-50 text-slate-400 rounded-2xl flex items-center justify-center hover:bg-indigo-600 hover:text-white transition-all shadow-sm" do %>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="4" d="M12 6v12m6-6H6"/></svg>
            <% end %>
          </div>
        <% end %>
      </div>
    </section>
  </div>
</div>

<script>
  function filterRoutines(day) {
    document.querySelectorAll('.routine-card').forEach(card => {
      if (day === 'all' || (card.dataset.days && card.dataset.days.split(',').includes(day))) {
        card.style.display = 'flex';
      } else {
        card.style.display = 'none';
      }
    });

    document.querySelectorAll('.routine-filter-btn').forEach(btn => {
      btn.classList.remove('bg-white', 'text-indigo-600', 'shadow-sm', 'ring-1', 'ring-slate-100');
      btn.classList.add('text-slate-400');
    });
    const activeBtn = document.getElementById('btn-' + day);
    if (activeBtn) {
      activeBtn.classList.remove('text-slate-400');
      activeBtn.classList.add('bg-white', 'text-indigo-600', 'shadow-sm', 'ring-1', 'ring-slate-100');
    }
  }
  
  document.addEventListener('DOMContentLoaded', () => {
    filterRoutines('<%= Date.current.wday %>');
  });
</script>

