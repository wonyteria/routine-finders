<div class="max-w-7xl mx-auto">
  <div class="flex flex-col md:flex-row justify-between items-start md:items-end gap-6 mb-12">
    <div>
      <div class="flex items-center gap-3 mb-2">
        <a href="/admin" class="p-2 bg-white/5 hover:bg-white/10 rounded-lg text-slate-400 hover:text-white transition-all">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/></svg>
        </a>
        <h1 class="text-4xl font-black tracking-tight">Banner & Marketing</h1>
      </div>
      <p class="text-slate-400 font-medium">서비스 메인 배너와 주요 광고 영역을 관리합니다.</p>
    </div>

    <%= link_to new_admin_banner_path, class: "px-8 py-4 bg-indigo-600 hover:bg-indigo-500 rounded-2xl text-sm font-black transition-all shadow-lg shadow-indigo-600/30 flex items-center gap-2" do %>
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M12 4v16m8-8H4"/></svg>
      Add New Banner
    <% end %>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
    <% @banners.each do |banner| %>
      <div class="admin-glass rounded-[40px] overflow-hidden group">
        <div class="aspect-[16/9] relative overflow-hidden bg-slate-800">
          <% if banner.image_url.present? %>
            <img src="<%= banner.image_url %>" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700">
          <% else %>
            <div class="w-full h-full flex items-center justify-center text-slate-700">
              <svg class="w-20 h-20" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>
            </div>
          <% end %>
          <div class="absolute inset-0 bg-gradient-to-t from-slate-900 via-transparent to-transparent opacity-80"></div>
          
          <div class="absolute top-4 left-4 flex gap-2">
            <span class="px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-widest bg-black/40 backdrop-blur-md border border-white/10 text-white">
              POS. <%= banner.position %>
            </span>
            <span class="px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-widest <%= banner.active? ? 'bg-emerald-500/20 text-emerald-400 border-emerald-500/20' : 'bg-red-500/20 text-red-400 border-red-500/20' %> backdrop-blur-md">
              <%= banner.active? ? 'Live' : 'Hidden' %>
            </span>
          </div>
        </div>

        <div class="p-8">
          <div class="mb-6">
            <p class="text-[10px] font-black text-indigo-400 uppercase tracking-[0.2em] mb-2"><%= banner.category || "General Promotion" %></p>
            <h3 class="text-xl font-black mb-2 line-clamp-1"><%= banner.title %></h3>
            <p class="text-sm text-slate-500 font-medium line-clamp-2"><%= banner.subtitle %></p>
          </div>

          <div class="flex gap-3">
            <%= link_to edit_admin_banner_path(banner), class: "flex-1 py-3 bg-white/5 hover:bg-white/10 rounded-xl text-xs font-black text-center transition-all border border-white/5" do %>
              Edit Content
            <% end %>
            <%= button_to admin_banner_path(banner), method: :delete, data: { turbo_confirm: "삭제하시겠습니까?" }, class: "px-4 py-3 bg-red-500/10 hover:bg-red-500 text-red-400 hover:text-white rounded-xl transition-all border border-red-500/20 shadow-sm" do %>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/></svg>
            <% end %>
          </div>
        </div>
      </div>
    <% end %>
    
    <% if @banners.empty? %>
      <div class="lg:col-span-3 py-32 text-center admin-glass rounded-[40px] border-dashed">
        <p class="text-slate-600 font-bold italic mb-4">현재 등록된 배너가 없습니다.</p>
        <a href="<%= new_admin_banner_path %>" class="text-indigo-400 font-black hover:underline">[+] 새 배너 추가하기</a>
      </div>
    <% end %>
  </div>
</div>
